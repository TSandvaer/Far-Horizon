# 3D Character Pipeline — Hyper3D Rodin → Mixamo → Unity

**Status:** Gen + auto-rig steps **validated 2026-06-15** (a chunky-low-poly castaway generated, rigged, and cleanly animating Idle+Walk in Mixamo's preview). In-engine/URP adoption verification is the open step (ticket `86ca8r72j`). The *recipe* below is reusable regardless of whether any single generated asset is adopted — it applies to future characters and props too.

> **Route ratification (2026-07-05, castaway v2) — this is now the ONLY hero-character route.** Seven procedural Blender attempts at a hero-character rebuild each failed the Sponsor's bar on GESTALT — the character as a WHOLE didn't land, not any itemized/fixable defect; every round cleared its named defects and the verdict stayed "still not there." Switching to this doc's route succeeded in under an hour. **Diagnostic to reuse:** when a round clears every itemized defect and still doesn't land, that's a gestalt failure, not a residual-bugs failure — switch routes immediately (sharpens `asset-routing.md`'s "2+ attempts fail → switch" rule). Procedural `bpy` modeling stays first-choice for props/weapons/world; for hero characters it is a closed option, not a discouraged one. **Castaway v2 is a candidate, not shipped** — the OLD castaway stays LIVE in-game until v2 is soak-approved (identity note: `art-direction.md`).

> **Scope nuance (2026-07-18, castaway v4) — the "closed option" ruling above applies to ORGANIC-fidelity hero rebuilds, not to hand-modeling in general.** A hand-modeled Blender attempt at a **chamfered-blocky "wooden toy"** hero (40 segmented parts, ~3.9 heads tall, painted face + geometry nose, mitten+thumb hands — Minecraft-adjacent, NOT organic musculature) PASSED the Sponsor's look-dev gate first-try via Blender MCP — the same tool the seven 2026-07-05 organic-fidelity attempts failed on. **Read this as:** procedural/hand Blender modeling stays a closed option for organic-fidelity heroes; it is back OPEN for a deliberately geometric/blocky stylization, evaluated on its own merits each time — do not blanket-cite the 2026-07-05 ruling against a non-organic target. **v4 is a look-dev-approved CANDIDATE only** — v3 remains the live hero until v4 clears Mixamo rig + Unity staged-toggle + soak (see "Rolling out a NEW character version" below). Full recipe + artifacts: `art-src/castaway-v4-README.md`, `art-src/castaway_v4.blend`, `art-src/castaway_v4_palette.png`, `art-src/castaway-v4-renders/judge_*.png`.
>
> **Process lesson for any future hand-modeled hero attempt (2026-07-18) — gate TWICE, not once at the end.** All seven 2026-07-05 procedural-hero failures (route-ratification blockquote above) were judged only at completion, after full polish investment was sunk into every failed round. Castaway v4 passed first-try using two touchpoints: (1) a grey-blockout SILHOUETTE gate before any polish effort — a cheap gestalt check before texture/shading work is sunk — then (2) a 6-shot judge set (front/side/¾/back/face-closeup/gameplay-distance, plus a side-by-side A/B against the current live hero) before declaring done. Apply this two-gate structure to any future from-scratch hero modeling attempt, procedural or hand-authored.

> **Topology refinement (2026-07-06, castaway v3) — Smart-Low-poly(BETA) Type=Quad Density=Low is the Sponsor-locked low-poly hero topology.** 3,605 quads / ~7.4k tri-equiv, real facets, painted face kept — taken directly from Rodin's Smart-Low-poly output, NOT via Quad-8000 + Blender retopo. The Quad-8000 → QuadriFlow-retopo (3k target) route cleared every itemized check (density Sponsor-picked, regions cleaned, face positions measured) yet was GESTALT-REJECTED by the Sponsor — second empirical case for the gestalt diagnostic above, with a new corollary: **a gestalt failure can mean the wrong SETTING within the same tool, not only the wrong route family.** Erik's 1.5–3k triangle-band recommendation (`team/erik-consult/lowpoly-hero-conversion-research.md`, harvest PR pending) is SUPERSEDED by this Sponsor verdict at ~7.4k tri-equiv — do not re-propose the lower band for the hero. (86cak41d4)

This is the third asset-creation route for Far Horizon, alongside **procedural** (`LowPolyMeshes`/`FacetedRock`) and **Blender/Blender-MCP sourcing**. Reach for it when you want a generated, on-style chunky low-poly **character** (the procedural route fights organic humanoid shapes; see `[[unity-conventions]]` §Asset creation "source when procedural fights the style").

## Route at a glance
openai-image concept → **Hyper3D Rodin** Image-to-3D (mesh) → **Mixamo** auto-rig (Hyper3D output is UN-rigged) → **Unity Generic rig** (NOT Humanoid — see Step 4).

## Step 1 — Concept reference (openai-image)
- **Image-to-3D ≫ Text-to-3D** for a specific look. Generate a full-body character, plain solid background, even lighting, single centered subject (`mcp__openai-image__text-to-image`, portrait `1024x1536`, quality `high`). Iterate identity with `image-to-image` off a chosen base to keep the character consistent.
- ⚠️ **POSE IS DRIVEN BY THE REFERENCE IMAGE, not the prompt.** In Rodin Image-to-3D the mesh copies the reference's pose. Rodin's **"T/A Pose" button only inserts the text "T/A Pose" into the prompt — it does NOT re-pose the mesh.** An arms-down (I-pose) reference → arms-down mesh, which is the *hard* case for auto-rig (shoulder/armpit weights pinch). To get a riggable mesh, **supply an A-pose reference** (arms ~45° down-and-out, clear gaps from torso, feet shoulder-width) — re-pose the concept via `image-to-image` ("keep the character identical, change ONLY the pose to a clean symmetric A-pose…"). A/T-pose costs nothing extra here and saves a wasted generation later.
- **Concept-prep checklist (castaway v2, 2026-07-05) — each item prevents a wasted generation:**
  - **Strip ALL gear** (straps, wristbands, cloth tabs — anything wearable). Anything drawn on the character fuses permanently into the single generated mesh; wearables get built later as separate modular palette meshes on the same skeleton (the customization goal).
  - **Explicitly prompt the render style** (e.g. "faceted low-poly 3D render style"). Rodin echoes the SURFACE STYLE of the input, not just the silhouette — a soft-2D-illustration concept cues smooth/rounded output geometry even with correct pose and mesh settings.
  - **Empty open hands, no held props** — a spear/tool in the reference fuses into the mesh exactly like gear; held props come from the weapon pipeline as separate assets.
  - **Check the expression before generating** — a first castaway-v2 concept pass read "angry" and needed Sponsor correction to friendly-neutral; expression carries through Rodin as much as pose does.

## Step 2 — Hyper3D Rodin (web UI, hyper3d.ai, Creator tier)
- Use the **3D** tab. **NOT the Avatar tab** — that is ChatAvatar = animatable *faces* only + realistic → dead end for a stylized full body (verified).
- **Image to 3D** → upload the A-pose reference.
- Model **Gen-2.5**. **Turbo** is the cheap iterate mode (protect credits — Creator tier is limited); **Gen-2.5 / High** for the final. A clean reference often nails it in one Gen-2.5 pass.
- Geometry presets: **Symmetric** ON for a humanoid; leave Detailed/Soft default.
- On **Confirm**, the topology picker offers Smart-Low-poly(BETA) / Triangle / **Quad Mesh**:
  - **Smart-Low-poly(BETA), Type=Quad, Density=Low — DEFAULT for the low-poly hero (castaway v3, `86cak41d4`).** Produces real faceted geometry directly (3,605 quads / ~7.4k tri-equiv for the castaway) with the painted face kept intact — no retopo pass needed. This is the Sponsor-locked route; do NOT route a low-poly hero through Quad Mesh + Blender retopo (that conversion cleared every itemized check and was GESTALT-REJECTED — see the 2026-07-06 blockquote above).
  - **Quad Mesh** (density **~8000**; 4000 = chunkier/leaner; keep **Baked Normal** on) — use when a SMOOTH/quad-dense mesh is the actual goal, or as base geometry for further sculpt work; **quads deform far better than triangles when rigged** on either quad-based option.
  - It then upsells "Confirm via Triangular for more details" → click **Keep Quads** (Triangular = a sharp tri mesh up to **1M** polys = far too heavy + worse deformation).
  - "Is the generated object symmetrical?" → **Yes** for a humanoid (cleaner mirrored topology/weights).
  - Texture treatment for the Smart-Low-poly hero: HSV-posterized de-lit diffuse ("posterized flats", Sponsor-locked) — recipe lives in `blender-asset-pipeline.md` §2, color-pipeline gotcha 4; don't re-derive it here.
- **Material:** **De-light ON** — strips baked shadows so the albedo is flat and lights correctly in-engine (fits the URP toon look). **Skip Face Restore** (pushes the toon face toward realism). HD/4K unnecessary — 2K is plenty.
- **Pack/Export:** **Base Model** (not LOD/High-poly), format **.fbx**, **Shaded + PBR**, **2K** → **Download** (not "Send", which is the Blender-send path). Export is a folder/zip: `base*.fbx` + `texture_diffuse|normal|roughness|metallic|pbr.png`.
- Alternative driver: the **Blender MCP** has Hyper3D Rodin tools (`mcp__blender__generate_hyper3d_model_via_images`/`_text`, `poll_rodin_job_status`, `import_generated_asset`) — but they need the **Blender addon connected** (`get_hyper3d_status` errors otherwise). The web UI is the no-setup route.
  - ⚠️ **The MCP FREE-TRIAL key route is DEAD (confirmed 2026-07-05) — do not retry or debug around it.** With the addon's built-in trial key set (`set_hyper3d_free_trial_api_key`, MAIN_SITE mode), `generate_hyper3d_model_via_images` returns HTTP 400 `"Unsupported or invalid image: Input buffer contains unsupported image format"` for ANY input — verified with a genuine 8-bit PNG and a small RGB JPEG, so the error text is a red herring (the endpoint/key is dead, not the image). The Sponsor's Creator tier has no API access either (`unity-conventions.md` §Asset creation), so there is currently NO working MCP path to Rodin, trial or paid — the web-UI upload above is the only route. Don't burn a cycle re-encoding images against this 400.

## Step 3 — Mixamo auto-rig (mixamo.com, free Adobe account)
Hyper3D output ships **un-rigged**; Mixamo adds the skeleton + skin weights + retargetable clips.
- **Upload Character** → the `.fbx` (use the `_basic_shaded` variant so the preview shows color). Auto-Rigger: drag markers onto **chin, both wrists, both elbows, both knees, groin**; **Use Symmetry** ON; **Skeleton LOD = Standard Skeleton (65)** (a clean, standard bone hierarchy — binds cleanly by transform path under the Generic rig; finger bones are harmless on mitten hands).
- Apply **Idle** + **Walking**; watch hips/knees/shoulders/elbows for pinch/collapse. **Mixamo previews ONE animation at a time** — applying a new clip replaces the on-screen one; it is NOT lost, each downloads separately.
- **Download split for Unity (Generic rig):** the character **With Skin** once (= mesh + rig + that clip) + each animation **Without Skin** (= skeleton+anim only, binds by transform path under Generic). Format **FBX for Unity(.fbx)**, **30 fps**, Keyframe Reduction **None**. Sanity check: with-skin FBX is multi-MB (carries the mesh); without-skin is ~hundreds of KB.
- **T-pose is Mixamo's "best results" advice, not a requirement (castaway v2, 2026-07-05).** A clean A-pose upload (as prepped in Step 1) auto-rigs fine — the marker drop (chin/wrists/elbows/knees/groin) teaches the rigger the joints regardless of pose. Don't re-pose a working A-pose reference just because the upload screen suggests T-pose.
- **Standard Skeleton bone count VARIES — bone COUNT does not track finger geometry (castaway v2 + v3, `86cak41d4`).** v2's 41-bone rig had NO finger bones; v3's 41-bone rig (Smart-Low-poly mesh, 2026-07-06) totals 41 while INCLUDING 16 finger bones — the earlier "count tracks finger-geometry presence" hypothesis is FALSIFIED. Treat the raw count as informational only; what to actually verify on each new export: root is `mixamorig:Hips`, 0 unweighted vertices, and the bone NAMES list. Either shape works — Humanoid retargeting is muscle-space (bone-count-agnostic) and Generic binds by `mixamorig:*` transform path/name, so an existing clip set carries over at 41, 46, or 65 bones.

## Step 4 — Unity
- Import the FBX. **Rig = GENERIC, NOT Humanoid** (hard-won — adoption 2026-06-15, ticket `86ca8rdkp` / PR #47): the Mixamo **Humanoid** rig **EXPLODES the skinned mesh into a cone at runtime** when the character sits under a SCALED scene hierarchy (the muscle retarget fights the parent scale — body flung far off-spawn, hand bones at thousands of units). **Generic** (transform-path binding, no muscle retarget) renders clean at the player. With-skin FBX carries the mesh + skeleton; without-skin clips bind by transform path (no avatar-copy step needed for Generic). URP material with `texture_diffuse` as Base Map (de-lit albedo → flat/toon).
- ⚠ **A spike/capture camera that FRAMES or FOLLOWS the mesh bounds can HIDE a runtime displacement** that a fixed/anchored gameplay camera would expose: the viability spike's Humanoid captures looked clean ONLY because the capture camera tracked the displaced mesh. When validating a rig, capture from the ANCHORED gameplay camera (or a fixed world camera) — not one that re-frames to the mesh. (Sibling of the `smr.bounds` capture trap in `[[unity-conventions]]`.)
- Mixamo bones carry **lossyScale ~1** (e.g. `mixamorig:RightHand`) — NO 267×-lossy-bone trap (unlike the old Sketchfab chibi's `RightHand_010`); held props can pose hand-local without the world-offset workaround.
- **Judge from a real build** (project lore: editor previews lied on the procedural humanoid — the "legs-up" incident, `[[unity-conventions]]` §editor-vs-runtime). The shipped-exe look is what the Sponsor judges.

## Cost & artifacts
- **Hyper3D** Creator tier = limited credits (Turbo cheap, Gen-2.5/High more — iterate on Turbo). **Mixamo** is free. openai-image concepts don't touch Hyper3D credits — generate references freely.
- Artifact convention: `inspiration/hyper3d-castaway/` — `concepts/` (openai-image refs), the gen GUID subfolder (`base*.fbx` + `texture_*.png`), and the Mixamo `Idle.fbx`(with-skin)/`Walking.fbx`(without-skin). These are untracked spike inputs unless promoted into `Assets/`.
- **Updated artifact convention (castaway v2, 2026-07-05):** new character generations land under **`art-src/`** (the project's established source-file location, mirroring `art-src/weapons_reauthor.blend`) — e.g. `art-src/castaway-rodin-export/` with a provenance `README.md` (concept history, Rodin/Mixamo settings, Unity-integration handoff) + `art-src/castaway_concept_apose.png`. Copy the provenance-README pattern for future exports — it's what lets a later session pick up the handoff without re-deriving the pipeline. The `inspiration/hyper3d-castaway/` convention above remains valid HISTORY for the original castaway's artifacts.

## Rolling out a NEW character version (staged toggle — castaway v2, 2026-07-05)
When SWAPPING the live character for a new version, do NOT rip out the old base — stage it so `main` stays always-shippable and the Sponsor gates the *activation*, not the merge:
1. **Integrate behind a default-OFF env toggle** (`UseCastawayV2Default=false`, overridable by `FARHORIZON_CASTAWAY_V2=1`). The integration PR then merges **dormant-safe** — the reviewer verifies "toggle OFF ⇒ old base byte-unchanged, every consumer gated" — so merging ships nothing new. This decouples the (safe, anytime) merge from the risky activation.
2. **Soak the toggled-off version via an env-var build:** the default CI/build shows the OLD base, so cut the soak build with the env var set (`FARHORIZON_CASTAWAY_V2=1` → bootstrap bakes the new base). ALWAYS open the identity capture and confirm the NEW character actually rendered before serving — a soak build that silently shows the old base is worse than useless (`[[verify-soak-builds-or-bake-and-judge]]`).
3. **Activate via a separate default-flip PR** (on soak-PASS): set the default true. This PR must **reconcile the capture gates** — every gate that frames the character now renders the new one, so update the goldens to the correct new-character frame (never weaken the gate to pass). Re-seat held props on the new hand (bone-axis re-measure, `[[procedural-animation-verbs]]`) as part of activation — the seat is rig-dependent, so old-rig prior values will be wrong.
