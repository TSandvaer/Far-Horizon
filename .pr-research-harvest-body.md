## docs(team): harvest uncommitted research/spec notes from orch worktree

Churn-triage harvest. ~25 files were sitting **uncommitted in the orchestrator's main worktree** (`C:/Trunk/PRIVATE/Far-Horizon`) ahead of an orch reset. This PR preserves the genuinely-new research/spec/doc notes (inventory category 2) so the reset doesn't drop them. **Read/cp only from the orch worktree — no git ops were run against it.**

### Harvested (11 files, all verified ABSENT from `origin/main`)

**`team/erik-consult/` (4 — research)**
- `concurrent-unity-build-isolation-research.md`
- `game-juice-concepts.md`
- `playmode-enter-headless-deadlock-research.md`
- `ui-toolkit-inventory-settings-research.md`

**`team/uma-ux/` (3 — UX direction/spec)**
- `need-meter-3bar-direction.md`
- `ui-iconography-sourcing.md`
- `ui-toolkit-panels-ux-spec.md`

**`team/tess-qa/` (2 — acceptance plans)**
- `inventory-acceptance-plan.md`
- `settings-panel-acceptance-plan.md`

**`team/spikes/` (2 — new dir)**
- `unity-build-slot-shortening-spike.md`
- `unity-concurrent-build-cache-isolation-spike.md`

### Excluded (per dispatch)
- **15 castaway FBX** (`Assets/Art/Character/Castaway/*.fbx`) — Devon is preserving these separately.
- **Junk** — `.claude/settings.json.bak`, `.pr79-body.md`, the stray temp `…berryBush.cs`.
- **Transient** — `.claude/auto-status.state`.
- **Already on `origin/main`** — these showed as `??` in the (stale-HEAD) orch worktree but are merged on main, so NOT re-added: `team/erik-consult/{blender-weapon-asset-pipeline,grass,low-poly-trees,sky-clouds,water-shader,world-prop-quality}-research.md`, `team/erik-consult/ui-toolkit-vs-ugui-fh.md`, `team/uma-ux/weapon-tool-style-spec.md`, and `team/priya/dev-tweak-console-spec.md` (on main at `team/priya/`, content identical).

### Not harvested — flagging for the orchestrator to decide
- **`inspiration/hyper3d-castaway/`** — the Hyper3D castaway concept/model assets (FBX + concept PNGs + PBR textures). Asset binaries, not research/spec notes; adjacent to the FBX class Devon owns. Left out of this docs PR — orchestrator to route (Devon? separate asset PR?).
- **`team/tess-qa/caps-pr109-9fbf275/`** — QA capture PNGs + `captures.zip` for PR #109 (build-stamp evidence). Capture artifacts, not a spec; `Captures/` is `.gitignore`'d by convention so these may be intentionally transient. Left out — orchestrator to confirm.
- **Untracked skill dirs** (`.claude/skills/{non-orchestration-session,read-obsidian-folder,runner-autostart,runner-window-tamer}/`) and **modified `.claude/` config + `team/STATE.md` / `team/DECISIONS.md`** — orchestrator/infra surface, out of scope for a docs harvest.

### Notes
- Branch cut from `origin/main`; commit is docs-only (no code, no `.claude/settings.json`, no `DECISIONS.md`).
- Reviewer: **Devon** (light docs read — no `codereview` skill needed for docs-only).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
