# Team State — Far Horizon

This file is the orchestrator's source of truth between ticks. The first section is always the live "Resume next-action" header — if a session dies, the next orchestrator starts there.

---

## RESUME NEXT-ACTION — 2026-06-17 (BIG MERGE + Erik wave + WASD ✅ DONE; run-on-Shift IN FLIGHT; locomotion-first confirmed)

**If this session died: `main` @ `af004c5`. DONE: the big merge (#50 baseline) + the Erik adopt-wave (#58/#59/#60/#61) + WASD locomotion (#63, Sponsor feel-approved) + whip pre-stage docs (#64 DECISIONS+plan / #65 inventory-QA-strategy / #66 gameplay-UI-direction). **IN FLIGHT: Devon on run-on-Shift `86ca9yq34`** (agentId `acd3003fddb8a20fe`; branch `devon/86ca9yq34-run-on-shift`; integrating the Mixamo `Running.fbx` — Walk↔Run blend + finger-curl + grounding) — holds the SINGLE Unity build slot (others justified-idle behind it). **SPONSOR-CONFIRMED SEQUENCE (2026-06-17 walkthrough): run → jump-on-Space `86ca9yq3q` → crouch-on-Ctrl `86caa3kur` → THEN the gameplay wave** (settings `86caa4bqp` → inventory `86caa4bya` → chop `86caa4c5c` → stone `86caa4c96`; pre-staged + de-risked, NOT started). **Coast-polish `86ca9ymne` = ABANDONED for now (Sponsor 2026-06-17).** Hit-reaction clips (Head/Rib/Stomach Hit, Getting Up, Stunned) PARKED for a future damage feature. **Mixamo clips** (Running/Jump/Crouching-Idle/Sneak-Walk; Without-Skin/In-Place) live UNTRACKED in `Assets/Art/Character/Castaway/` (main worktree) — the implementer copies them in AFTER its Step-0 `git clean`. Erik's UI-Toolkit research note is UNTRACKED at `team/erik-consult/ui-toolkit-inventory-settings-research.md` (commit it at gameplay-wave start). **PENDING (Sponsor): build `f34a829` (WASD main) in `C:\Trunk\PRIVATE\Far-Horizon`** — queued behind Devon's run on the single Unity slot; do BEFORE any run-PR review build. On the run-on-Shift PR: Drew review (diff-only while CI/Devon hold Unity) → Tess QA → Sponsor feel-soak → merge → then jump. auto-status OFF (do NOT re-arm). ⚠ Persona worktrees self-heal their branch on Step-0 `git checkout -f -B <branch> origin/main`.**
- **FINAL STATE:** `#50` (`drew/big-round-island`) **@ `4aad55a`** = the consolidated FINAL baseline (#55 ff-reconciled in this session). Contains: seed-42 ORGANIC island + grounded float (`modelSoleGround`) + coast (warm wide sand / foam / water-to-sand) + water (DISTINCT teal + MOVING waves, backface-cull fix) + held axe (facing-follow + no-bounce/ratchet + FOLLOWS-the-arm + Sponsor's baked seat `HeldAxeWorldOffsetFromHand=(-0.1502,-0.1602,-0.0528)`, euler `(16,2,-82)`). EditMode 225/225 + PlayMode 88/88. **ALL soak items Sponsor-APPROVED** (island/coast/float/water/axe).
- **RESUME = THE BIG MERGE (Sponsor-authorized; do FIRST next session):** (1) **Tess QA** the final build (`#50 @ 4aad55a`) via the pre-staged plan `team/tess-qa/big-merge-acceptance-plan.md` (**PR #54**); (2) on QA-green → `gh pr merge 50 --admin --squash --delete-branch` (#50 → main = new baseline); (3) CLOSE superseded PRs: #38/#39/#40/#43/#44/#48 (old soak stack) · #45 (spike, never-merge) · #47 (character, in #50) · #51/#52/#53/#55 (axe PRs folded into #50); (4) FLIP tickets → complete: `86ca9qwr3`/`86ca9xyqa`/`86ca9yn57`/`86ca9xz00`/`86ca9ykp0`/`86ca9zcjn`/`86ca8rdkp`/`86ca9a7qn`/`86ca8t9pq`/`86ca8r72j`; (5) **orch-docs PR** — commit the uncommitted-on-main session doc work (`.claude/docs/unity-conventions.md` edits, `.claude/docs/unity6-mastery.md`, `CLAUDE.md`, `team/orchestrator/dispatch-template.md`, `.claude/agents/drew.md`+`devon.md`, `team/STATE.md`, `team/erik-consult/*`); (6) merge the separate docs PRs (#49 Erik-operationalize / #41 DECISIONS / #42 Uma-audit / #46 Uma-direction / #54 QA-plan) — peer-reviewed; (7) THEN dispatch backlog: Erik adopt-tickets `86ca9a340`/`86ca9a36g`/`86ca9a39q`/`86ca9a3b3` + locomotion `86ca9yq2x`(WASD)→`86ca9yq34`(run)→`86ca9yq3q`(jump) + deferred coast-polish `86ca9ymne`.
- **Unity-6.4 MASTERY (Sponsor HIGH-PRIORITY) ✅ DONE + WIRED always-on:** `.claude/docs/unity6-mastery.md` (distilled; auto-loaded at SessionStart + MANDATORY-read for Drew/Devon via CLAUDE.md + dispatch-template + persona files) + `team/erik-consult/unity6-mastery-research.md` (full + gap-fill incl. the Input-System guide for the locomotion backlog).
- **2026-06-17 BIG MERGE ✅ EXECUTED** (Sponsor authorized "Full sequence, auto-merge on green" via AskUserQuestion). Tess GO (agentId `a6c194f25e9bf98c5`: build #50@`4aad55a` stamp-verified, EditMode 225/225 + PlayMode 88/88, gameplay-cam ACs confirmed). **Done:** (1) #50 squash-merged → main `6aada8f` (caught + fixed: #50's base was `drew/world-look-impl`, retargeted to `main` first; main was a clean ancestor so no conflicts); (2) closed superseded #38/#39/#40/#43/#44/#45/#47/#48 + deleted stale remote branches; (3) flipped 11 tickets → complete (the 10 planned + `86ca9qwvd` hand-local axe); (4) orch-docs PR **#56** (unity6-mastery + held-prop/water/shadow conventions + Erik research; unity-conventions.md unioned with #50's additions) merged; (5) merged docs PRs **#41/#42/#46/#54/#49** (#49 rebased + dispatch-template block unioned with #56). **main now `6515ed7`; 0 open PRs.** ⚠ classifier denied the first docs-merge batch (admin-bypass + #54 red CI = env EPERM); Sponsor re-authorized admin-merge → proceeded. **REMAINING (surfaced to Sponsor):** (a) CLAUDE.md core-feel line click-to-move→WASD wording confirm; (b) DECISIONS catch-up entries (organic-island / water-backface-cull / held-prop-FOLLOWS-arm / WASD-pivot / unity6-mastery-integration) — bundle (a)+(b) in one docs PR; (c) next-wave dispatch (locomotion WASD `86ca9yq2x`→run `86ca9yq34`→jump `86ca9yq3q` / Erik adopt-tickets `86ca9a340`/`86ca9a36g`/`86ca9a39q`/`86ca9a3b3` / coast-polish `86ca9ymne`). auto-status OFF.

---

## RESUME NEXT-ACTION — 2026-06-16 (island soak ✅✅ ALL 5 APPROVED; 3 new look items raised)

**If this session dies right now — CURRENT (supersedes everything below):**
- ✅✅ **SPONSOR APPROVED ALL 5 island-soak items on `a3db7c3` (PR #50) 2026-06-16:** walk-everywhere (N1), camera-above-hills (N2), windowed (N3), float-planted-on-island, island look. The 9-attempt float saga + the big-round-island redesign are BOTH validated. THE BIG MERGE is unblocked pending the 3 new look items below (Sponsor-gated — island #50 = new world baseline; do NOT merge without explicit sign-off).
- ❓ **3 new look items the Sponsor raised on the SAME soak (all Drew / world-gen, PR #50 surface) — AWAITING his pick of which to fix:**
  - **(L1) SQUARE around the island** *(genuine artifact)* — terrain is a SQUARE welded grid (`LowPolyZoneGen.IslandGridHalf = 165f`, 330u square); only the *height* is radial, so the submerged seabed mesh is square and its edge reads as a square outline from above (grid ends at 165u; water plane continues to `WaterHalfExtent 700`). Fix: clip terrain mesh to a disc (radial cull) OR sink+extend the grid edge past the fog.
  - **(L2) BEACH down a hill** *(correct topology, tunable)* — radial heightmap: inland plateau (`IslandPlateauH 0.15`) + Perlin hills (`IslandHillAmp 9.0`, ramped down toward coast) slope to the waterline (`WaterY -0.20`) at `IslandShoreR 120`, so the beach sits below the interior. Tune: gentler coastal falloff / lower hill amp near the coast.
  - **(L3) LINE through the island** *(benign — tree-trunk shadow)* — long thin dark streak = a tree-trunk shadow from the warm directional Sun (`WorldBootstrap.cs:185-190` Sun `Euler(48,-35,0)`, soft shadows); it drapes over + bends with the hill = confirms shadow, not a seam. Tune by raising sun pitch (shorter shadows) if wanted.
- **SPONSOR SUPERSEDED L1/L2/L3 with a bigger directive (2026-06-16):** the island should NOT be a disc — make it **organic/irregular** (real-island shape, some edges CLIFFS not beach), the **beach LEVEL with the grass** (no down-a-hill), and **foam on ALL edges**. Trees/shadows (L3) = "fine for now." This subsumes L1 (square) + L2 (beach-dip).
- **`/whip` → 2 agents DISPATCHED in parallel (2026-06-16, background):**
  - **Drew `a797b1d075421e692`** → ticket **`86ca9qwr3`** — ✅ **DONE → PR #50 @ `33b0afc`**. Organic irregular island (warped-coast `ShoreRadiusAt`/`CliffinessAt`, square GONE, foam all edges, beach-level). **AC0 LINE diagnosed = URP shadow-distance boundary** (`m_ShadowDistance 50` < framed grass; read "world-fixed" only because the orbit stays over spawn) → fix `shadowDistance=160 + cascadeBorder=0.35` in `BootstrapProject.ConfigureUrp`. EditMode 213/213 + PlayMode 82/82; RoundIslandTests rewritten to irregular contract (14/14); NavMesh 100%; float/cam/windowed/axe untouched. **4 SEED variants for the Sponsor to pick:** default `20250616` (baked into Boot.unity), `7`, `42`, `1234` — captures `ci-out/organic-default/` + `ci-out/seed-variants/seed{7,42,1234}/` (stamp `dfc9618` = ancestor of `33b0afc`; only Boot.unity bake + URP + NavMesh changed after, so the shapes are representative). Re-bake a non-default via `LowPolyZoneGen.IslandSeed` / `-islandSeed N`. Reviewer Devon (HELD until look-verdict). Doc candidate (maintain-docs): dark line/ring across world = URP shadow-distance boundary, not a seam/tree-shadow → unity-conventions §Editor-vs-runtime.
- **SPONSOR PICKED SEED 42** (2026-06-16, from the variant captures — most "real island": peninsula + bays). **→ Drew DISPATCHED for the reconcile** (agentId `aed72256c2586a9ae`, ticket `86ca9qwr3`, PR #50): `git merge --no-ff origin/devon/axe-hand-local` (#51 axe fix) + `LowPolyZoneGen.IslandSeed = 42` + regen Boot.unity (bakes seed-42 island + hand-local axe defaults + NavMesh) + build + capture `ci-out/combined-soak/` (walk + axe from ≥3 facings + coast/cliff + overhead). ✅ **DONE → PR #50 @ `ec947e6`** (PR #51 axe fix merged CLEAN — no Boot.unity conflict; seed-42 island + hand-local axe both baked; EditMode 213/213 + PlayMode 85/85; 60 FPS, NavMesh 100%). #51 folds into #50 (close as superseded at the big merge). **Orch VERIFIED fresh** (HEAD==origin==PR#50==`ec947e6`, globalgamemanagers 17:09, HUD renders `ec947e6`) + **OPENED the captures MYSELF** (`ci-out/combined-soak/`): seed-42 organic shape (no square / no line, foam all edges, feet planted) + axe SEATED across 0/90/180 facings = hand-local fix PROVEN. ⚠ **CLIFFS (AC2) read SUBTLE in captures** (coast shows mostly beach) — flagged to the Sponsor to check in-game; dial cliff-fraction up if he wants more. **→ SERVED the combined soak** (drew-wt exe `Build/Windows/FarHorizon.exe`, stamp `ec947e6`, windowed) 2026-06-16. **AWAITING Sponsor verdict.** On approve → THE BIG MERGE (island #50 = new baseline; Tess QA combined; close superseded #38-#44/#48 + #51; flip tickets `86ca9qwr3`/`86ca9qwvd`/`86ca9a7qn`/`86ca8rdkp`/`86ca8t9pq`/`86ca8r72j`; Erik adopt-tickets; orch-docs PR; #49/#41/#42).
- **SPONSOR SOAKED `ec947e6` 2026-06-16 → ISLAND ✅ APPROVED ("I love this island, commit this"); line GONE ✅; no square ✅; cliffs accepted-for-now ✅. SEED 42 LOCKED (do NOT re-roll).** 3 polish items remain → 2 PARALLEL dispatches (PR #50 surface):
  - **Drew `af6f11c69e2d92d0a`** → `86ca9xyqa` COAST polish — ✅ **DONE → PR #50 @ `92765c2`** (all 5 coast ACs; 220/220 EditMode + 85/85 PlayMode; captures `ci-out/coast-polish/` show warm wide beach + foam ring all sides + water laps sand). Root cause #5c (ticket framing REFUTED): anchors were never cooled — `IslandColorAt` dragged seaward sand toward cool `SandDamp` on EVERY sector → fix gated that drag to cliff sectors only. ⚠ #6b waterline smooth at default/overhead; faint grid-edge silhouette ONLY at extreme grazing angle (clip-edge skirt = scoped follow-up if Sponsor flags). ⚠ **Resident capture exes remain on the runner → SERIALIZE (ensure Unity/FarHorizon exes exited) before the reconcile serve_soak build** (PackageCache EPERM). Reviewer Devon (HELD until soak).
  - **Devon `a8a29643c703195b7`** → `86ca9xz00` AXE facing fix — ✅ **DONE → PR #52 @ `959fc96`** (base `drew/big-round-island`). Fix: `stabilizeFrame` → the facing-carrying `_model` (new `CastawayCharacter.ModelTransform`). AC3 test `HeldAxeFacingFramePlayModeTests` RED vs root (hand-local seat drifts 0.0958u @45°, tol 0.01) → GREEN vs `_model`; EditMode 214/214 + PlayMode 88/88; AC5 dynamic-facing capture added. #52 folds into #50 at the reconcile. **ROOT CAUSE orch-confirmed:** `HeldAxeRig.stabilizeFrame`=CastawayCharacter ROOT, but facing lives on the `_model` CHILD (`_model.localRotation = Euler(0,_bodyYaw,0)`, CastawayCharacter.cs:744) → the slow grip-anchor (0.12/s ~8s) EATS the facing yaw → axe lags facing → "seated only one direction." FIX: `stabilizeFrame` → `_model`. (My axe_facing captures FALSE-GREENED — static per-facing snapshots; AC3 must drive `_model` facing over multiple frames.) Reviewer Drew (HELD until soak).
  - **Merge stance:** SEED 42 LOCKED; **polish-first then merge** (offered Sponsor the merge-now alternative; he didn't take it). On BOTH done → orch reconciles (merge axe into #50 + regen Boot.unity + build) → verify-fresh + OPEN captures incl. a **DYNAMIC axe-facing** check (NOT static) → serve. auto-status OFF.
  - **Doc captured this session:** unity-conventions §Editor-vs-runtime — URP shadow-distance-boundary-reads-as-a-line false-symptom (Drew PR #50 AC0).
- **BOTH POLISH DONE (coast #50 @ `92765c2` + axe-facing #52 @ `959fc96`) → RECONCILE dispatched to Drew** (continue `drew/big-round-island`): `git merge --no-ff origin/devon/axe-facing-fix` (#52) + regen Boot.unity (seed 42 LOCKED + `_model`-frame axe + coast) + build + capture `ci-out/combined-soak2/` incl. a **DYNAMIC axe-facing** check (NOT static — the lesson from this round's false-green) + push #50. ⚠ Serialize resident capture exes before bootstrap (PackageCache EPERM; the kill may be classifier-denied → retry/surface). #52 folds into #50 (close as superseded at big merge, like #51). On done → orch verify-fresh + OPEN captures (esp. the dynamic axe-facing) + SERVE the next combined soak. **[RETRY: first reconcile agent `a6d5d786ab30d62ec` DIED on a transient API 500 after 7 calls (before the merge) — drew-wt verified CLEAN at `92765c2`, no partial merge, nothing pushed; RE-DISPATCHED Drew `ac8b8327ec834ef50` 2026-06-16.]** ✅ **RECONCILE DONE → PR #50 @ `a0eb595`** (EditMode 221/221 + PlayMode 88/88; stamp==HEAD; Drew terminated resident capture exes pre-push → no EPERM). **Orch verified fresh + OPENED captures (`ci-out/combined-soak2/`):** axe SEATED across the DYNAMIC mid-turn facing sweep (060/180/300) ✓; warm wide golden sand + foam ring all edges + water-laps-sand ✓; waterline smoother (residual faint stepping ONLY at extreme grazing angle — #6b clip-edge-skirt follow-up available if Sponsor flags). **→ SERVED the combined soak** (drew-wt exe `Build/Windows/FarHorizon.exe`, stamp `a0eb595`, windowed) 2026-06-16. **AWAITING Sponsor verdict.** On approve → THE BIG MERGE (#51 + #52 fold into #50; Tess QA combined; close superseded #38-#44/#48; flip tickets `86ca9xyqa`/`86ca9xz00`/`86ca9qwr3`/`86ca9qwvd`/+; Erik adopt-tickets; orch-docs PR; #49/#41/#42). **Axe seat:** F9-dial HELD until the axe is steady (it bounces — see below).
- **SPONSOR RE-SOAKED `a0eb595` (2026-06-16 cont'd) — 2 items still open → 2 PARALLEL dispatches:**
  - **AXE:** facing ✅ tracks now, but it BOUNCES + RATCHETS UP per walk step ("positioned higher and higher after each walk"). → **Devon `a48433ee356521b6f`** → `86ca9ykp0` (branch `devon/axe-bounce-fix` off `a0eb595`, PR base #50). **/unstick → INSTRUMENT-FIRST** (attempt 3+ on the axe): build `-axeWalkTrace` (per-frame axe-Y decomposition) → pin the ratchet (hypothesis: grip-anchor easing integrates the `modelSoleGround` Y-oscillation via `_model` InverseTransformPoint→TransformPoint feedback) → decouple the axe vertical from the `_model.y`/anchor loop (keep facing + arm-swing-damp + float UNTOUCHED). AC4: walk-Y returns-to-baseline test (NO cumulative drift over ≥5 steps).
  - **COAST: SPONSOR APPROVED soak items 2/3/4 (beach/waterline/island) as good-enough** (2026-06-16: "soak 2,3,4 approved, you can move on, we can polish later"). Coast round-1 (warm wide sand + water-to-sand + foam-back + smoother waterline) is IN #50 @ `a0eb595`; `86ca9xyqa` → ready for qa test. **Coast round-2 (Drew `a2e69903ed4f09ddd`) KILLED (TaskStop).** Soft-wave-waterline + PULSING-foam + cliffs DEFERRED → backlog **`86ca9ymne`** (post-merge polish).
  - **TWO BLOCKERS before THE BIG MERGE (2 PARALLEL agents in flight):**
    - **(1) AXE bounce/ratchet** — Devon `a48433ee356521b6f`, `86ca9ykp0` — ✅ **DONE → PR #53** (base `drew/big-round-island`). **/unstick instrument WORKED:** `-axeWalkTrace` pinned the ratchet = the grip anchor eased toward the hand pose IN the bobbing `_model` frame, and the walk hip-lift (~+0.66u) made `anchorLocalY` integrate UP monotonically (`0.290→0.459`, never eased back) → axe climbed each step. FIX: reconstruct `followPos.Y` from a STABLE grounded ref (bob removed + fixed grip-height) → CUMULATIVE_DRIFT `-0.003u` bounded. AC4 guard `HeldAxeWalkBouncePlayModeTests` RED(5/5 monotoneUp)→GREEN(0/5); EditMode 221/221 + PlayMode 91/91; dynamic-facing stays green. Instrument `-axeWalkTrace` (`AxeWalkTrace.cs`) persisted. facing(86ca9xz00)+arm-swing(86ca8rdkp)+modelSoleGround UNTOUCHED. #53 folds into #50 at reconcile. Doc candidate (maintain-docs): held-prop vertical must decouple from a bobbing frame's local-Y; verify the OUTPUT is decoupled, not just that the anchor stops drifting.
    - **(2) WATER reads SAME as sky + static** — Drew `a67c8d70c322abd2f`, `86ca9yn57` — ✅ **DONE → PR #50 @ `d944f6c`**. Diagnose OVERTURNED the fog hypothesis: real cause = the sea was **BACKFACE-CULLED** (URP `Cull Back` culls by triangle WINDING, not the normal; the +Y-normal guard was a proxy a culled mesh satisfies → the gameplay cam saw SKY through the invisible sea → water==sky). FIX: reverse tri winding (top=front face) + water-only `_FogCap=0.5` (mountain seam-kill BIT-IDENTICAL). WAVES: `_WaveAmp 0.10→0.45` (0.10 was imperceptible over the ~1400u sea). EditMode 225/225 + PlayMode 89/89. New instruments `-verifyCoast/-seaDiag/-coastWaves` (`SeaVerifyCapture.cs`). Sea sits ~120u out behind jungle — only a coast vantage frames it. Doc candidate (maintain-docs): winding-not-normal cull guard (perceptual-vs-proxy / cull family).
  - **/WHIP (2026-06-16, while reconcile-3 runs — filled 2 idle slots, non-conflicting):** **Erik `ad270624cae2b2976`** → Unity-6 research GAP-FILL — ✅ **DONE (5/5 gaps, Strong official docs):** GC culprits + no-alloc alternatives, vSync/targetFrameRate scaling, Forward-vs-Forward+-vs-Deferred matrix (why Forward+ for FH), Addressables, **Input System end-to-end WASD/Jump/Sprint guide → directly feeds locomotion `86ca9yq2x`/`86ca9yq34`/`86ca9yq3q`**. Added §11 Input System + §12 FrameRate/GC to `.claude/docs/unity6-mastery.md`; GF-1..GF-5 to the full note. Still open: URP e-book body + NavMesh (unity.com 403 — need a direct/authenticated read). `86ca9yu43` → ready for qa test. **Tess** → big-merge QA acceptance-plan pre-stage — ✅ **DONE → PR #54** (`tess/big-merge-acceptance-plan` → main, `test(qa)` doc, 12 sections / ~38 items, reviewer Drew — review after reconcile; PLAN only). Finding: the new verify-flags/surfaces (`-coastWaves`/`modelSoleGround`/seed-42 etc.) are NOT on main yet (ship from #50) → the plan separates confirmed-on-main infra from #50-sourced surfaces + greps the COMBINED HEAD at QA time (never assume a flag exists; gameplay-cam visual if none). `86ca9z1a6` → ready for qa test. Devon/Uma/Priya justified-idle (WASD + Erik-adopt-tickets are Sponsor-sequenced post-merge + WASD conflicts with the in-flight CastawayCharacter axe reconcile; Uma direction given directly; Priya DECISIONS #41 open + orch-docs fold at merge).
  - **BOTH BLOCKERS DONE → RECONCILE dispatched to Drew `a259df104fb119d98`** (merge #53 axe-bounce into #50 @ `d944f6c` + regen Boot.unity + build + capture `ci-out/combined-soak3/`: dynamic axe-WALK Y-steady + axe-facing + water-vs-sky + wave-motion + coast). ✅ **RECONCILE DONE → PR #50 @ `0fa5bbb`** (HUD stamp `6bcc1bc`; `0fa5bbb` only re-stamped BuildStamp.txt — content-identical; EditMode 225/225 + PlayMode 92/92). **Orch VERIFIED + OPENED captures (`ci-out/combined-soak3/`):** axe-walk Y-steady via `-axeWalkTrace` (6-step band ~9mm, `CUMULATIVE_DRIFT −0.0035 BOUNDED — no ratchet`) ✓; axe seated across facings ✓; water now DISTINCT TEAL vs pale sky (backface-cull fix) ✓; waves moving (waterline shifts + trace deltas; subtle at far/overhead vantage — best at a coast vantage) ✓; float feet-planted ✓. ⚠ trace log had a NavMesh-warning in the `-axeWalkTrace` HARNESS context (scripted walk) — the shipped build has the baked NavMesh (PlayMode 92/92 incl. coverage); Sponsor's walk confirms click-to-move. **→ SERVED the combined soak** (HUD `6bcc1bc`) → **SPONSOR SOAKED 2026-06-16:** ✅ **WATER APPROVED** ("looks really good against the sky… waves… foaming against the sand again. excellent") + axe FACING-FOLLOW works in-game ✅ + axe placement nudged ✅ (F9 values seen across 2 shots: forest `(-0.0925,-0.1253,-0.0103)` / coast `(0.0580,-0.1431,0.0230)`, euler `(16,2,-82)` — FINAL value TBC after the behavior decision). **✅ DECIDED: FOLLOW the arm (natural swing)** (Sponsor 2026-06-16). → **Devon `a6f8821649d182284` → `86ca9zcjn`** (branch `devon/axe-follow-arm` off `0fa5bbb`, PR base #50): axe rides the RAW hand (natural swing+bob) — REMOVE the stabilizer/anchor + the bounce-fix vertical-decouple, KEEP facing-fix + float, light-damp for smoothness, NO cumulative ratchet. Choice CARRIES to run/jump (`86ca9yq34`/`86ca9yq3q`). ✅ **DONE → PR #55 @ `6fffde2`** (axe rides the RAW hand; CUMULATIVE_DRIFT −0.00249 bounded; facing sweep maxSeatDrift 0; seat baked from the coast dial `(0.0580,-0.1431,0.0230)`; EditMode 225/225 + PlayMode 88/88; new `-verifyAxeWalk` instrument). #55 = #50 (`0fa5bbb`) + axe-follow (clean superset; #50 hasn't moved). **Orch verified:** no-ratchet trace ✓, axe in-hand during walk + feet grounded ✓ (captures top-down → the SWING feel is the Sponsor's live call), facing-locked ✓. **→ SERVED the FINAL axe soak from devon-wt** (exe `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe`, stamp `6fffde2`, windowed) 2026-06-16. ✅✅ **SPONSOR APPROVED axe-follow ("it works perfectly") + dialed the FINAL seat `offsetFromHand=(-0.1502,-0.1602,-0.0528)` euler `(16,2,-82)` 2026-06-16 (source: his F9 screenshot).** **ALL SOAK ITEMS NOW APPROVED** (island / coast / float / water / axe facing + bounce + follow + seat). → **Devon `aff1b91b83c63420c` baking the final seat** (`86ca9zcjn`, on `devon/axe-follow-arm`: set `HeldAxeWorldOffsetFromHand=(-0.1502f,-0.1602f,-0.0528f)` + rebuild Boot.unity; no re-soak — he approved that live-dialed state). **Sponsor "3: approved" = FULL soak checklist signed off (swing + seat + facing + everything else).** On bake-done → reconcile #55→#50 (ff; #50 hasn't moved past `0fa5bbb`) → **Tess QA (plan #54) on the final combined build** → confirm Sponsor merge-go → **THE BIG MERGE** (Sponsor-gated `--admin --squash`: island #50 = new `main` baseline; close superseded #38-#44/#48/#51/#52/#53; #55 folds; flip ALL tickets complete; Erik adopt-tickets `86ca9a340`/`86ca9a36g`/`86ca9a39q`/`86ca9a3b3`; orch-docs PR incl. unity6-mastery + all session doc edits; #49/#41/#42). Decision draft (Priya/DECISIONS): held-prop FOLLOWS-the-arm (reverses 86ca8rdkp stabilizer + 86ca9ykp0 vertical-decouple). Decision draft (Priya/DECISIONS): held-prop locomotion policy REVERSED — follows the arm (supersedes the 86ca8rdkp stabilizer + 86ca9ykp0 vertical-decouple). (island #50 = new baseline; coast polish `86ca9ymne` + cliffs = post-merge backlog; Tess QA combined; close superseded #38-#44/#48/#51/#52/#53; flip tickets; Erik adopt-tickets `86ca9a340`/`86ca9a36g`/`86ca9a39q`/`86ca9a3b3`; orch-docs PR incl. the unity6-mastery integration + all session doc edits; #49/#41/#42). F9 axe-dial after the Sponsor confirms it's steady. auto-status OFF.
- **LOCOMOTION BACKLOG (Sponsor 2026-06-16, sequenced WASD→run→jump; NOT dispatched — backlog AFTER the current world/character merge; team busy on axe+water):**
  - `86ca9yq2x` feat(input): **WASD movement** (replace click-to-move) — ⚠ SUPERSEDES the "Sponsor-locked PoE-style click-to-move core feel" (CLAUDE.md Context) — record in DECISIONS + update the CLAUDE.md core-feel line (flagged to Sponsor; orch did NOT edit the contract unprompted). Assignee rec Devon.
  - `86ca9yq34` feat(locomotion): **run on Shift** (source a Mixamo RUN clip) — BLOCKED-BY `86ca9yq2x`. Assignee rec Devon.
  - `86ca9yq3q` feat(locomotion): **jump on Space** (source a Mixamo JUMP clip) — BLOCKED-BY `86ca9yq2x` (+run). ⚠ touches the float system (must SUSPEND `modelSoleGround` ground-snap airborne, grounded behavior unchanged). Assignee rec Devon.
  - All 3 carry the Mixamo MANGLED-FINGER note (open-hand clips → the HasAxe-gated `CastawayFingerCurl` driver must cover the new clips). Dispatch in sequence (WASD → run → jump) after the merge.
- **UNITY 6.4 MASTERY RESEARCH (Sponsor HIGH-PRIORITY 2026-06-16; "cannot stress enough how important" — must be known in EVERY Drew/Devon action) — ticket `86ca9yu43`; Workflow `wf_700bbf75-6bb` IN FLIGHT:** 8 parallel deep-readers (one per Sponsor source) → Erik consolidates → `team/erik-consult/unity6-mastery-research.md` (full) + `.claude/docs/unity6-mastery.md` (distilled, always-on). ✅ **RESEARCH DONE + INTEGRATION WIRED.** Both docs written: `.claude/docs/unity6-mastery.md` (189L distilled always-on) + `team/erik-consult/unity6-mastery-research.md` (660L full cited) + 8 raw extracts in `team/erik-consult/unity6-raw/`. Readers honestly flagged unity.com 403-blocks (WebFetch UA-blocked) → fell back to `docs.unity3d.com` primary pages + WebSearch excerpts + the LOCAL UI-Toolkit Unity-6 PDF (richest, 147pp/52 topics); gated e-books NOT scraped (flagged, not fabricated). **PHASE 3 ALWAYS-ON INTEGRATION ✅ WIRED:** SessionStart hook globs `.claude/docs/*.md` (verified line 28 — auto-loads for orch); CLAUDE.md auto-load note + Detailed-Docs bullet updated (unity6-mastery MANDATORY); `.claude/agents/drew.md` + `devon.md` line-10 doc-read FIXED (replaced STALE Godot doc names [combat-architecture/html5-export/etc — don't exist here] with the real FH docs + named `unity6-mastery.md` MANDATORY before any Unity code); `dispatch-template.md` gained a "Mandatory docs pre-read" block. `86ca9yu43` → ready for qa test. All these orch-docs are UNCOMMITTED on main → fold into the orch-docs PR at the big merge. ⚠ FOLLOW-UPS: (1) the distilled doc still references click-to-move (Command pattern / NavMesh) — minor stale vs the WASD pivot (`86ca9yq2x`); (2) the gated Unity-6 e-books (perf, URP-advanced, version-control) are 403-blocked — if their depth is wanted, need an authenticated fetch or a fetchable mirror (noted in the doc's "Unresolved" section).
  - **Devon `af2a00d2ce819b801`** → ticket **`86ca9qwvd`** (held-axe hand-LOCAL fix) — ✅ **DONE → PR #51 @ `bc48ef3`** (base `drew/big-round-island`). Fix: `HeldAxeRig.cs:176` position now hand-local (`followPos + followRot * worldOffsetFromHand`); Sponsor's spawn-facing look preserved via WORLD→hand-local conversion at bake (real hand rotation, not guessed). AC3 PlayMode test `HeldAxeStaysSeatedAcrossFacingsPlayModeTests.cs` RED→GREEN proven; EditMode 209/209 + PlayMode 85/85. Reviewer Drew (HELD until soak). Doc updates: none (unity-conventions §FBX already covers the lossy-bone world-vs-local class).
- **ON BOTH DONE:** orch RECONCILES Devon's axe fix onto Drew's reshaped #50 (regenerate Boot.unity; no file overlap → mechanical) → build + capture → SERVE the combined soak WITH the seed variants for the Sponsor to pick the island shape (verify-fresh + OPEN the gameplay-cam capture per [[verify-grounding-soaks-by-gameplay-cam-visual]] before serving). On approve → THE BIG MERGE (island #50 = new baseline; Tess QA combined; close superseded #38-#44/#48; flip `86ca9a7qn`/`86ca8rdkp`/`86ca8t9pq`/`86ca8r72j` + the 2 new tickets; Erik adopt-tickets `86ca9a340`/`86ca9a36g`/`86ca9a39q`/`86ca9a3b3`; orch-docs PR; #49/#41/#42). auto-status OFF (do NOT re-arm unless Sponsor says `/auto-status away`).

---

## RESUME NEXT-ACTION — 2026-06-15 EVENING (float attempt-8 + BIG-ISLAND redesign both IN FLIGHT)

**If this session dies right now — CURRENT (supersedes the historical log below):**
- **Devon ✅ FLOAT SOLVED (attempt 8) → PR #47 @ `e1289ef`, CI GREEN** (149 EditMode + 70 PlayMode). **ROOT CAUSE (definitive):** the FBX bakes a **100× cm→m scale on the SkinnedMeshRenderer's OWN transform** (`model` node localScale (100,100,100); verts authored in metres ~0.01u); the float-measurement DOUBLE-APPLIED it via `localToWorldMatrix` → world-Y blew up → the −68 runaway. FIX: ground the avatar ROOT to `terrainHit + K` (fixed K, NO per-frame BakeMesh, no 100× exposure); unit-scale `TRS(pos,rot,Vector3.one)` for any sole readout. **ORCH READ the walk `[FloatTrace]` MYSELF (kept the commitment):** `proxyRootGap` (root−ground) ≤2.4mm the WHOLE walk (spawn `0.0199` → foreshore `−0.327`, bounded — NO −68); shore capture LOOKED grounded — BUT ❌ **NOT ACTUALLY FIXED:** the Sponsor's gameplay soak of `e1289ef` STILL shows the character floating ~0.5-1u (shadow on the ground below, standing + mid-stride). **root-grounded ≠ MESH-grounded** — the visible mesh hangs off the 100×-scaled `model` node OFFSET from the grounded root; the `+0.64` mid-stride GAP WAS REAL float (orch wrongly dismissed it as diagnostic noise + trusted proxyRootGap + a misleading capture → served a broken build). **VERIFICATION LESSON: under the 100× scale, proxyRootGap / sole / bounds / BakeMesh ALL lie, and a non-player-framing capture can mislead — ONLY a gameplay-DEFAULT-cam visual (feet-vs-sand) is truth.** **→ Devon ATTEMPT-9 ✅ DONE → PR #47 @ `9c5cacd`, CI GREEN.** **REAL ROOT CAUSE (found via scale-immune `ClipBaselineDiagnose`, overturned the scale-normalize framing): the Mixamo WALK clip authors the HIPS ~0.66u HIGHER than IDLE** (Idle sole root-rel −0.003, Walk +0.63..+0.69) → the rendered body floats ONLY while walking even with the root grounded; NOT scale/snap/shadow/bounds, and unfixable via clip import flags. FIX: ground the SCALE-IMMUNE rendered sole via a model-child local-Y offset (`CastawayCharacter.modelSoleGround`, rate 90); regression guard in EDITMODE via `AnimationClip.SampleAnimation` (a PlayMode anim-grounding test is a FALSE-GREEN — headless Animator never ticks). **ORCH OPENED the 6 gameplay-cam captures MYSELF (kept the commitment):** spawn/foreshore MID-STRIDE + standing all show FEET PLANTED with a TIGHT shadow at the feet (NOT the separated-shadow float of `e1289ef`), ~45° framing; scale-immune GAP(sole-ground) ≤0.4mm everywhere. ✅ **Strongest evidence yet — float fixed.** ⚠ Captures are ~45°, not the Sponsor's exact lower over-shoulder cam — his soak on return is the FINAL word (but a 0.66u float would show at this angle and is GONE). Self-Test Report (classifier-blocked from `gh pr comment`) saved at `ci-out/selftest_walkfloat.md`. Ticket `86ca8rdkp` → ready-for-qa-test. **READY TO SOAK on Sponsor's return** (float-confirm build from #47 `9c5cacd`; verify exe stamp + serve fresh). Doc-candidate (maintain-docs): Walk-clip hip-lift root cause + PlayMode-anim-test-is-false-green → unity-conventions §FBX. **⚠ FOLLOW-UP (Devon, post-soak): the DIAGNOSTIC sole/foot-bone WORLD-Y readout (the F8 GAP) is STILL scale-noisy DURING ANIMATION** — `GAP(sole-ground)` swings to +0.64 mid-stride while the ROOT stays grounded (corrupted readout, NOT real float; geometrically impossible as real since the grounded root forces the body to ground). Make the gauge readout scale-immune during animation so F8 is trustworthy. Ticket `86ca8rdkp` → ready-for-qa-test. **→ orch SERVING the float-confirm soak from #47 `e1289ef`** (character-focused; build verified fresh — globalgamemanagers 20:03 UTC, stamp==HEAD==origin `e1289ef`; judge GROUNDING; the gauge mid-walk number is disclosed-noisy). **Decision draft (Priya/DECISIONS):** ground imported-rig avatars by the ROOT to `terrainHit + K`, NEVER per-frame BakeMesh world-Y — Mixamo/Hyper3D FBX bake a 100× cm→m scale on the SMR transform that `localToWorldMatrix` double-applies.
- **Drew ✅ BIG ROUND ISLAND FEASIBLE → PR #50** (`drew/big-round-island`, base `drew/world-look-impl`; head `36becad`, captures at `08d368c` world-identical). **NO SHOWSTOPPER — all 7 reqs met, 60 FPS** (vsync-capped; 320 trees + sea + 5 separate mountain-islands in frustum). 201/201 EditMode + 78/78 PlayMode. **ORCH VISUALLY VERIFIED the captures myself:** `island_overhead.png` = round green densely-forested island + continuous beach/foam waterline ring + water-on-all-sides (far water reads tan-washed in the overhead = fog/grade, a flagged F10 look item); `island_gameplay.png` = character on island grass + trees + stump. Radial `HeightAtRadial` heightmap (hills), `BuildIslandWater` (water-all-sides), 5 mountain-islands @ 114-217u clearance off the 120u coast, dense tall jungle. Ticket `86ca9a7qn` → ready-for-qa-test. **Follow-up (not blocking):** far-sea-tan in extreme overhead (F10 fog/water tune at the look-verdict). **Decision draft (Priya):** BIG ROUND ISLAND = the new world basis (supersedes the beach-to-meadow strip; strip-era water/vista tests rewritten to the radial contract). **READY TO SOAK on Sponsor's return** (his showstopper Q → answer: FEASIBLE). Later: integrate the float-fixed character (#47) into this island.
- ✅✅ **SPONSOR CONFIRMED FLOAT SOLVED 2026-06-16: "float problem solved he now walks perfectly on the ground"** (after 9 attempts; real cause = Mixamo Walk clip authors hips ~0.66u above Idle, fixed by grounding the rendered sole; #47 `9c5cacd`). The dominant session blocker is DONE. **→ INTEGRATION ✅ DONE → PR #50 @ `cd8570c`** (clean, 0 markers; EditMode **205/205** + PlayMode **77/77**; serve_soak verified stamp==HEAD). Grounded float-fixed character + big round island in ONE build. Walk-trace GAP(sole-ground) ≤0.0007u on the radial island terrain (Y 0.29→0.42 = genuine island); island intact (320 trees / 15 peaks / 5 islands, 60 FPS). **ORCH OPENED the captures MYSELF:** 2_midbeach + 3_foreshore MID-STRIDE + island_gameplay all show feet PLANTED w/ a TIGHT shadow (grounded) on the island. **→ orch SERVED the Sponsor the COMBINED soak** (drew-wt exe `c:/Trunk/PRIVATE/Far-Horizon-drew-wt/Build/Windows/FarHorizon.exe`, stamp `cd8570c` — verified HEAD==origin==`cd8570c`, fresh) 2026-06-16 — grounded castaway walking the big round island. **AWAITING Sponsor verdict. On APPROVE → THE BIG MERGE** (island #50 = new baseline; Tess QA the combined [+ post `ci-out/selftest_walkfloat.md` to #47]; held-axe dial; close superseded #38-#44/#48; Erik adopt-tickets) — all Sponsor-gated. Far-sea-tan in extreme overhead = F10 fog tune if flagged. Tess QA HELD until the Sponsor's look-verdict. **→ SPONSOR SOAKED `cd8570c` 2026-06-16 → ✅✅ FLOAT ON ISLAND CONFIRMED ("feet stays planted") — the 9-attempt float saga is CLOSED (do NOT touch `CastawayCharacter.modelSoleGround`).** ❌ 3 island fixes (all Drew, #50): **(N1) CAN'T WALK EVERYWHERE** — click-to-move covers only part of the island; bake/extend the NavMesh over the WHOLE walkable radial-heightmap island + hills (slope/step settings; steep peaks may be excluded). **(N2) DISAPPEARS UNDER A HILL** — orbit camera clips under/through hill terrain on the elevation; add camera-terrain raycast-clamp in `MovementCameraScene` (cam stays ABOVE ground) + hill-occlusion handling so the player stays visible. **(N3) WINDOWED MODE** — Sponsor wants the game in WINDOWED mode (not the current borderless-fullscreen): Player Settings default + runtime `Screen.SetResolution(..., FullScreenMode.Windowed)`. **→ Drew ✅ DONE → PR #50 @ `a3db7c3`** (209 EditMode + 82 PlayMode; float grounding `modelSoleGround` UNTOUCHED = empty diff, no regression): **N1** whole-island NavMesh 100% coverage (192/192; steep-hills framing REFUTED — real cause was a competing flat-slab bake; fix = single authoritative WorldBootstrap whole-island bake + slab surface disabled at runtime); **N2** camera hill-collision (`OrbitCamera.ResolveCameraCollision` — occlusion pull-in + above-ground clamp; on-hill camAboveGround 12.6, character visible); **N3** windowed launch (`FullscreenBoot`→Windowed 1600×900, ProjectSettings fullscreenMode 3). Ticket `86ca9a7qn` → ready-for-qa-test. **READY TO SOAK on resume** (drew-wt exe `Build/Windows/FarHorizon.exe`, stamp `a3db7c3`, launches WINDOWED — ⚠ orch must verify-fresh [globalgamemanagers + stamp==HEAD] + OPEN the walking-on-hills capture before serving, per the float-saga lesson: trust only the gameplay-cam visual). **THEN:** Sponsor soaks the big island (nav/cam/windowed + grounded char) → on approve → THE BIG MERGE (island #50 = new baseline; #48 strip-world-look dropped/superseded; Tess QA the combined + post `ci-out/selftest_walkfloat.md` to #47; held-axe dial; close superseded #38-#44; Erik adopt-tickets `86ca9a340`/`86ca9a36g`/`86ca9a39q`/`86ca9a3b3` + operationalize PR #49 + DECISIONS #41 + Uma audit #42). **SESSION DRAINED + SAVED 2026-06-16 — cron killed, auto-status OFF; 0 agents in flight; all PRs Sponsor-gated (expected queue).** Orch OPENS a WALKING-ON-ISLAND gameplay-cam capture (mid-stride) to confirm the float survives on the radial-heightmap terrain BEFORE serving the combined soak. **Then:** merge strategy (island #50 = new baseline; #48 strip-world-look standalone merge dropped/superseded — sky/water/mountains carry via shaders); Tess QA the combined (post Self-Test Report `ci-out/selftest_walkfloat.md` to #47); held-axe dial (Sponsor's, post-soak); big merge. #48 (`fbe5d15`) NOT served/merged.
- **Backlog:** Erik patterns filed post-merge (`86ca9a340`/`86ca9a36g`/`86ca9a39q`/`86ca9a3b3`); #41 DECISIONS @ `97a1fbf`; close superseded #38/#39/#40/#43/#44 AT the big merge.
- **Erik mastery consult ✅ DONE** (ticket `86ca9a93g` → complete) → note `team/erik-consult/unity-3d-mastery-path.md` (uncommitted → orch-docs). TOP-3 (by next-pain): (1) **character rig/grounding → Animation Rigging Two-Bone foot-IK** (the elite grounding path — directly the float-saga domain; a candidate beyond the current 68u-fix); (2) **procedural world-gen → Sebastian Lague chunk-LOD** (maps to the big island + expansion); (3) **URP flat-shading ddx/ddy + keijiro skybox** (closes 2 recurring shader bug classes). OPEN-Qs: M-U3 island-scale upskill wants ~1-2wk lead (Priya sequence a learning sprint); is `com.unity.animation.rigging` in the manifest? (Devon confirm before the next character PR). **Note:** foot-IK is a FUTURE grounding upgrade — Devon's attempt-8 (fix the 68u at source + terrain-raycast) is the correct IMMEDIATE fix; don't derail it. **→ OPERATIONALIZED (Sponsor decided 2026-06-15): Priya ✅ DONE → PR #49 `de33445`** — Erik's actionable techniques folded into the MANDATORY-READ surfaces: NEW `.claude/docs/elite-techniques.md` (rig-grounding / chunk-LOD / URP flat-shading) + `TESTING_BAR.md` gates (Diagnose-Before-Fix, locomotion-sampling, SRP-batcher) + `dispatch-template.md` pointer — so every future Devon/Drew dispatch picks them up (no longer depends on the un-auto-read consult notes). Left unity-conventions.md/STATE.md untouched (in-flight orch edits) — cross-ref'd. Reviewer Devon; open-Q (confirm `com.unity.animation.rigging` in manifest). Sponsor-gated.

---

### HISTORICAL LOG (2026-06-15 day — float saga + world-look; superseded by CURRENT above):

**Earlier:** Devon + Drew + Erik ALL ✅ DONE (see roster below). **RECONCILE ✅ DONE (`de97ba4`) → KEY-SPLIT ✅ DONE → PR #48 @ `fa9f1b1`** (F9=character dials [arm/axe/GROUND-Y] / F10=world-look dials [sky/fog/clouds/mountains] + mutual-exclusion so only one panel ever active; EditMode **187/187** + PlayMode **68/68**; serve_soak verified, capture-gate 4/4). Orch-verified the SHIPPED build is FRESH: `FarHorizon_Data/globalgamemanagers` rebuilt 2026-06-15 13:32 UTC + stamp==HEAD==`fa9f1b1` (the exe-stub mtime reads Jun-12 = Unity player-stub reuse, NOT staleness — the DATA is fresh). **→ orch SERVED the Sponsor the COMBINED soak** (drew-wt exe `c:/Trunk/PRIVATE/Far-Horizon-drew-wt/Build/Windows/FarHorizon.exe`, stamp `fa9f1b1`) 2026-06-15 — addresses all 5 `b54482c` issues (C5 walk-grounding + F9 GROUND-Y dial, W1 shoreline-out, W2 mountains chunkier/snow + F10 warmth dial, W3 clouds, W4 sky). **→ SPONSOR SOAKED `fa9f1b1` 2026-06-15 → APPROVE-IN-PART (progress!):** ✅ **C5 walk-grounding ACCEPTED** (no elevation complaint — the 4th attempt landed) + **W1 shoreline POSITION fixed** (tree/campfire/debris on dry sand). ❌ 3 world-look issues (all Drew, #48): **(S1) SHORELINE FOAM REGRESSION** — moving the waterline out LOST the soft foam/water-washing-up-on-shore he LIKED in `b54482c` ("I really like the shoreline") → now a static hard line; restore the foam AT the new moved-out waterline (objects stay on dry sand). **(S2) SKY greyish-blue + NO clear clouds** — warm/brighten the sky toward the board's cheerful palette + make clouds READ (bigger/lower/more/contrast). **(S3) MOUNTAINS need MORE DETAIL** — smooth tan pyramids → add geometric richness (faceting/ridges/sub-relief/rock+snow banding; GEOMETRY not just color — F10 color-dial can't add detail). **→ Drew (#48) ✅ DONE → PR #48 @ `3dad12a`** (S1 foam / S2 sky+clouds / S3 mountain-detail; 189 EditMode + 68 PlayMode; serve_soak verified). Diagnose-via-trace OVERTURNED his own 2 framings: S1 foam wasn't "kept, only shifted" — the STEEP ramp + coarse linear water grid stranded foam over deep water (wash 68%→10%) → two-phase shore + near-dense water grid + tightened foam + swell bump; S2 sky wasn't "saturate the zenith" — the over-shoulder orbit frames the MID/horizon band → cheerful mid-blue + lowered `_MidPoint`; S3 = multi-ring ridged `FacetedMountain` + more sides. Ticket `86ca8t9pq` → ready-for-qa-test. **Decision draft (Priya/DECISIONS):** geometry/threshold-bound subjective tuning (foam-over-wet-shelf wash, sky visible-band saturation) should be trace-swept vs a headless geom model + scene-verify BEFORE soak — twice (W1→S1, W4→S2) the intuitive fix-shape was wrong, only trace caught it. Then re-serve combined soak. **AWAITING this Drew round.**

**⚠ SPONSOR ESCALATION 2026-06-15 — "we have been chasing this floating issue for many iterations, you have to add logging or nudging":** the walk-FLOAT is NOT actually accepted (despite Devon's smoothing-lag fix + the F9 GROUND-Y nudge already in `fa9f1b1`) — the Sponsor wants it MEASURABLE, not guessed (the unstick/instrument rule + [[sponsor-prefers-direct-tweak-tools-for-fiddly-placement]]). Told the Sponsor he can dial F9→GROUND-Y in `fa9f1b1` NOW + report the value. **→ Devon (#47) ✅ DONE → PR #47 @ `b6cd801`** — LIVE FLOAT-DIAGNOSTIC shipped: **F8 overlay** (feet-Y / ground-Y / **GAP** RED>1cm / GREEN planted / moving / snap-rate / groundYOffset) + live GAP in the **F9→GROUND-Y** panel (dial + measure together) + ~1Hz `[FloatTrace]` player-log (orchestrator-readable; `-floatTrace` log-only). 149 EditMode + 67 PlayMode; serve_soak verified. **⚑ GROUND TRUTH: at offset=0 the feet already track the dipping foreshore within ≤2.6mm the WHOLE walk (gauge reads GREEN/planted)** — the snap fix DOES work; the instrument now PROVES it so we stop arguing. Sponsor dials GROUND-Y only if he wants a different look. Files: `FloatDiagnostic.cs`, `CastawayCharacter.cs` (FloatGap/ComputeFloatGap), `AxeNudgeTool.cs` (GAP in panel), `MovementCameraScene.cs` (wiring). Ticket `86ca8rdkp` → ready-for-qa-test. Lesson → memory [[sponsor-prefers-direct-tweak-tools-for-fiddly-placement]] (don't ship a dial without its gauge). **RECONCILE ✅ DONE → PR #48 @ `c9039ad`** (clean, 0 code markers; EditMode **191/191** + PlayMode **74/74**; serve_soak verified stamp==HEAD, capture-gate 4/4). Combined = float-gauge (#47 `b6cd801`) + world-look pass-3 (#48 `3dad12a`). **⚑ Drew caught a PRE-EXISTING RED en route:** the committed `GradientSky.mat` (+ `LowPolyWaterMat`/`LowPolyTerrainMat`) were STALE/pale vs the `WorldLookPalette` SOURCE (`0.60/0.78/0.90` MidPoint 0.35 vs source `0.45/0.70/0.92` MidPoint 0.18) — **LIKELY WHY the prior soak's sky read greyish-blue** (the stale material masked Drew's S2 source changes); now regenerated source-correct in `c9039ad`. **Follow-up:** a guard that bootstrap-regen material values match `WorldLookPalette` source (Priya/DECISIONS). **→ orch VERIFIED fresh** (globalgamemanagers 15:02 UTC, stamp==HEAD `c9039ad`) **+ SERVED the Sponsor the combined soak** (drew-wt exe `c:/Trunk/PRIVATE/Far-Horizon-drew-wt/Build/Windows/FarHorizon.exe`, stamp `c9039ad`) 2026-06-15 — float gauge (F8) + S1 foam + S2 cheerful sky (stale-material fixed) + S3 detailed mountains. **→ SPONSOR SOAKED `c9039ad` 2026-06-15 → ✅ SKY + WATER + MOUNTAINS ACCEPTED ("for now") — WORLD-LOOK DONE** (S1/S2/S3 all land; the stale-material fix made the sky read cheerful). ❌ **FLOAT STILL PERSISTS — but the instrument PINNED IT (the breakthrough):** the Sponsor dialed GROUND-Y until the gauge reads **GAP=0.0000 "✓ planted" YET the character is STILL visibly floating above its shadow**. → ROOT CAUSE (finally): the snap/gauge "feet" is a **PROXY point (root/bone) DECOUPLED from the actual rendered mesh bottom** — the proxy grounds (GAP=0) while the visible mesh sits a fixed offset ABOVE it. Every prior iteration (×5) snapped the WRONG reference. **→ Devon (#47) ✅ FLOAT SOLVED → PR #47 @ `496bf3b`** (149 EditMode + 69 PlayMode + a guard that REDS against root-grounding). **REAL ROOT CAUSE (confirmed via the instrument dump — SUPERSEDES the smoothing-lag partial-diagnosis):** the snap grounded the avatar ROOT (FBX/armature origin), but the rendered sole (`SkinnedMeshRenderer.bounds.min.y`) sits at a **POSE-DEPENDENT** offset from the root — the walk anim lifts the feet bones so sole−root swings −0.15 (idle, slightly sunk) → **+0.47 (walk, FLOATING)** while the old gauge read GAP=0 "planted" (the false green). ALL ~5 prior iterations grounded the ROOT (proxy) → could never fix the pose-dependent float. FIX: measure (sole−root) each frame + snap so `SMR.bounds.min.y` lands on terrain; gauge GAP redefined = sole−ground; shadow tracks the plant level. Proof: meshBottom≈groundHit ≤3mm across the whole walk. Ticket `86ca8rdkp` → ready-for-qa-test. **DOC + DECISIONS update (maintain-docs/Priya at merge):** "ground the rendered SOLE not the avatar ROOT — the sole−root offset is POSE-DEPENDENT (anim lifts feet); root-grounding floats the mesh during walk" → unity-conventions §FBX + supersede the smoothing-lag DECISIONS entry. **→ RECONCILE ✅ DONE → PR #48 @ `26d04a0`** (clean, 0 code markers; materials source-correct — `GradientSky.mat` byte-verified vs `WorldLookPalette`, no greyish-sky drift; EditMode **191/191** + PlayMode **76/76**; serve_soak baked-stamp grepped from `resources.assets` = `26d04a0` == HEAD, capture-gate 4/4). **→ orch SERVED the Sponsor the (FINAL?) combined soak** (drew-wt exe `c:/Trunk/PRIVATE/Far-Horizon-drew-wt/Build/Windows/FarHorizon.exe`, stamp `26d04a0`) — REAL float fix (sole-grounding) + accepted world-look. **→ SPONSOR SOAKED `26d04a0` 2026-06-15 → FLOAT STILL THERE (attempt ~6 FAILED, Sponsor FURIOUS — "FUCKING annoying ... add extensive debugging"):** the F8 gauge reads rendered-sole-Y `0.2216` == ground `0.2216`, **GAP≈0 "✓ planted" — YET VISIBLY FLOATING EVEN STANDING STILL at load.** New symptoms: standing GAP JITTERS −0.003↔+0.0003; arrival causes a "BOB". → **DEEPER ROOT CAUSE (hypothesis, to PROVE):** the gauge measures `SkinnedMeshRenderer.bounds.min.y` but Unity SMR.bounds is the CONSERVATIVE anim-max AABB (not the current-frame mesh) → `bounds.min.y` sits BELOW the real soles → grounding it floats the visible mesh while GAP reads 0 (the DEEPER false-green); the AABB wobble = the jitter. **→ Devon (#47) ✅ FLOAT FIXED + ORCH-VERIFIED → PR #47 @ `2b93cec`** (149 EditMode + 71 PlayMode + guard `Snap_GroundsTheBakedSole_NotTheConservativeBoundsFloor` reds if anything re-reads bounds). **CONFIRMED root cause:** grounding `SkinnedMeshRenderer.bounds.min.y` was the deeper false-green — Unity SMR.bounds is the conservative anim-MAX AABB (garbage: `boundsMinY` 0.39 spawn → **−68.7 walk**), sat below the real sole → floated the mesh while GAP read 0. Fix grounds the per-frame `BakeMesh` actual-lowest-vertex. **ORCH READ the `[FloatTrace]` log myself (broke the false-green loop):** `bakedSole==ground==plant`, GAP `0.0000` standing (`0.0199`) AND after walk to foreshore (`-0.3272`); shadow grounded (`-0.3072`); no jitter, unified snap-rate (bob gone). **⚠ LATENT FOLLOW-UP (flagged, not blocking the float):** the snap drives `avatarRootY≈−69` + SMR bounds to −68 to compensate a LARGE intrinsic model offset — rendered output is correct but (a) bone-child props (HELD AXE — Sponsor's next step) may mis-seat, (b) wrong −68 bounds = latent frustum-culling pop. Investigate the 68-unit model offset post-float. **ORCH verifies the axe seats in-hand on the COMBINED build before serving. → RECONCILE-4 ✅ DONE (`fbe5d15`, 191 EditMode + 78 PlayMode, axe-in-hand CONFIRMED GOOD) — but the pre-serve verification CAUGHT A SINK BLOCKER → NOT SERVED.** Drew's gameplay-WALK verify: `2b93cec`'s per-frame `BakeMesh` sole-grounding RUNS AWAY — at `moving=True` onset BakeMesh returns a transient WRONG-SCALE sole (~33u), the snap over-corrects, `avatarRootY` settles at **−68.42** in a FALSE-GREEN equilibrium (gauge GAP=0 "planted" while the character is UNDERGROUND/out-of-frame — shore capture shows only the blob shadow). Standing/spawn grounded fine; WALK sinks. (The −68 I flagged from Devon's log = THIS bug, not benign.) **PATTERN (attempts 1-7): every approach DYNAMICALLY compensates a ~68-unit intrinsic model offset → floats (root/bounds) or sinks (BakeMesh runaway). The 68u + transient ~33u = almost certainly the cm/m 100× scale trap (character-pipeline.md spike finding).** **→ Devon redirect (attempt 8) agentId `a3904457724467c3e`:** ROOT-CAUSE the 68u offset (why is the rendered mesh 68u off its transform? residual 100× cm scale on the avatar/a bone?) + FIX IT AT THE SOURCE (avatar origin at the feet at unit scale) so ground-snap = a STABLE terrain-raycast + near-zero constant — ABANDON per-frame `BakeMesh` chasing (it diverges); GUARD `avatarRootY` BOUNDED across a gameplay WALK. **ORCH reads the WALK `[FloatTrace]` (root bounded + sole==ground while MOVING) before serving.** World-look ACCEPTED/locked; axe-in-hand GOOD (`ci-out/axehand/axe_closeup.png`). Sponsor: "solve the floating THEN I'll look at nudging the axe." World-look ACCEPTED (locked). On float-PROVEN-solved → reconcile #48 onto #47 → serve → axe-nudge → THE BIG MERGE (Sponsor-gated; + #41 DECISIONS + #42 Uma audit; close superseded #38-#44; Erik's 4 patterns). Sponsor: "solve the floating then I'll look at nudging the axe." **On float-solved + Sponsor OK → THE BIG MERGE** (Sponsor-gated; land #47 then #48 or retarget+CI; + #41 DECISIONS + #42 Uma audit; flip tickets; orch-docs PR; follow-ups). **WHIP (parallel): Priya ✅ DONE → PR #41 @ `97a1fbf`** (9 DECISIONS entries for the 2026-06-15 saga; entry 3 body-root grip REFINES — not contradicts — the 2026-06-13 world-space decision: old chibi rig's 267× lossy-scale made bone-local non-deterministic, new `mixamorig:RightHand` lossyScale-1 makes it superior). Tess QA + Devon review HELD until the Sponsor's look-verdict (world-look churned 3× → avoid re-QA waste). **BOARD QUIET — NOTHING in flight; all agents done** (Devon #47 `b6cd801` / Drew #48 `c9039ad` / Erik notes / Priya #41 `97a1fbf`). Sole gate = the Sponsor's combined-soak verdict on `c9039ad`. **On APPROVE → THE BIG MERGE** (Sponsor-gated `--admin --squash`): land #47 first OR retarget #48→main + re-run CI (EPERM-aware), merge #48 (= character + world-look, supersedes #47 + soak PRs #44/#43/#40/#39/#38) + #41 DECISIONS + #42 Uma audit → flip `86ca8rdkp`/`86ca8t9pq`/`86ca8r72j` complete + orch-docs PR (`.claude/docs` + STATE + erik/drew notes) + follow-ups (Erik's 4 accuracy/perf patterns; the Drew/Devon/Tess comment-hygiene NITs; Priya DECISIONS catch-up incl. the smoothing-lag + ambient-vs-albedo drafts). **On CHANGES** → dial via F9/F10 + bake, or re-dispatch the flagged surface.

**MERGE-TOPOLOGY (for the big merge, NOT now):** #48's base is `devon/adopt-hyper3d-castaway` (linear stack) → CI doesn't auto-run on #48 (fires only on PRs→main); local 187/187+66/66 + serve_soak is the authoritative soak evidence. At the big merge: land #47 first then #48, OR retarget #48→main (overlaps #47's diff) — re-run CI then. Did NOT retarget now (out of soak scope).

_Prior roster (all done):_
- **Devon `aa8509860f051d00b`** ✅ **DONE → PR #47 @ `dc6ad74`** (CI GREEN; 147 EditMode + 61 PlayMode; Self-Test posted): **C5 during-walk elevation ROOT-CAUSED (4th attempt nailed it) = EXPONENTIAL-SMOOTHING LAG** — snap math was correct at rest (feet+shadow plant on the −0.5054 foreshore dip, gap 0.0000), but a constant-rate (k=18) filter lagged the descending foreshore ~1.2cm at 5.5 u/s = "grounded standing, elevated walking"; blob shadow compounded it (driven off the RAW target while feet rode the SMOOTHED Y). Fix: **speed-adaptive snap rate** (`snapRateMove` 60 ≫ `snapRateRest` 18) + shadow off the avatar's ACTUAL world-Y (locked rest+motion) + **Sponsor-dialable `groundYOffset`** (default 0) + new F9 GROUND-Y target (Tab→PgUp/PgDn, reads `groundYOffset=<v>f` to bake). Guards: PlayMode `BlobShadow_TracksTheActualFeet_NotTheRawTerrainHit`, `GroundYOffset_LiftsTheFeet_ByTheDialedAmount`; EditMode `Avatar_GroundSnap_MoveRateExceedsRestRate_ForDuringWalkTracking`, `Avatar_GroundYOffset_ShipsAtSaneDefault`. Ticket `86ca8rdkp` → ready-for-qa-test. **Decision draft (Priya/DECISIONS):** during-walk percepts on a snapped avatar = smoothing-lag + shadow-vs-feet-source class (NOT snap-pick); guard via speed-adaptive snap rate + shadow-off-avatar-world-Y + ship a dialable offset rather than grind the value after 2+ rejects.
- **Drew `a3fa10f6d357c3a90`** ✅ **DONE → PR #48 @ `00f91c1`** (185 EditMode + 64 PlayMode; serve_soak stamp==HEAD): **W1 shoreline-out FIXED** (shore-dip `ShoreRampEnd 0.045`, `WaterlineWorldZ -10.2`, `WaterInlandOverlap 4` — beach objects on dry sand), **W3 clouds visible FIXED**, **W4 cheerful sky FIXED**; **W2 mountains PARTIAL** — chunkier + snow-capped, but the body still reads grey-blue on-frame (cold skybox AMBIENT dominates the distant flat-lit peak; albedo-warming alone can't flip it without re-tuning global ambient, which the Sponsor-liked grass/sand depends on) → shipped warmest-reasonable default + a **new F9 mountain warmth/brightness/snow dial** for the Sponsor to finalize at soak (give-him-the-knob). Ticket `86ca8t9pq` → ready-for-qa. **Decision draft (Priya/DECISIONS):** distant-vista hue is governed by skybox-driven AMBIENT not albedo → dial via F9 + bake, or a vista-specific lighting layer, NOT a global ambient change. ⚠ #48 @ `00f91c1` is based on the OLD #47 (`e424d32` via the `b54482c` reconcile) — when Devon pushes the new #47 walk-fix, RE-RECONCILE #48 onto it + regenerate Boot.unity.
- **Erik `a468eaee77587a233`** ✅ **DONE** → note `team/erik-consult/developer-accuracy-performance-research.md` (UNCOMMITTED on main → fold into orch-docs PR). Ticket `86ca92vrk` → complete. **TOP-4 patterns (payoff order; Strong evidence #1-#3):** (1) **Diagnose-Before-Fix** as a mandatory pre-fix PR convention (formalizes the isolation-probe method — kills 2-4 soak-overturns/defect, our recurring failure); (2) **PlayMode locomotion-sampling tests** — `[UnityTest]` per-frame (`yield return null`) assert feet-Y / held-prop envelope / finger-curl EVERY frame during walk (closes the "tests green but Sponsor sees during-walk elevation" gap — directly relevant to Devon's C5); (3) **Configurable Enter Play Mode** (disable domain reload per worktree → sub-second play-cycle; + `RuntimeInitializeOnLoadMethod` static resets); (4) **Frame Debugger SRP-batcher audit** as a TESTING_BAR gate before any new visual pass. Open-Qs → Devon/Drew grep (CBUFFER_START(UnityPerMaterial) completeness; live MaterialPropertyBlock in LowPolyZoneGen scatter). **SPONSOR DECIDED (walkthrough 2026-06-15): file ALL 4 Erik patterns as backlog tickets, SCHEDULE post-merge** → **Priya ✅ FILED 4 "to do" tickets (post-#48-gated): `86ca9a340` Diagnose-Before-Fix convention · `86ca9a36g` PlayMode locomotion-sampling harness (Urgent) · `86ca9a39q` Configurable Enter Play Mode · `86ca9a3b3` SRP-batcher TESTING_BAR gate. Dispatch AFTER the big merge.** **+ SPONSOR DECIDED (walkthrough): CLOSE superseded PRs #38/#39/#40/#43/#44 AT the big merge** (not now — keep as fallback until #48 lands on main).

On Devon + Drew done → re-reconcile #48 onto new #47 + regenerate Boot.unity → serve the COMBINED soak (Sponsor-gated). Erik's note → orchestrator routes (tickets / fold into `unity-conventions.md`); not on the soak path. **THE BIG MERGE stays Sponsor-gated** (protected branch; no merge without explicit sign-off). auto-status cron `265786dd` (15-min, PROBES each in-flight agent).

---

## RESUME NEXT-ACTION — 2026-06-14 EVENING (Sponsor PIVOTED to Hyper3D 3D-mesh character creation; AXE PARKED mid-re-dial)

**▶ LIVE — HYPER3D CHARACTER SPIKE (2026-06-15):** A NEW chunky-low-poly castaway was generated end-to-end this session: concept art via openai-image → Sponsor picked `concept_01_front.png` → re-posed to A-pose `concept_01_apose.png` (image-to-image) → Rodin **Gen-2.5** Image-to-3D (Quad mesh **8k**, symmetric, **de-lit** material) → exported FBX. **Files at `inspiration/hyper3d-castaway/`:** `Idle.fbx` (4.8MB, **WITH skin** = mesh+rig+idle), `Walking.fbx` (334KB, **WITHOUT skin** = walk clip), plus gen subfolder `52759510-d900-4329-af64-627d017b4661/` (`base*.fbx` + `texture_*.png` diffuse/normal/roughness/metallic/pbr). Concept refs in `inspiration/hyper3d-castaway/concepts/`. **Mixamo auto-rig PASSED** (Standard Skeleton 65, symmetry on) — both **Idle + Walk deform cleanly** (knees/hips/shoulders/elbows, no collapse) AND read on-style in the Mixamo preview. **DEVON IN-ENGINE SPIKE ✅ DONE — VIABLE** (2026-06-15, agentId `a9346e8c47e9fab42`, ticket `86ca8r72j` → ready for qa test, **PR #45 `devon/hyper3d-castaway-spike` @ `6522e56` — DO NOT MERGE**): the Hyper3D+Mixamo castaway imports clean + animates clean (Idle+Walk) + reads on-style in a **shipped URP exe** on BOTH Humanoid AND Generic rigs. Captures `Assets/_Spike/captures/0[1-5]_*.png` (orch-verified 02_idle + 03_walk — clean, on-style; minor hair-crown artifact = cosmetic). Spike isolated to `Assets/_Spike/`; `Boot.unity` + live `CastawayCharacter` untouched (worktree restored clean). Non-obvious finding (maintain-docs candidate): a capture-tool `BakeMesh`+`TRS(...,Vector3.one)` dropped the SMR cm→m 100× scale → a FALSE skin-mangle smear; fixed via `smr.bounds` (sibling of the stale-SMR-bounds trap).

**→ SPONSOR CHOSE ADOPT (2026-06-15)** → adoption ticket **`86ca8rdkp`** (to do): integrate the Hyper3D castaway into `CastawayCharacter`/`MovementCameraScene` + recolor-to-warm + re-attach held axe to the NEW rig's right-hand bone (⚠ old held-axe dial values are MOOT on the new skeleton; chibi flat-hair work is MOOT) + retarget tests + shipped-build capture gate. Devon owns; Drew reviews + Tess QA. **DEVON ✅ DONE → PR #47** (`devon/adopt-hyper3d-castaway` @ `b741b1b`, ticket `86ca8rdkp` → ready-for-qa; review-only DO NOT MERGE): new Hyper3D castaway replaces the chibi, renders clean + on-style in the shipped exe (warm-tan recolor, sandy hair, barefoot); EditMode 139/139 + PlayMode 47/47; serve_soak gate passed @ `b741b1b`. **⚠ LOAD-BEARING FINDING:** the Mixamo **Humanoid** rig EXPLODES the skinned mesh at runtime under the scaled scene hierarchy (cone displacement; the spike's bounds-following camera HID it) → shipped on **GENERIC rig** (transform-path binding, no muscle retarget) which renders clean. New right-hand bone `mixamorig:RightHand` (lossyScale 1 — no 267× trap). Held-axe default (F9-dialable): scale 0.45, offset (0.04,-0.04,0.06), euler (0,0,-90). Recolor = luma-preserving HSV remap (gradient kept). Supersedes soak PRs #44/#43/#40/#39/#38. **DOC-DEBT:** add the Generic-vs-Humanoid runtime-explosion + "spike-capture-follows-mesh masks displacement" lesson to `.claude/docs/character-pipeline.md` §Step 4 + `unity-conventions.md` §FBX (Devon couldn't — his worktree docs forked pre-`character-pipeline.md`).

**AWAY DISPATCHES on PR #47 (2026-06-15 ~01:33):**
> - **Drew peer code-review ✅ APPROVE_WITH_NITS** (agentId `a345174c1b1464991`, comment `issuecomment-4703818785`): code sound on all 5 points — Generic-rig decision correct + well-evidenced (transform-path bind, no muscle retarget), recolor preserves the toon gradient (`CharacterAssetGen.cs:288-290`), bone re-point excludes fingers (`MovementCameraScene.cs:610-616`), cap/`_hips` removed clean, CI green + mergeable CLEAN, 139/139+47/47. NOT blocking. **NITs (stale-vocabulary / comment-hygiene — ABSORB into PR #47's next Devon touch, else a `chore` at merge):** (1) misleading runtime `Debug.Log` `rig=Humanoid CopyFromOther` while the code does Generic/CreateFromThisModel (`CharacterAssetGen.cs:166-167,203`) — highest-value in a diagnose-via-trace codebase; (2) test-name/PR-body mismatch (`CastawayCharacterTests.cs:110` `_AndHumanoidAvatar`); (3) stale Humanoid method comments; (4) stale `1.8× avatar-root scale` comment (`MovementCameraScene.cs:470`); (5) inherited `267×-lossy-bone` comments on HeldAxeRig/AxeNudgeTool (now moot — Mixamo bones lossyScale 1). Drew also flagged `.claude/docs/character-pipeline.md` isn't on the PR branch — correct; it's uncommitted on main (the orch-docs PR will carry it).
> - **Tess QA ✅ APPROVE** (agentId `a2a992594b1de3ab8`, comment `issuecomment-4703840164`): built her own stamp-matched soak (`b741b1b`); all 6 gates PASS from the SHIPPED exe — chibi GONE + Generic castaway renders CLEAN (rig-trace: body upright at spawn, RightHand AT the body, NOT the Humanoid-explosion `(2100,-655,261)`); Idle+Walk clean; held axe seated ~4cm from RightHand (un-dialed default NOT flagged — F9 follow-up); world intact (trees/clouds/stump-axe/teal-sea/debris/fullscreen); 139/139 + 47/47, zero errors; recolor warm-tan, gradient preserved. Same 2 comment-hygiene NITs as Drew. Left ticket at ready-for-qa (the list has NO QA-approved intermediate status; close-on-merge is the orch's call). DID NOT MERGE.

**✅ PR #47 FULLY VALIDATED — awaiting ONLY the Sponsor (never-auto-decide):** Drew APPROVE_WITH_NITS + Tess APPROVE + CI green + mergeable CLEAN + 139/139+47/47 + shipped-build QA passed. The ONLY remaining gate is the **Sponsor's subjective look-approval + the merge go** (protected branch). On Sponsor APPROVE → orch does the bundled merge (PR #47 carries world+character, supersedes #44/#43/#40/#39/#38) + handle #41/#42 + close `86ca8rdkp`/`86ca8r72j`. The 2 comment-hygiene NITs (stale `rig=Humanoid` logs `CharacterAssetGen.cs:166,203` + test name `CastawayCharacterTests.cs:110`) ABSORB cleanly into the held-axe-re-dial / recolor follow-ups (they touch the same files) — no standalone dispatch. **SPONSOR WALKTHROUGH 2026-06-15 (he's back):** Q1 PR #47 → **SOAK-FIRST chosen → SOAK SERVED** (devon-wt exe `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe`, stamp `b741b1b` — serve_soak-verified == HEAD) for him to judge the new character + warm-tan recolor + **F9-dial the held axe** (placement reset on the new `mixamorig:RightHand` rig) + check the walk/swing. On his post-soak APPROVE → bundled merge (PR #47 + #41 DECISIONS + #42 Uma audit, supersedes #44/#43/#40/#39/#38) + close `86ca8rdkp`/`86ca8r72j`. Q2 world-look (PR #46) → **START-IN-PARALLEL chosen** → dispatching Erik (far-horizon vista / distant-landmass URP technique research — Uma deferred this) + Drew (implement clouds + sky-tint + near-vista from Uma's brief, base on PR #47 `b741b1b` — keeps the stack linear). The world-look LOOK is Sponsor-judged from the shipped-build capture (Uma's 3 gates: sky warm-vs-cold, bloom intensity, "does the vista feel big"). Reviewer for Drew = Devon (free after the soak build). **DISPATCHED 2026-06-15:** Erik ✅ DONE (agentId `a3cd824096f7ba199`, ticket `86ca8t9rh` → complete): far-vista = **Route A** — two faceted mountain-ring meshes (~500u/1000u) + URP exp² fog with fog-color == horizon `#DCE8E4` (kills the seam, no extra pass) + the 3-stop gradient skybox; ~550 tris, negligible. Note `team/erik-consult/world-look-far-vista-research.md` (UNCOMMITTED on main → fold into the coordination/orch-docs PR). RELAYED to Drew (`a388bf0edee884f7d`) to fold the far-vista into his world-look PR. Open Qs: confirm HDR gradient values w/ Uma; keep fog-color == horizon stop if it shifts; Drew ✅ DONE → **PR #48** (`drew/world-look-impl` @ `a89f508`, ticket `86ca8t9pq` → ready-for-qa; based on PR #47; DO NOT MERGE): clouds + 3-stop sky-tint + near/far vista (Erik's 2 rings 520/1000u folded in); 164/164 + 50/50; shipped-build captures verified. Diagnose-via-trace caught + FIXED a real bug — the `GradientSkybox.shader` was drawing OVER geometry (`positionCS.xyww` depth-force on a Background-queue SubShader → whole-frame wash); fixed to standard skybox-pass render state (`-flatSky` probe was the tell). **DOC-DEBT (maintain-docs candidate):** a custom URP skybox shader on `RenderSettings.skybox` must use standard skybox-pass render state (`Cull Off`/`ZWrite Off`, object-space dir, normal clip) — sibling of the magenta/cull-back false-symptom family → `unity-conventions.md §Editor-vs-runtime`. **REVIEW/QA DISPATCHED on PR #48 (away-tick):** Devon code-review ✅ **APPROVE_WITH_NITS** (agentId `abe404ed5e4d06149`, comment `issuecomment-4705202795`): skybox-fix sound + complete; clouds/sky/vista clean (outward-winding guarded, palette matches Uma, seam-kill bound to one `WorldLookPalette.SkyHorizon` constant, post `Add<T>` carry preserved); 2 non-blocking NITs (one-directional `CloudDrift` wrap — brittle if wind reverses; a test-name over-claim) → absorb into a follow-up. + Tess QA ✅ **APPROVE** (agentId `a6c1c2246f7521710`, PR comment): all 6 gates pass from her own stamp-matched build (`a89f508`) — sky no-wash (skybox bug GONE, `-wlDiag` confirms `FarHorizon/GradientSkybox` shipped), 6 faceted drifting clouds, 4 vista ranges + exact seam-kill (`fogColor==SkyHorizon #DCE8E4`), stacked character+world intact, 164/164+50/50, zero errors. Judgment-grade orbit captures: `C:\Trunk\PRIVATE\Far-Horizon-tess-wt\ci-out\worldlook\worldlook_{default,drift,horizon,horizon2}.png`. 1 non-blocking NIT (the `Skybox_*` EditMode tests wrap asserts in `if(resolved)`/`Assert.Ignore` → could silently pass headless; absorb in a follow-up). **→ PR #48 FULLY VALIDATED** (Devon APPROVE_WITH_NITS + Tess APPROVE); only the Sponsor's world-look LOOK-judgment remains (sky warmth / bloom 0.25 / "vista feels big"). **→ Sponsor chose ONE COMBINED SOAK (2026-06-15 walkthrough): PR #48 = character + world-look together (supersedes the earlier character-only PR #47 build) — orch **SERVED the combined soak** (drew-wt exe `c:/Trunk/PRIVATE/Far-Horizon-drew-wt/Build/Windows/FarHorizon.exe`, stamp `a89f508` — serve_soak-verified == HEAD). **→ SPONSOR SOAKED `a89f508` 2026-06-15 → APPROVE-IN-PART:** ✅ character identity + **warm-tan recolor read GOOD** (cream-tan shirt — KEEP, recolor done), axe color/blade good, sea/trees/rocks/debris/fullscreen good. ❌ **5 FIXES:**
> **CHARACTER (Devon, branch `devon/adopt-hyper3d-castaway` / PR #47, ticket `86ca8rdkp` → in progress):** (1) **WALKS IN THE AIR** — feet float off the ground during Walk → fix grounding (Mixamo Generic Walk root-Y / foot-height vs Idle). (2) **HELD-AXE ARM POSE** — holding the axe, the RIGHT arm/hand should be a bit AWAY from the body + RAISED ~10-15° (a "ready" carry, not hanging at the hip) → pose the axe-arm (additive/bone offset; replaces the F9 exact-dial — Sponsor gave a directive, not values). (3) **IDLE ARMS TOO TIGHT** — relax the arms a bit away from the torso in Idle.
> **WORLD-LOOK (Drew, branch `drew/world-look-impl` / PR #48, ticket `86ca8t9pq` → in progress):** (4) **TOO MANY MOUNTAINS, no sky/clouds visible** — the vista walls the whole horizon → REDUCE count/density/height so OPEN SKY dominates + the clouds show. (5) **MOUNTAINS ON THE WATER → put on LAND/ISLANDS** — they rise from/encircle the sea → reposition onto THIS island (land) or distinct far ISLANDS, leaving open sea + sky (deviates from Erik's full-encircling-ring — constrain to land/islands).
> **NO MERGE.** After both fix → reconcile (rebase #48 onto the updated #47 + regenerate Boot.unity) → re-soak the combined → Sponsor. (Azure-DevOps screenshot in the soak msg = unrelated EDC work, ignored.) **DISPATCHED 2026-06-15:** Drew world-look-soakfix ✅ **DONE → PR #48 @ `8fdfc1a`** (62 peaks/4 full 360° rings → **18 peaks in ~6 discrete island clusters** on landmass bases +2-6u, ≥5/12 azimuth sectors left open sky/sea; open sky now dominates; 167/167+50/50; captures verified). Erik's full-encircling-ring rec (`86ca8t9rh` Route A) **SUPERSEDED** → production vista = discrete land/island clusters (Drew decision draft → Priya/DECISIONS). Devon character-soakfix ✅ **DONE → PR #47 @ `0527c56`** (agentId `ab03968194309f212`): (1) walks-in-air ROOT CAUSE = NavMeshAgent grounds the root on the flat NavMesh slab (Y 0.084) ABOVE the dipping visible Zone-D terrain (Y 0.020) → feet float ~6cm; fixed via `CastawayCharacter.ApplyGroundSnap` (per-frame Ground-raycast → root.y 0.084→0.020). (2)/(3) arms via new `CastawayArmPose` (post-anim additive on `mixamorig:RightArm`/`LeftArm`, order 50 so the axe@order100 seats to the posed hand; bone axes measured via `-armTrace`). 141/141 + 51/51; serve_soak PASSED. **DOC-CANDIDATES (maintain-docs → unity-conventions §FBX):** (a) avatar floats above the VISIBLE terrain when grounded on the flat NavMesh slab → ground-snap raycast (asset-fine / view-is-the-problem family); (b) posing imported-clip-driven arms = additive post-anim upper-arm offset, bone-local→world axis measured first (not guessable). **→ RECONCILE ✅ DONE → PR #48 @ `4457d47`** (Drew `af311d933ee74d7d0`): clean reconcile — both feature sets in ONE regenerated Boot.unity (only `Boot.unity`+`BuildStamp` conflicted → regenerated per playbook; `MovementCameraScene` auto-merged clean); EditMode **169/169** + PlayMode **54/54**; serve_soak stamp==HEAD; ocean survives the regenerate. ⚠ CI does NOT run on #48 (base = Devon's branch, not main; CI fires at the main-merge) — local serve_soak + the full suite are the authoritative evidence. **→ orch SERVED the combined re-soak** (drew-wt exe `c:/Trunk/PRIVATE/Far-Horizon-drew-wt/Build/Windows/FarHorizon.exe`, stamp `4457d47` — serve_soak-verified == HEAD) 2026-06-15. **→ SPONSOR SOAKED `4457d47` 2026-06-15 → STILL NOT APPROVED (3 issues):** (A) **ARM POSE WORSE** with axe equipped (auto-`CastawayArmPose` reads awkward, axe too high) + Sponsor ASKED for an **ARM NUDGE TOOL** → **Devon ✅ DONE → PR #47 @ `0af20b8`** (agentId `a73f477b2d6eb81c2`): (A) ARM NUDGE TOOL added — `AxeNudgeTool` arm-pose target (Tab→arm, `[B]` right/left) dials `CastawayArmPose` per-arm eulers IN-GAME (F9-gated); conservative default replaces the rejected too-high pose; the SPONSOR dials → reports → Devon bakes. (B) WALK-FLOAT FIXED — `ApplyGroundSnap` was picking the renderer-DISABLED NavMesh slab (Y=0, topmost hit) over the dipping visible sand → now snaps to the renderer-ENABLED VISIBLE terrain (`-groundTrace`: feet track −0.02→−0.42 across the shoreward walk vs pinned-at-0 before). 142/142 + 52/52; serve_soak == HEAD. **DOC-CANDIDATE (maintain-docs):** ground-snap must pick the renderer-ENABLED Ground hit, NOT the topmost — the collision-proxy slab rides ABOVE the dipping foreshore terrain, so a topmost-hit raycast snaps feet to the invisible slab (the "still walks elevated" re-soak class; refines the existing ground-snap entry). **→ SERVED the Sponsor a CHAR soak (`0af20b8`) → he DIALED + flagged 2 issues (2026-06-15):** ✅ **DIALED VALUES to BAKE** (off the F9 panel; echoed to Sponsor for digit-check): HeldAxe `offsetFromHand=(0.0800,-0.1400,-0.0400)` `euler=(16.0,2.0,-82.0)`; `LeftArmEuler=(-5.0,22.0,0.0)`; `RightArmEuler=(-4.0,-50.0,-3.0)`. ❌ **(A) STILL ELEVATED** — 3rd float attempt; the renderer-enabled ground-snap STILL leaves him looking elevated → Devon RE-DIAGNOSE DEEPER: the visible FOOT-bone / mesh-origin offset vs the root (snap the FEET to the ground, not just the root); if that doesn't nail it, add a Sponsor-dialable ground-Y-offset to the F9 tool. ❌ **(B) WATER "elevated" — Drew REFUTED the water-Y bug** (`afb60000c4daaabf7`; diag commit `17450d5`): water-Y `-0.20` is geometrically CORRECT (below the +0.05 beach; sand dips under it at waterline worldZ -2.0). REAL cause = the default SEAWARD gameplay view (orbit pitch 55, dist 14) is mostly SKYBOX void → the sea reads as a thin teal stripe FLOATING over void (the "elevated/floating" percept); + foam band peaks at worldZ -10.5 (8.5u out), NOT the real waterline -2.0 (surf disconnected from sand). **→ Drew RE-DISPATCHED `a65a86dacdb180bf2`:** extend the ocean to fill the seaward view (sea-to-horizon, no void) + foam to the waterline; flag any camera pitch/dist change for the Sponsor (subjective). **⚠ DREW DIAGNOSIS PIVOT (resumed report, 2026-06-15):** the REAL root cause is NOT water-Y, NOT sea-extent, NOT occlusion (all refuted by trace) — it's a **TERRAIN SHADING / VERTEX-COLOR bug: the terrain renders PALE SKY-BLUE (`#CBE1EF`) instead of its sand/grass vertex colors at the gameplay angle** → the whole "pale void / floating water / elevated" percept. (camDiag: centre physics ray hits `Ground_Play` @ (0,0.02,5.25) but that pixel samples sky-blue — terrain present but mis-shaded.) **→ Drew (`a65a86dacdb180bf2`) DIRECTED to PROCEED:** magenta-clear confirm → pin + FIX the terrain shading/vertex-color root cause (suspect: **URP/Lit ignores vertex color** → needs the custom vertex-color shader per `unity-conventions.md` §Build-stripping/shaders; or a lighting/ambient wash) + fold in sea-extent + foam-to-waterline; commit+push PR #48; gameplay-orbit capture showing terrain in REAL sand/grass colors. drew-wt HEAD `17450d5` (no fix commit yet). Devon `a2d335cdba031adaa` (#47) still in-flight (probed alive earlier). ⚠ Resident `Unity.exe` PID 31644 lingering on the runner (PackageCache EPERM build risk — let it exit, don't force-kill). ❌ **(C) HELD-AXE POSITION CHANGES during walk** — the axe pose shifts/clips as the hand animates through the walk (the swing-class on the new rig) → Devon make the held-axe read as a STABLE believable grip through the WHOLE walk cycle (diagnose whether the holding-arm swing moves it; dampen/stabilize). ❌ **(D) RIGHT FINGER MANGLED** — the right-hand fingers deform badly (Generic-rig skinning artifact on the axe hand) → Devon diagnose + fix (re-weight / simplify / hide the mangled finger geo). **→ Devon (#47): BAKE dialed values + re-fix float (A) + axe-walk-stability (C) + finger-mangle (D). Drew (#48): water-Y (B). Then reconcile #48 onto #47 → combined soak.** (Sponsor's latest finger image couldn't load — asked to resend smaller; Devon repros in-build.) **DISPATCHED 2026-06-15 → CURRENT (~11:10):** ⚠ completion notifications DROPPED all session + agents PAUSE-await mid-task ("what would you like me to do?") — must PROBE, never wait. **Devon `a2d335cdba031adaa` — ✅ ALL 4 FIXES DONE IN SOURCE** (not pushed): #1 dialed values BAKED (held `(0.08,-0.14,-0.04)`/`(16,2,-82)`; `LeftArm(-5,22,0)`/`RightArm(-4,-50,-3)`); #2 float OVERTURNED = the **BlobShadow stranded ~9cm ABOVE the snapped feet** (body WAS grounded; the shadow floated → looked elevated) → shadow tracks feet (no F9 offset needed); #3 axe-walk-shift fixed via a body-local grip-anchor in HeldAxeRig (0.94→0.18u, 81%); #4 finger OVERTURNED = the **OPEN CLIP-HAND around the haft** (skinning CLEAN) → `CastawayFingerCurl` HasAxe-gated. → ✅ **DONE → PR #47 @ `e424d32` PUSHED** (145/145 EditMode + 59/59 PlayMode; serve_soak verified stamp==HEAD; capture-gate 4/4; grip close-up confirms closed-fist grip; card → ready-for-qa). Files: `HeldAxeRig.cs` (body-root grip-anchor swing-stabilize #3), new `CastawayFingerCurl.cs` (#4), `CastawayCharacter.cs` (shadow-grounds-to-feet #2), `CastawayArmPose.cs` (baked eulers #1), `MovementCameraScene.cs` (baked held-axe + wiring). **Decision draft (Priya):** held-prop stabilization vs clip arm-swing must be in the BODY-ROOT local frame (grip anchor), NOT world-space (world lags locomotion: 0.93→1.5u WORSE vs →0.18u). ⚠ #47 CI may need a re-run (raced Drew's local Unity PackageCache lock — EPERM policy). → **TESS QA #47 dispatched** agentId `aa5ee50f01f294b66` (tess-wt; early character gate; combined re-QA after the reconcile). **If session dies:** probe `aa5ee50f01f294b66` + `a65a86dacdb180bf2` (Drew #48) + `gh pr view 47`/`48`. **Drew `a65a86dacdb180bf2`** (supersedes afb6 water-Y-refuted): **ROOT CAUSE PINNED** (magenta-confirmed: terrain DRAWS but renders ambient-washed sky-blue `#CBE1EF` — vertex colors not reaching frag albedo; suspect URP/Lit-ignores-vertex-color / `LowPolyVertexColor` fallback) → ✅ **DONE → PR #48 @ `3ad8f44` PUSHED** (177/177 EditMode + 56/56 PlayMode; card ready-for-qa). **ROOT CAUSE overturned a 3rd time (and nailed):** the "pale frame" was NOT the terrain shader — the **vista landmass ISLANDS (esp. `Vista_Inland` @ worldZ -32) were DRAPING over the play space + burying the sand/grass terrain** at the gameplay orbit (`-hideVista` isolation proved the terrain renders perfect once they clear). Fix: pushed islands OUT + shrank footprints (≥15u radial clearance) + regression guard `Vista_LandmassBases_DoNotDrapeOverThePlaySpace`; folded in sea-extent 220→600u + foam-to-real-waterline. Captures `ci-out/paleframe/final*/sea_seaward.png` (stamp `17450d5` code-identical; re-bake at `3ad8f44`). Camera change NOT needed. **→ DEVON ✅ APPROVE #48** (code, world-look only; agentId `aacbabb3c6337bf0b`): vista-island fix sound (`Vista_Inland` clears the play box ~90u vs the 15u margin; guard `Vista_LandmassBases_DoNotDrapeOverThePlaySpace` correctly built — real serialized bounds, opens Boot.unity), sea-extent 220→600u + foam-at-real-waterline sound, no regressions to clouds/sky/skybox-shader/F9 tool; 2 non-blocking NITs (stale comment literals `LowPolyZoneGen.cs:611-612`; symmetric foam fade) → absorb. **Tess QA #47 ✅ APPROVE** (`aa5ee50f01f294b66`, comment `issuecomment-4707610303`): all 4 fixes PASS on her own shipped build @ `e424d32` (145/145 + 59/59, zero console errors). ⚠ #47 CI RED = environmental PackageCache EPERM → needs an orchestrator CI re-run before the eventual merge (NOT a code fix; her clean local run is authoritative). 2 non-blocking NITs (stale PR-body test counts; `-verifyAxe` lower-body framing) → absorb. **→ BOTH PRs APPROVED (Tess #47 + Devon #48) → RECONCILE ✅ DONE → `drew/world-look-impl` @ `b54482c`** (Drew `abd8fe3346ab5d19d`, pushed): both feature sets confirmed in the shipped build (character all-4-fixes + full world-look); only `Boot.unity`+`BuildStamp` conflicted (regenerated via bootstrap); `MovementCameraScene`+`BootstrapProject` auto-merged clean (0 markers); EditMode **181/181** + PlayMode **64/64**; serve_soak passed; captures show both (`ci-out/wlverify/` + `ci-out/castverify/castaway_front.png`). **→ orch SERVED the COMBINED soak** (drew-wt exe `c:/Trunk/PRIVATE/Far-Horizon-drew-wt/Build/Windows/FarHorizon.exe`, stamp `b54482c` — serve_soak-verified == HEAD) 2026-06-15. **→ SPONSOR SOAKED `b54482c` 2026-06-15 → APPROVE-IN-PART:** ✅ **shoreline LOOK** ("I really like the shoreline") + **grass/sand acceptable** (terrain pale-frame GONE). ❌ 5 fixes: **(W1) shoreline POSITION** — move the waterline OUT (seaward) so the tree/campfire/debris sit on DRY SAND, not in the water (keep the foam/sea look he likes); **(W2) mountains STILL not acceptable** (read as flat grey triangles); **(W3) NO clouds visible**; **(W4) sky not nice**; **(C5) STILL elevated WHEN WALKING** (4th attempt — standing IS grounded per Tess QA, but the DURING-WALK case isn't). Sponsor: "I'll look into the rest when these are fixed." **→ Devon (#47): C5 walk-elevation + add F9 ground-offset dial. Drew (#48): W1 shoreline-out + W2 mountains + W3 clouds + W4 sky.** Then re-reconcile #48 onto new #47 → combined soak. **On APPROVE → THE BIG MERGE** (Sponsor-gated `--admin --squash`): merge #48 `b54482c` (= character + world-look, supersedes #47 + soak PRs #44/#43/#40/#39/#38) — re-run #47/#48 CI first (EPERM) — + #41 DECISIONS + #42 Uma audit → flip tickets `86ca8rdkp`/`86ca8t9pq`/`86ca8r72j` complete + fold the orch-docs (`.claude/docs` + STATE + erik/drew notes) into an orch-docs PR + the follow-ups (held-axe further dial, NITs, routes-quality patterns). **#48 is the combined PR.** On CHANGES → dial via F9 / re-dispatch the flagged surface. **If session dies:** combined build on `drew/world-look-impl` @ `b54482c`; re-serve via serve_soak from a clean `b54482c` checkout. **If session dies:** probe `abd8fe3346ab5d19d` + `gh pr view 48`; on reconcile done → build combined soak via serve_soak from the reconciled #48 + serve Sponsor; + re-run #47 CI at merge. **SEQUENCE:** Tess QA #47 (in flight) + Devon review #48 → on both clear → **Drew reconciles #48 onto #47 `e424d32` + regenerate Boot** → orch builds the COMBINED soak → Sponsor. **On BOTH PRs pushed → reconcile #48 onto updated #47 + regenerate Boot → serve the Sponsor the combined soak (Sponsor-gated).** **auto-status RE-ARMED** (cron `265786dd`, 15-min; the tick PROBES each in-flight agent since notifications drop). **If session dies:** probe `a2d335cdba031adaa` (Devon #47) + `a65a86dacdb180bf2` (Drew sea-extent #48) (SendMessage) + git log devon-wt/drew-wt + `gh pr view 47`/`48`. (C) **WORLD-LOOK "doesn't look good"** — water + sky + clouds + mountains all need to look better (mountains render as FLOATING TRANSLUCENT shards; water = hard flat diagonal edge). **→ Sponsor DIRECTED (2026-06-15): "consult Erik for fixing sky, cloud, mountain, water."** ERIK all-4 quality research ✅ DONE (agentId `ad8d4eb4a0800ff6c`; ONE note `team/erik-consult/world-look-quality-research.md` — ranked technique + "Drew implements like this" per element; ticket `86ca8t9pq` in-progress; **+ Sponsor added 2026-06-15: evaluate https://3d-agent.com/ — fit for world-look assets / characters/props / asset-pipeline vs Hyper3D + procedural + Blender, with a use/don't recommendation + any sign-up/cost flagged → `team/erik-consult/3d-agent-eval.md`; + ref article on low-poly AI-prompt 3D art (aifreeforever.com) — assess AI-prompt generation for world-look assets / characters vs Hyper3D image-to-3D**). **Erik VERDICTS:** mountains = surface/winding bug → solid+grounded+opaque (likely the SAME fix as the sea-grid inverted-winding); sky = correct background-queue gradient shader; water = depth-fade shoreline foam + flat-shaded UNWELDED mesh + depth gradient (Shader Graph / Cyanilux URP); clouds = chunky blob-mesh (Erik suggested Blender-MCP, NOT connected → procedural). **3D-AGENT.com NOT recommended** (photoreal, no low-poly control; Meshy.ai free tier as fallback); existing routes cover everything. (Drew's pre-consult hands-on rework `a31149b0ddc4380f8` was stopped — only reading, drew-wt clean.) **→ DREW ✅ DONE → PR #48 @ `feeeaad`** (agentId `ad5a6160844e79abe`): mountains + sky + water + clouds reworked + F9 `WorldLookNudgeTool` (Tab-cycles SKY/FOG/CLOUDS/MOUNTAINS, logs values to bake). **Mountain ROOT CAUSE = DOUBLE-FADE** (per-cluster tint 0.45-0.82 + Exp² fog both → far clusters washed 70-95% to horizon = the "floating translucent shards"); fix = cap tint (`MtnFadeCap` 0.25) + pull clusters in (`MtnDistanceScale` 0.55) + drop the 950u ghost range + ground each on a new `FacetedLandmass` island shelf (Erik's winding/surface hypothesis REFUTED by trace — shader was opaque alpha=1, winding already green). **Water** = vertex-color foam on the OPAQUE shader (deliberately NOT Erik's Transparent depth-texture route → sidesteps the shadow/fog render-queue regression risk). 176/176 + 56/56; captures stamped pre-commit (`4457d47`) → orch re-bakes to HEAD via serve_soak. **Decision draft (Priya/DECISIONS):** vista = FOG-only recession + ≤500u grounded islands; supersedes Erik's far-encircling-ring + deep-fade-tint. **SEQUENCE TO MERGE:** (1) Sponsor's CHAR-soak arm-dial + walk-confirm (in flight — char build `0af20b8` served) → (2) Devon bakes the arm into #47 → (3) reconcile #48 (`feeeaad`) onto the final #47 + regenerate Boot → (4) serve the COMBINED soak (baked arm + grounded walk + new world-look + F9 world-look tweak tool) → (5) Sponsor approve → THE BIG MERGE. **#48 review:** Devon (reserved — keep devon-wt free for the arm-bake; review after). **If session dies:** PR #48 @ `feeeaad`; on Sponsor arm-values → Devon bakes (#47) → reconcile #48 + combined soak. **OPTIONAL Sponsor billing decision (never-auto-decide; DEFAULT = skip, existing routes cover it):** trial 3D-Agent ($19/mo — Erik recommends AGAINST) or buy Polyverse Skies / Stylized Low Poly Clouds (~$15 ea, optional accelerators). **If session dies:** probe `ad5a6160844e79abe` (SendMessage) + `gh pr view 48`.
> **+ NEW R&D (Sponsor 2026-06-15): Erik researching how to LEVEL UP our in-house procedural + Shader-Graph routes** (ticket `86ca8x038`, agentId `a8cb838cce9919f16`, note `team/erik-consult/procedural-shadergraph-quality-research.md`) — general quality bar for OUR own routes (Sponsor declined paid tools; committing to procedural + Shader Graph). ✅ **DONE** — note `team/erik-consult/procedural-shadergraph-quality-research.md`. TOP-3 (impact/effort): (1) merge the `QuantizeFine` 24-step pink-cast fix (already authored — verify it's in the stack at merge); (2) **ddx/ddy `_FlatShading` mode** in `LowPolyVertexColor.shader` → kills the winding-inversion bug class BY CONSTRUCTION (default-off flag; terrain/water/canopy unaffected); (3) **depth-fade intersection foam** on water (URP Asset Depth+Opaque textures + a `LowPolyWater.shader` Transparent-queue `SampleSceneDepth`) — biggest visual gap, OVERLAPS Drew's current water. Secondary: Fresnel rim, Blender chamfer-highlight (needs Blender MCP — not connected), vertex-color AO. **Open-Q RELAYED to Drew (`ad5a6160844e79abe`):** water render-queue Opaque→Transparent must not break shadow-receive/fog — verify in-build. **→ FOLLOW-UP impl ticket** (adopt #2 ddx/ddy flat-shading + Fresnel + AO) AFTER the current world-look/character soak settles — NOT now. (Sponsor also sent a 'see' image that exceeded the API image-size limit → asked to resend smaller.) **NO MERGE.** After character + world-look both fixed → re-reconcile #48 → re-soak. On APPROVE → THE BIG MERGE (PR #48 brings character + char-fixes + world-look + mountain-fixes, supersedes #44/#43/#40/#39/#38 + the now-subsumed #47; + #41 DECISIONS + #42 Uma audit) → close `86ca8rdkp`/`86ca8t9pq`/`86ca8r72j`/`86ca8ca1m` etc. + fold held-axe F9 dial + the Drew/Tess/Devon NITs + the Erik-ring-superseded DECISIONS entry as follow-ups. **If session dies:** the combined build is on PR #48 @ `4457d47`; re-serve via serve_soak from a clean `4457d47` checkout. On both done → reconcile (rebase #48 onto updated #47 + regenerate Boot.unity) → re-soak combined → Sponsor. **If session dies:** probe `ab03968194309f212` + `a56ce8d47dc66025e` (SendMessage) + git log devon-wt/drew-wt + `gh pr view 47`/`48`. On his combined-soak APPROVE (character + recolor + held-axe F9-dial + world-look) → THE BIG MERGE: merge PR #48 (brings character + world-look, supersedes soak PRs #44/#43/#40/#39/#38) + #41 DECISIONS + #42 Uma audit → close `86ca8rdkp`/`86ca8t9pq`/`86ca8r72j` → fold the held-axe re-dial + recolor sign-off + the Drew/Tess comment-hygiene NITs as follow-ups.** On verdicts: both APPROVE → QUEUE the Sponsor world-look look-approval (he judges from Tess's captures). **If session dies:** probe `abe404ed5e4d06149` + `a6c1c2246f7521710` (SendMessage) + git log devon-wt/tess-wt + `gh pr view 48`.

**→ SPONSOR SIGN-OFF QUEUE (never-auto-decide — surfaced on return):** (1) **PR #47 look/merge** — judge the new character from the shipped-build captures + approve the swap; (2) **held-axe F9 re-dial** on the new rig (old dial values moot); (3) **final recolor** sign-off; (4) **world-look look-approval** (Uma PR #46 — esp. "does the vista feel big"); (5) **the big merge** bundling world+character (supersedes #44/#43/#40/#39/#38) + #41 DECISIONS + #42 Uma audit.

**WHIP PASS 2026-06-15 ~01:1X:** Uma ✅ DONE — world-look-polish DIRECTION BRIEF (clouds + far-horizon vista + sky-tint) at `team/uma-ux/world-look-polish-direction.md`, **PR #46** (`uma/world-look-direction` @ `9e4ecf1`, ticket `86ca8rd6a` ready-for-qa). PARKED — Sponsor chose character-adoption first; world-look wave deferred (needs Sponsor look-approval — esp. "does the vista feel big" — before Erik research / Priya tickets / Drew impl sequence in). Idle-justified: Devon (next = character-adoption = Sponsor-gated), Drew (world-look impl sequences after Uma + Sponsor look-approval), Tess (no ready QA/merge target — spike is DO-NOT-MERGE), Priya + Erik (sequence after Uma's brief). **If session dies:** probe agentId `ac803e7694b376f22` (SendMessage) + `git -C ../Far-Horizon-uma-wt log`; Devon spike already landed (PR #45). **Sponsor approval REQUIRED before swapping the live chibi.** **AXE RE-DIAL PARKED** (resume after the character spike or on Sponsor's call):
> - **Soak build = `9bee6b8`** (devon-wt exe `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe`, stamp `zoned | 2026-06-14T21:30:09Z | 9bee6b8` — orch-verified, fresh-built this session via serve_soak).
> - **Sponsor F9-dialed held-axe values (NOT yet baked):** set A `offsetFromHand=(0.0030,-0.1370,0.0490)` `euler=(37.3,-4.2,-86.1)`; set B (he said "this is better") `offsetFromHand=(0.0830,-0.0170,-0.0310)` `euler=(15.3,-18.2,-104.1)`.
> - **NEW BLOCKER (dynamic — NOT a bake):** the held axe **swings INTO HIS HEAD during the walk arm-swing.** Root cause (orch-read `HeldAxeRig.cs:65-66`): `rotation = hand.rotation * Euler(relEuler)` tracks the FULL hand-bone rotation INCLUDING the local walk arm-swing arc → the axe head sweeps up into the head at the top of the forward swing (position = `hand.position + worldOffsetFromHand`, world units). The static F9 nudge cannot fix a swing. Fix candidates for Devon: (1) gentle the holding-arm swing amplitude in the Walk clip; (2) decouple the rotation source — follow body/root yaw for turns but NOT the local arm-swing; (3) clamp the axe out of a head zone; (4) damp the follow. Needs a Devon fix + a PlayMode regression (axe clears the head across the full Walk cycle), THEN bake the dialed pose. main @ `d9dbf62`; nothing merged.

---

## RESUME NEXT-ACTION — 2026-06-14 (PRIOR SESSION DIED mid-fix-wave → RE-DISPATCHED FRESH: Devon a4f026be + Drew a804d2b2 in flight → re-integrate → Tess QA → re-soak)

**▶ LIVE (2026-06-14, RECOVERY — fix wave RE-DISPATCHED after a dead session):** The prior session dispatched the fix wave (Devon `a141eb136a067f4ea` / Drew `ac420481aa96d9c5a`) then DIED, taking both background agents with it. Verified THIS session: both old agentIds return "no transcript to resume — cleaned up"; NO `devon/castaway-soakfix6` / `drew/scatter-rocks-wreckage` branch on origin; both worktrees sat on the fix-wave branch name @ `5f7e7ba` CLEAN (Step-0 branch created, zero commits). **Zero code lost, zero progress made.** Sponsor (present → clicked "Re-dispatch fix wave" popup) → **RE-DISPATCHED FRESH 2026-06-14, both background, each `git checkout -B <branch> 5f7e7ba`:**
> - **Devon `a4f026bef371584b3`** → `devon/castaway-soakfix6` — FIX1 HAIR reads-as-helmet/seam from the DEFAULT gameplay cam (RECURRING false-green ~4–5×: judge from the Sponsor's actual over-shoulder default orbit MULTI-ANGLE, NOT `-verifyHair`; add a gameplay-cam silhouette guard) + fold close-up seam NIT `86ca8m3t2`; FIX2 held-axe handle-grip DEFAULT (was dangling blade-down at hip); FIX3 nudge-panel OFF the HUD + clearer. Tickets `86ca8ce6y`+`86ca8ca1m`+`86ca8m3t2` → in progress. **✅ DONE → `f0f283d` (VERIFIED on origin via ls-remote). EditMode 130/130 + PlayMode 40/40 local; serve_soak green stamp==`f0f283d`; no CI run (feature branch; CI on:main → runs on the integration PR).** FIX1 hair ROOT CAUSE: the prior 4–5 iters FLATTENED a dome (and the guards ENFORCED that flatness) → it kept reading as a helmet; redo = `LowPolyMeshes.TuftedHair` = a CLUSTER of protruding tuft lobes over a base cap (blob-canopy idiom), + new `HairSilhouetteTrace` instrument measuring gameplay-over-shoulder-cam roughness (0.302/5 protrusions @55°, 0.343/5 @70° top-down, 0.366/4 profile vs a dome's ~<0.05/0), new guard `TuftedHair_ReadsClumped`, folds 86ca8m3t2 via `CastawayHair_SeatsFlushToHeadBone`. FIX2 axe handle-grip default (max.y 0.557 < head-bone 0.775 = clears ear, blade hangs at side). FIX3 nudge panel right-anchored off the hotbar (`NudgePanel_ClearsTheHotbar` across 5 sizes; F9-gated/inert). **DOC-CANDIDATE: bootstrap-restore `git checkout -- Assets/ ProjectSettings/` SILENTLY reverts uncommitted source `.cs` edits under `Assets/Scripts/` → commit source BEFORE bootstrap (integration playbook step 5 already does this).**
> - **Drew `a804d2b25fda061b7`** → `drew/scatter-rocks-wreckage` — FIX1 scatter rocks `FacetedSphere(0.6,0,0.30)` subdiv-0 spikes → round into faceted boulders (subdiv ≥1) + warm + thin ~22 (Sponsor = **beautify+thin, NOT remove**; "remove entirely" still a one-word switch); FIX2 beach wreckage reads as planks/crate/driftwood. Tickets `86ca8m5zu`+`86ca8feuf` → in progress. **✅ DONE → `71541cf` (VERIFIED on origin via ls-remote).** FIX1 rocks: `FacetedSphere(0.6,0,j0.30)`→`(0.6,2,j0.14)` (6→66 verts, no spike) + warm `RockCol .55→.66` + thinned ~22→~5 clustered outcrops (10–20 rocks) spawn-clear; new `RockVerifyCapture` (`-verifyRock` 3 yaws) + `RockBoulderSceneTests` (spike/jitter/normal regression guards). **FIX2 was a FALSE PREMISE — beach wreckage already shipped at `ed8da36` (CONFIRMED ancestor of `5f7e7ba`), 4/4 BeachDebrisSceneTests green, unchanged on his branch → NO redo needed.** EditMode 137/137 + PlayMode 39/39 local; serve_soak green stamp==`71541cf`; no CI run (feature branch, CI is on:main — runs on the integration PR). **Drew follow-up: consider closing `86ca8feuf` as already-satisfied-by-`ed8da36`.**
>
> **BOTH BACK + GREEN + PUSHED (Devon `f0f283d`, Drew `71541cf`).** Merge-tree verified clean-integration (orch, this session): ONLY `Boot.unity`+`BuildStamp.txt` conflict (binary→regenerate); `MovementCameraScene.cs` AUTO-MERGES CLEAN (both Devon's hair/axe/nudge params + Drew's rock params kept). **→ INTEGRATION DISPATCHED to devon-wt** (base-off-Devon `f0f283d` + merge Drew `71541cf` → new branch `integ/soakfix6-rocks`; follow `team/orchestrator/integration-39-38-playbook.md` w/ new branches+guards; agent `a647ebfa6f884e358`, background). **✅ INTEGRATION DONE → PR #43 @ `30ef320`** (MERGEABLE, SUPERSEDES #40; CI green run `27495499017`; EditMode 137/137 + PlayMode 40/40 BOTH feature-sets together on the regenerated scene; conflict surface matched the merge-tree pre-check exactly — only Boot.unity+BuildStamp regenerated, MovementCameraScene auto-merged 0 markers; soak exe `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe`, baked stamp `zoned | 2026-06-14T10:12:05Z | 30ef320` == HEAD — ALL VERIFIED on origin). **→ Sponsor asked "when can I soak" (2026-06-14) — combined build is VERIFIED-READY NOW** (devon-wt exe `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe`, stamp `30ef320`, serve_soak capture-gated). **Sponsor chose Tess-QA-FIRST (popup) → TESS QA #43 DISPATCHED, agent `a65b38436375f9d94`** (tess-wt, checkout `30ef320` DETACHED; builds her OWN fresh stamp-matched soak exe; all 5 fix tickets → ready-for-qa-test). On Tess APPROVE → SERVE the Sponsor (her exe path + verified stamp `30ef320` + the 4-reject checklist). On Tess REQUEST_CHANGES → re-dispatch the flagged fix (devon/drew by surface). **✅ TESS APPROVE** (agent `a65b38436375f9d94`, PR #43 comment `issuecomment-4701566342`): all 4 hard gates PASS from the gameplay orbit (hair clustered-tufts via the PERCEPT guard `TuftedHair_ReadsClumped` 0.30–0.37 roughness vs 0.12 dome floor; axe handle-grip; nudge off-HUD inert; rounded warm boulders) + carries (teal sea / gray-slab-GONE / debris / clothes-separate / one-axe swap / click-move); 137/137 + 40/40; zero console errors. Non-blocking NIT: `-verifyRock` framing washes boulder warmth (soak-tunable; silhouette fix unambiguous). Process note: #43 has no author Self-Test *comment* (evidence in PR body — acceptable for a mechanical integration; integrator should add a one-liner). **→ SOAK SERVED to Sponsor 2026-06-14:** exe `c:/Trunk/PRIVATE/Far-Horizon-tess-wt/Build/Windows/FarHorizon.exe`, baked stamp `zoned | 2026-06-14T11:06:08Z | 30ef320` (ORCH-VERIFIED via ls + grep == HEAD) + the 4-reject + carries checklist. **AWAITING Sponsor soak verdict.** **→ SPONSOR SOAKED `30ef320` 2026-06-14 (stamp confirmed in 4 screenshots) → APPROVE-IN-PART + 2 fixes:**
> - ✅ **AXE PERFECT — Sponsor dialed in held+stump via the F9 nudge tool; BAKE these as the DEFAULTS** (transcribed from his screenshots; European-decimal commas → dots): **HELD `offsetFromHand=(-0.018,-0.038,-0.021)` `euler=(0.0,9.9,124.7)`** · **STUMP `localPos=(-0.210,1.540,0.430)` `euler=(12.0,53.0,48.0)`**. (Echoed back to Sponsor for digit-check.)
> - ✅ **NUDGE TOOL worked** (off the hotbar, he used it successfully) — keep build-gated/inert.
> - ❌ **HAIR — the soakfix6 `TuftedHair` redo is WORSE** (orange tuft-lobes poke out as spikes/antlers). Sponsor verbatim: "hair is not good, id rather have the FLAT hair than this, if messy cannot be accomplished." → Devon REVERTS hair to the clean flat dome (the `5f7e7ba` pre-tuft state: flat `MessyHairCap` + soakfix5 front-fringe fix, NO forward tuft, NO spikes); DROP the `TuftedHair` lobes + the `TuftedHair_ReadsClumped` guard; keep `CastawayHair_SeatsFlushToHeadBone`. Genuinely-messy-hair-done-well = a FUTURE sourced-hair-asset effort, NOT another procedural grind (surfaced to Sponsor).
> - ❌ **ROCKS — still don't read as rocks + now TOO FEW** ("before was a decent amount"). The rounded `FacetedSphere` boulders read as smooth dark mounds. → Drew SOURCES a low-poly rock asset (procedural keeps blob-ing → apply the "source when procedural fights the style" heuristic, unity-conventions §Asset creation — like the axe/character) + RESTORE density to ~22 + warm-grey faceted stone.
>
> **→ FIX WAVE (soakfix7) DISPATCHED 2026-06-14, both off `30ef320`, background, parallel:** Devon `ae0bdd1dc84835e74` → `devon/hair-flat-axe-bake` (revert hair to flat dome `86ca8ca1m` + bake Sponsor's axe DEFAULTS `86ca8ce6y`); Drew `a17fec30f5c2558be` → `drew/rocks-sourced` (SOURCE a low-poly rock asset / angular-faceted rework + restore density ~22, `86ca8m5zu`). → re-integrate (base-off-Devon + merge Drew → new integ off `30ef320`) → Tess QA → re-soak. Reviewer Tess. Axe color/feel + nudge + sea + debris + gray-slab + clothes all Sponsor-APPROVED (carry unchanged).**
> - **Devon ✅ DONE → `46da7dd` (PUSHED to origin):** hair `LowPolyMeshes.cs` BYTE-IDENTICAL to `5f7e7ba` (orch-verified empty diff = clean flat-dome revert; TuftedHair + AppendCutDome removed); axe defaults BAKED (HELD `offsetFromHand=(-0.018,-0.038,-0.021)` euler `(0,9.9,124.7)`; STUMP `localPos=(-0.210,1.540,0.430)` euler `(12,53,48)`); restored `MessyHairCap_CrownIsFlatPlateau`+`_FrontFringe` guards, dropped `TuftedHair_ReadsClumped`, KEPT `CastawayHair_SeatsFlushToHeadBone`; held-axe goldilocks band → `[0.7,1.3]`; EditMode 138/138 + PlayMode 40/40. `HairSilhouetteTrace` kept as a durable instrument (un-wired). F9 nudge stays build-gated.
> - **Drew ✅ DONE → `d358176` (PUSHED to origin):** Blender MCP NOT reachable → fallback = reworked angular flat-shaded PROCEDURAL `FacetedRock` (NOT a sourced asset — **relay to Sponsor: connect Blender if a sourced rock is still wanted**). 3 diagnose-via-trace passes: flat-per-face-normals = the stone read; fixed backface-cull (inward-wound faces → `FacetedRock_AllFacesPointOutward` guard); fixed pink-tint (coarse 12-step quantizer split warm-grey → R>G=B pink → 24-step `QuantizeFine`). Density restored to 22; grass not dark-sharding. EditMode 144/144 + PlayMode 41/41. Orch sanity-checked `rock_a.png` → reads as chunky faceted stone ✓. **DOC-CANDIDATE (maintain-docs; defer API cites until merge): (1) explicit-face-normal flat-shaded meshes need outward-winding enforcement or they backface-cull (sibling of the −Z grid); (2) coarse 12-step quantizer splits near-neutral warm tints into pink R>G=B — use the fine quantizer for near-grey props.**
> - **→ BOTH BACK+GREEN+PUSHED (Devon `46da7dd`, Drew `d358176`). Merge-tree verified clean (orch this tick): ONLY `Boot.unity` regenerates; `LowPolyMeshes.cs` auto-merges (Devon TuftedHair-removal + Drew FacetedRock-add). → INTEGRATION DISPATCHED `af630f6555a4049d4` → `integ/soakfix7`** (base-off-Devon `46da7dd` + merge Drew `d358176`; brief verifies no-TuftedHair-leftover AND FacetedRock-present + regenerate Boot.unity + dual-feature gate). Then Tess QA the combined → QUEUE the soak for the Sponsor (do NOT decide/merge — his sign-off). **✅ INTEGRATION DONE → PR #44 @ `808725a`** (MERGEABLE, supersedes #43; CI green run `27498725850`; EditMode 145/145 + PlayMode 41/41 both features together; NO TuftedHair leftover + FacetedRock present, orch-verified on origin; soak exe `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe` stamp `808725a`). **→ TESS QA #44 DISPATCHED (away-tick), agent `a5148268e4cd8da7f`** (tess-wt checkout `808725a` DETACHED; builds own fresh soak exe; tickets `86ca8ca1m`/`86ca8ce6y`/`86ca8m5zu` → ready-for-qa). On Tess APPROVE → QUEUE the soak for Sponsor (exe + verified stamp + 2-fix checklist: flat-hair + faceted-rocks-as-stone-density-22; carries axe-at-baked-default/sea/debris/gray-slab/clothes/nudge-off-hotbar). Do NOT decide/merge — Sponsor sign-off. **✅ TESS APPROVE (#44)** — agent `a5148268e4cd8da7f`, PR #44 comment `issuecomment-4702075103` (`gh pr review --approve` classifier-denied → verdict in comment, established pattern). All 3 soakfix7 gates PASS (flat hair clean from 4 angles incl top-down; faceted warm-grey stone from 3 yaws, 12 `LP_Rock`, no pink/mounds/spikes; held+stump axe at baked default, `-verifyCraft` rc=0) + carries (sea/debris/gray-slab-GONE/clothes/nudge-off-hotbar/click-move); 145/145+41/41. **→ SOAK QUEUED FOR SPONSOR (away-tick; NOT served-and-decided — his sign-off):** exe `c:/Trunk/PRIVATE/Far-Horizon-tess-wt/Build/Windows/FarHorizon.exe`, baked stamp `zoned | 2026-06-14T14:31:17Z | 808725a` (ORCH-VERIFIED via grep == HEAD; tess-wt detached at 808725a — if stale by return, re-serve via serve_soak from a clean 808725a checkout). Checklist: (1) HAIR clean flat (the revert, no spikes/tufts); (2) ROCKS chunky faceted warm-grey stone ~22 clustered (not mounds/spikes/pink); (3) AXE held gripped at handle/side + stump in block (your dialed values); carries teal-sea/debris/no-gray-slab/weathered-clothes/nudge-off-hotbar/click-move. **AWAITING Sponsor soak verdict — team FULLY IDLE, everything else Sponsor-gated.** **→ SPONSOR SOAKED `808725a` 2026-06-14 (stamp confirmed in 3 screenshots) → APPROVE-IN-PART:** ✅ ROCKS ("stones look much better now") faceted stone APPROVED · ✅ HAIR acceptable-for-now (flat) · ✅ STUMP axe good. ❌ **HELD AXE — 2 issues:** (1) the baked default sits WRONG in the hand → Sponsor re-dialed via the F9 nudge tool to **`offsetFromHand=(0.003,-0.017,0.009)` `euler=(6.5,-187.5,93.3)`** (transcribed from screenshots, European-comma→dot; echoed to Sponsor for digit-check); (2) **BUG: the held axe points the WRONG WAY once MOVING. Sponsor CLARIFIED: "the axe moves up and down as the hand moves, but it points the same way on the x axis all the time"** → the axe FOLLOWS the hand's POSITION but its ROTATION is FIXED IN WORLD SPACE (does NOT track the hand bone's rotation) → on a turn it points wrong. Pure rotation-not-following bug — position-follow already works; the world-space-rotation posing (unity-conventions §FBX held-prop) is the culprit. (STUMP axe unaffected — planted in the block, not hand-parented.) **→ Devon soakfix8: make the held-axe pose HAND-BONE-RELATIVE (stable through Walk/Idle/turn/movement → fixes points-wrong-on-move) + bake the new dialed values as that stable pose + ensure the F9 nudge dials a STABLE pose (what-you-dial-is-what-you-get, static AND moving) + PlayMode regression test (held axe keeps hand-relative pose across a turn/move). Stump/hair/rocks UNTOUCHED (approved). + FIX3: the exe opens as a small ~1280×720 WINDOW on the Sponsor's widescreen → set Player Settings to BORDERLESS-FULLSCREEN at native res (KEEP the capture path windowed via its `-screen-fullscreen 0` CLI arg, so QA captures don't break).** **FUTURE (parked — Sponsor "maybe"): redo the CHARACTER via Hyper3D web-gen (his subscription, manual export) — Hyper3D output is UN-RIGGED (unity-conventions §Asset creation) → would need re-rigging; NOT now (flat hair acceptable).** On Devon done → Tess QA → re-soak the held axe (verify static AND MOVING) → on Sponsor APPROVE merge (supersedes #44) + #41/#42 → flip tickets → world-look-polish wave. Then Tess QA the combined (FRESH stamp-matched exe) → serve Sponsor re-soak. **NOTE Drew finding: `86ca8feuf` (beach wreckage) is ALREADY-SATISFIED by `ed8da36` (ancestor of `5f7e7ba`) — close it at merge, no new wreckage work.** Reviewer Tess (both). One Devon / one Drew (worktree-concurrency). main @ `d9dbf62`; 5 open PRs (#38–42) unchanged. auto-status OFF (Sponsor decision; Sponsor present this session, not re-enabling without ask). —— SUPERSEDED HISTORY (the prior fix-wave dispatch that died) BELOW ——
>
> **▶ LIVE — SUPERSEDED (prior session, agents since dead):** Sponsor returned → chose "Tess QA + fresh exe". **TESS DONE → APPROVE_WITH_NITS** on the combined build #40 @ `5f7e7ba` (review COMMENTED, verdict up front; agent `a189963e0bd82fe40` completed). **No blockers.** Both feature-sets GREEN TOGETHER: EditMode 130/130 + PlayMode 39/39; CI run `27492516490` both jobs success; build Succeeded size=99330764. Gray slab GONE, sea+debris intact, NO orange tuft (4 gameplay-orbit angles), exactly-one-axe swap, clothes separate from skin, nudge tool inert un-toggled — all verified multi-angle at gameplay scale. **SOAK ARTIFACT (SERVED TO SPONSOR):** exe `c:/Trunk/PRIVATE/Far-Horizon-tess-wt/Build/Windows/FarHorizon.exe`, baked stamp **`zoned | 2026-06-14T08:27:09Z | 5f7e7ba`** (grepped from `FarHorizon_Data/resources.assets`, == HEAD). **NIT (non-blocking, CAPTURE-ONLY):** `-verifyCastaway` ~10× hero close-up shows a floating `CastawayHair` seam (mesh seats slightly high/detached at magnification; INVISIBLE at gameplay scale + all `-verifyHair` 14u orbit angles + the soak orbit; EditMode `MessyHairCap` gates pass — they test mesh geometry, not head-bone placement). → **filed `86ca8m3t2`** (Devon, XS, low/"to do" — seat hair flush; capture-only, not gating the current soak). **SPONSOR SOAKED `5f7e7ba` 2026-06-14 (stamp confirmed via 2 annotated screenshots) → REJECT-WITH-FIXES (4 items):** (1) **HELD AXE not gripped at the HANDLE** — dangles blade-down beside/below the right hand, reads as floating-at-hip not hand-held (nudge panel showed target=HELD, offsetFromHand=(-0.016,0.082,0.043), euler≈(-20.3,-76.0,22.9)); (2) **HAIR issue STILL present** from the DEFAULT over-shoulder gameplay cam — Sponsor arrow to crown-top; **Tess's 4-angle `-verifyHair` APPROVE missed it AGAIN → recurring false-green** (Devon: reproduce at the EXACT screenshot view; hair reads as a brown helmet/seam, not messy hair); (3) **BEACH DEBRIS doesn't read as wreckage** — Sponsor: "what are these items sticking up from the ground" (dark abstract triangles, not washed-ashore planks/crate/driftwood); (4) **NUDGE TOOL UNUSABLE** — the AXE-NUDGE overlay covers the inventory hotbar AND its purpose was unclear to him ("i dont really know what is supposed to do"). **SPONSOR ANSWERS (2026-06-14 popup):** axe = **better-default + fix-tool** (Devon sets a proper handle-grip DEFAULT AND moves the nudge panel off the HUD + clearer); rocks = **beautify + thin** (override "remove the rocks" still OPEN — relay to Drew if Sponsor says so). Debris-question premise was CORRECTED by the Sponsor ("not only on the beach, they are all over") → the "all over" props are the scatter ROCKS, not the beach wreckage (orch trace → new ticket `86ca8m5zu`). **→ FIX WAVE DISPATCHED 2026-06-14 (2 agents, background, both branched off `5f7e7ba`):** **Devon `a141eb136a067f4ea`** → `devon/castaway-soakfix6` (FIX1 HAIR reads-as-helmet/seam from the default gameplay cam — false-green AGAIN, reproduce at the Sponsor's exact view + fold the close-up seam NIT `86ca8m3t2`; FIX2 held-axe handle-grip DEFAULT; FIX3 nudge-panel off the HUD + clearer; tickets `86ca8ce6y`+`86ca8ca1m`). **Drew `ac420481aa96d9c5a`** → `drew/scatter-rocks-wreckage` (FIX1 scatter rocks `FacetedSphere(0.6,0,0.30)` subdiv-0 spikes → round into boulders + warm + thin ~22; verify grass not dark-sharding; FIX2 beach wreckage reads as planks/crate/driftwood; tickets `86ca8m5zu`+`86ca8feuf`). Both BAKE Boot.unity → **orch RE-INTEGRATES (regenerate-on-rebase, `team/orchestrator/integration-39-38-playbook.md`) when both back+green** → Tess QA the combined (FRESH stamp-matched exe) → re-soak. Expected ~20–40 min each; staleness-probe by agentId if no `<task-notification>` by ~2×. Reviewer Tess (both). **One Devon / one Drew dispatch (worktree-concurrency).** **OLD:** re-soak verdict + the F9-nudge axe values (held + stump) → Devon bakes into constants → final build. **F9 nudge how-to** (relayed): Tab cycles held↔stump; arrows/PgUp/PgDn nudge pos; T/G·Y/H·U/J rotate; Shift=5×, Ctrl=0.2×; read the live `HeldAxeWorldOffsetFromHand`/`StumpAxeLocalPos`+Euler off the AXE-NUDGE panel → report → Devon bakes. **On Sponsor APPROVE → merge #40** (`gh pr merge 40 -R TSandvaer/Far-Horizon --admin --squash --delete-branch` — closes #38/#39 superseded) + #41 (DECISIONS, Drew-APPROVE) + #42 (Uma audit) — ALL Sponsor-gated → flip 86ca8ce6y/86ca8ca1m/86ca8fet0/86ca8feuf → complete → orch-docs PR.

**If this session dies right now, the next orchestrator does this:** **⚠ SOAK VERDICT IN (2026-06-14): rocks/hair/stump APPROVED — HELD AXE + FULLSCREEN → Devon soakfix8 ✅ DONE @ `cef8f56` (PUSHED) → **TESS APPROVE (`ac724d6a1f428ef07`) → SOAK SERVED to Sponsor 2026-06-14** (exe `c:/Trunk/PRIVATE/Far-Horizon-tess-wt/Build/Windows/FarHorizon.exe`, baked stamp `zoned | 2026-06-14T18:09:36Z | cef8f56`, ORCH-VERIFIED == HEAD; 147/147+44/44; axe rotation tracks-hand INVARIANT across 4 facings; capture still windowed 1280×720). Soak focus: FULLSCREEN-fills-his-widescreen + HELD-AXE-IN-MOTION (turn/move → does it stay gripped + turn WITH the body). Tess NIT (non-blocking): gameplay-scale axe seat ~0.53u is the Sponsor's F9-dialable call. **→ SPONSOR SOAKED `cef8f56` 2026-06-14 → FULLSCREEN ✅ (fills his widescreen — confirmed by a 3660×1246 screenshot); HELD-AXE DEFAULT ❌ "the axe is messed up, its like you dont use my axe nudging."** ROOT: the Sponsor's EARLIER dialed values were in the OLD world-space nudge tool; Devon's soakfix8 switched the axe to hand-local + CONVERTED those values → the conversion did NOT reproduce the dialed look. Devon ALSO made the nudge tool + bake BOTH hand-local (dial==baked, no conversion), so a FRESH dial on `cef8f56` will bake FAITHFULLY. **→ ASKED Sponsor to RE-DIAL the held axe via F9 on this build + report `offsetFromHand`+`euler` → then Devon bakes the EXACT values (one-line const, no conversion) → re-soak. Stump UNCHANGED (approved). AWAITING the Sponsor's freshly-dialed values — do NOT bake/dispatch until he reports them.** (Fullscreen + rotation-tracks-hand from soakfix8 are KEEPERS.) **→ SPONSOR RE-DIALED on `cef8f56` + FOUND A NUDGE-TOOL BUG (2026-06-14): one arrow-left click (`localPos.X` step 0.02) moves the held axe ~5 METERS left ("something is off with the nudging"). Screenshots: `localPos=(0,0.0001,0)` axe in-hand → `localPos=(-0.02,0.0001,0)` axe flew meters left; `euler=(4.1,95.8,-56.1)` stable.** ROOT: soakfix8 made the held-axe POSITION `localPosition` on `RightHand_010` (~267× lossyScale) → a 0.02 LOCAL step = ~5.3 WORLD units — the EXACT documented §FBX lossy-scale trap. Rotation hand-local is FINE (tracks turn); only POSITION is broken. **→ Devon soakfix9 ✅ DONE @ `5a1e77b` → TESS PASS (`a29ae9fae3da38308`: nudge step ~0.02u not 5m; rotation-tracks-hand 8 facings; fullscreen capture windowed; 147/147+46/46) → SOAK SERVED to Sponsor 2026-06-14 (exe `c:/Trunk/PRIVATE/Far-Horizon-tess-wt/Build/Windows/FarHorizon.exe`, stamp `zoned | 2026-06-14T19:23:36Z | 5a1e77b`, ORCH-VERIFIED == HEAD). Sponsor now dials the held-axe POSITION via the USABLE F9 nudge (~2cm/click) → reports values or approves. **→ SPONSOR NUDGED on `5a1e77b` 2026-06-14 (axe reads well-seated at his side) BUT the F9 PANEL BOX ITSELF CUTS OFF the euler's 3rd value** (Sponsor confirmed "its your nudge tool box that cuts off the text, this is the full window" — NOT a window/ultrawide issue; the panel box is too NARROW so the `euler` line overflows its right edge). Latest readable (still cut): `offsetFromHand=(0.003,-0.017,0.029)` `euler=(24.1,-12.2,?)`. He's still iterating (values change per shot) + can't read the full rotation to settle. **→ Devon soakfix10 ✅ DONE @ `9bee6b8` (PUSHED; 147/147+47/47, no Boot re-bake) → TESS APPROVE (`a2e779eb9a8cd0c29`) → SOAK SERVED 2026-06-14 (exe `c:/Trunk/PRIVATE/Far-Horizon-tess-wt/Build/Windows/FarHorizon.exe`, stamp `zoned | 2026-06-14T20:39:28Z | 9bee6b8`, ORCH-VERIFIED == HEAD; Sponsor dials axe on the readable panel + reports `offsetFromHand`+`euler` → Devon bakes → final). FIX WAS: F9 PANEL LAYOUT — put `offsetFromHand` + `euler` on SEPARATE lines + size the box wide enough so the FULL values are ALWAYS visible/copyable on any screen width (no cutoff). No nudge-behavior change. → Tess QA → re-soak → Sponsor dials to FINAL on the readable panel + reports complete values → Devon bakes (one-line const) → final soak.** POSITION → WORLD-offset from the hand (sensible nudge units ~0.02 world/click, NOT lossy-scaled `localPosition`) + KEEP rotation hand-relative + bake Sponsor's settled `euler=(4.1,95.8,-56.1)` + a sensible default world-offset position + PlayMode guard (one nudge click moves the axe ≤~0.05 world, not meters). Stump/hair/rocks/fullscreen UNTOUCHED.** → Tess QA → re-soak (Sponsor fine-tunes POSITION on the now-usable nudge tool). Devon FIX1: held-axe rotation now HAND-LOCAL (`localRotation` child of `RightHand_010`) — root cause was `axe.rotation = Euler(worldEuler)` re-applied every frame; proven `-verifyAxeFacings` localEuler INVARIANT across 0/90/180/270° while the head flips with the body. FIX2: new dialed grip baked hand-local. FIX3 fullscreen: Player-Setting was ALREADY correct — real cause was Unity persisting the capture-gate's 1280×720 to the HKCU registry → new gated `FullscreenBoot` runtime component forces borderless-native (`5120x1440`) on normal launch, inert under capture flags, capture gate re-verified still windowed. 147/147+44/44. Devon added 2 findings to unity-conventions.md ON HIS BRANCH (held-prop-hand-local + PlayerPrefs-res-trap) → RECONCILE with the orch's uncommitted unity-conventions.md edits at merge.** Sponsor soaked `808725a`: held axe (1) sits wrong at the baked default → re-dialed via F9 to `offsetFromHand=(0.003,-0.017,0.009)` `euler=(6.5,-187.5,93.3)`; (2) BUG — axe FOLLOWS hand POSITION but ROTATION is world-fixed ("points the same way on the x axis all the time") → doesn't track the hand bone on a turn; (3) exe opens as a small ~1280×720 WINDOW — wants FULLSCREEN on his widescreen. **Devon soakfix8 (off `808725a`, SINGLE-AUTHOR — no Drew/integration): held-axe ROTATION → track the hand bone (apply relative to RightHand_010's current rotation each frame; position-follow already works) + bake the new dialed values + F9-nudge consistency (dial==baked==in-motion) + PlayMode multi-facing turn/move regression test; + Player Settings → borderless-fullscreen native res (KEEP capture windowed via `-screen-fullscreen 0` CLI); stump/hair/rocks UNTOUCHED.** On resume: **Devon soakfix10 + TESS APPROVE → SOAK SERVED (`9bee6b8`)** — AWAITING Sponsor's dialed axe values (exe `c:/Trunk/PRIVATE/Far-Horizon-tess-wt/Build/Windows/FarHorizon.exe`, stamp `zoned | 2026-06-14T20:39:28Z | 9bee6b8`, orch-verified; F9 panel now readable — values on 2 lines). Sponsor dials held-axe FINAL on the readable panel + reports `offsetFromHand`+`euler` (OR "approve" to keep Devon's default) → Devon bakes (one-line const) → final soak. **⚠ STALE-BUILD MIX-UP (2026-06-14): Sponsor ran the STALE devon-wt build (`5a1e77b`, 19:14:58Z — matched his screenshot HUD) instead of the panel-fix tess-wt soak (`9bee6b8`) → "still cut off." Re-pointed him to the TESS-WT exe. The stamp ritual caught it. Stale FarHorizon builds exist across worktrees: devon-wt=`5a1e77b`, drew-wt=`47f8330`, main=`28d9de7` (Jun-12) — CANONICAL SOAK PATH = tess-wt only; offered to delete the stale ones (gitignored throwaway) to keep the soak unambiguous.** → **Sponsor dials the held-axe to FINAL on the readable panel + reports the complete `offsetFromHand`+`euler` → Devon bakes (one-line const) → final soak.** (Soak base build `5a1e77b` exe still at `c:/Trunk/PRIVATE/Far-Horizon-tess-wt/Build/Windows/FarHorizon.exe`, stamp `5a1e77b`.) **On his APPROVE → open a PR for the final Devon soakfix9 branch (`devon/soakfix9-axe-pos` @ `5a1e77b`)→main, get CI green, then merge** (Sponsor-gated; supersedes #44/#43/#40/#39/#38) + #41 (re-run its FAILED CI first) + #42 (green) → flip tickets 86ca8ca1m/86ca8ce6y/86ca8m5zu/86ca8m3t2/86ca8feuf complete → **RECONCILE Devon's branch unity-conventions.md edits (held-prop SPLIT: position=world-offset / rotation=hand-local; PlayerPrefs-res-trap) with the orch's uncommitted doc edits** → orch-docs PR → world-look-polish wave (Sponsor-confirmed next focus). If Sponsor reports new dialed values → Devon bakes (one-line const) → re-soak. **On Sponsor APPROVE → open a PR for the final Devon soakfix9 branch→main, get CI green, then merge** (Sponsor-gated; supersedes #44 → closes #43/#40/#39/#38) + #41 (re-run its FAILED CI first) + #42 (green) → flip tickets 86ca8ca1m/86ca8ce6y/86ca8m5zu/86ca8m3t2/86ca8feuf complete → orch-docs PR (unity-conventions captures: bootstrap-restore-reverts-.cs / guard-the-PERCEPT / explicit-normal-winding / quantizer-pink / + held-prop-rotation-must-track-bone). **ACTIVE — HYPER3D CHARACTER SPIKE (Sponsor chose the de-risked spike 2026-06-14):** Sponsor generates a chunky low-poly castaway on hyper3d.ai (Rodin; Creator-tier web-gen + "Unlimited Export"; NO Business-tier MCP API) **IN A T-POSE/A-POSE (critical for auto-rig — arms clear of the torso)**, image-to-3D w/ a chibi/inspiration ref, exports FBX → drops it in `inspiration/hyper3d-castaway/` (or paths it). THEN orch dispatches an AUTO-RIG SPIKE (Mixamo auto-rig + Idle/Walk attempt) → judge RIGGABILITY before committing to a character swap (Hyper3D output is UN-RIGGED — the spike tests whether it animates cleanly). **AWAITING the Sponsor's generated FBX.** Separate track from the axe; no rush. Guidance given to Sponsor 2026-06-14 (he opened hyper3d.ai Rodin Gen-2.5; 35 credits). **UI findings to leverage:** (1) Rodin has an **"Avatar" tab** distinct from "3D" — IF it outputs a RIGGED character it SKIPS the auto-rig spike entirely (told Sponsor to check it first); (2) **DCC Bridge** offers Blender + Unity plugins (could pipe the asset straight into Blender for rigging — vs the FBX-export route); (3) gen modes: **Turbo Gen** (cheap, for iterating) → final at **Gen-2.5/High**; (4) `developer.hyper3d.ai/get-started` = the programmatic API — VERIFIED by orch WebFetch 2026-06-14 (explicitly "You must have a Business subscription to request this API") → NOT his Creator plan; web UI is the route. (That API page does NOT cover export formats / rigging / DCC bridge — those are other doc sections.) **AVATAR-TAB CHECKED (Sponsor screenshot 2026-06-14): it's "ChatAvatar" = animatable FACES/HEADS ONLY + REALISTIC style — NOT a full-body character + wrong aesthetic → DEAD END for our toy castaway.** → Spike uses the **3D tab**: Image-to-3D w/ chibi ref + prompt (chunky low-poly toy castaway, T-pose arms-out) → Turbo to iterate, Gen-2.5/High final → export FBX to `inspiration/hyper3d-castaway/` → Mixamo auto-rig spike (output is UN-RIGGED). AWAITING the Sponsor's FBX. **NEXT WAVE — Sponsor-CONFIRMED 2026-06-14 (walkthrough popup): WORLD-LOOK POLISH = clouds + far-horizon vista + sky-tint (Uma #42 backlog)** — serves the BIG/endless north-star + the "finish the world first" priority; CHOSEN OVER starting the next gameplay/survival wave. Dispatches once the soak lands + #44 merges (Uma direction brief first → Drew impl). [Persist to DECISIONS.md via Priya's next catch-up.] auto-status AWAY (cron `7dc6a776`). Sponsor soaked `30ef320` → APPROVE-IN-PART: axe PERFECT (values dialed-in, baking), nudge OK, sea/debris/gray-slab/clothes OK — 2 rejects only (hair tufts→flat, rocks still-not-rocks+too-few). On resume: PROBE both agentIds + `git ls-remote`/`gh pr list` (prior fix waves have died with nothing pushed — verify). Both back+green → RE-INTEGRATE (playbook: base-off-Devon + merge Drew → new integ branch off `30ef320`) → Tess QA → serve Sponsor re-soak. **Axe values to bake (if a re-dispatch is needed): HELD `offsetFromHand=(-0.018,-0.038,-0.021)` `euler=(0.0,9.9,124.7)`; STUMP `localPos=(-0.210,1.540,0.430)` `euler=(12.0,53.0,48.0)`.** On final Sponsor APPROVE → merge the final integ (Sponsor-gated) → close #40/#39/#38 + #43 superseded + #41 (DECISIONS) + #42 (Uma audit) → flip tickets 86ca8ce6y/86ca8ca1m/86ca8m3t2/86ca8m5zu/86ca8feuf complete → orch-docs PR (incl. this session's unity-conventions.md captures). On Sponsor REJECT → trace → re-dispatch flagged fix. DEFERRED polish (Uma #42): clouds/far-horizon-vista/sky-tint. **auto-status AWAY (cron `7dc6a776`, every 15m, session-only; re-armed on restart via the auto-status SessionStart hook + `.claude/auto-status.state` mode=away) — the away tick drives fix-wave→re-integrate→Tess QA→QUEUE-soak-for-Sponsor; it does NOT merge to main or decide the soak (Sponsor sign-off).** [HISTORY — stale: DRAIN COMPLETE 2026-06-14.]** **RE-INTEGRATION DONE → PR #40 @ `5f7e7ba`** (CI green run 27492516490; both feature-sets' scene-presence tests GREEN TOGETHER: EditMode 130/130 + PlayMode 39/39 — incl. NEW `MessyHairCap_FrontFringe_SitsBelowCrown` + `AxeNudgeTool` + `TestGround_IsCollisionProxyOnly` + `WaterFacesUpTests` + `BeachDebrisSceneTests`; `MovementCameraScene.cs` auto-merged clean; 1st CI = transient runner EPERM, clean re-run green). **COMBINED BUILD READY** (Drew's intermediate exe `c:/Trunk/PRIVATE/Far-Horizon-drew-wt/Build/Windows/FarHorizon.exe` stamp `89ae970` = build-before-the-artifact-commit; integ HEAD is `5f7e7ba`). **▶ NEXT SESSION (in order):** (1) Tess QA the combined build — **checkout integ `5f7e7ba` DETACHED (worktree-lock)**; acceptance plan ready `team/tess-qa/combined-resoak-acceptance.md`; the soak exe is built FRESH at QA (stamp == the QA HEAD, not 89ae970). (2) Serve the Sponsor re-soak: exe + stamp + checklist + **the F9 nudge tool** to dial the held + stump axes (Sponsor nudges → reports values → Devon BAKES them into the constants → final). (3) On Sponsor APPROVE → merge #40 (closes #38/#39) + #41 (DECISIONS, Drew-APPROVE) + #42 (Uma audit) — ALL Sponsor-gated/classifier-blocked → flip tickets 86ca8ce6y/86ca8ca1m/86ca8fet0/86ca8feuf → complete + orch-docs PR. (4) DEFERRED polish (Uma #42, Sponsor "soak fixes clean first"): clouds + far-horizon vista + sky-tint. (5) SURFACED strategic: next-wave scope (world-expand / M-U3 survival / polish). [No new dispatches this session — Sponsor drained + saved.] **5 open PRs all SPONSOR-GATED → next session:** Tess QA the combined build (checkout integ SHA DETACHED — worktree-lock) → serve the re-soak (Sponsor dials axes via the F9 nudge tool → reports values → bake) → on Sponsor APPROVE merge #40 (closes #38/#39) + #41 (DECISIONS, Drew-APPROVE) + #42 (Uma audit, deferred-polish) → flip tickets. DEFERRED polish (Uma #42): clouds + far-horizon vista + sky-tint (Sponsor: "soak fixes clean first"). AUTO-STATUS OFF (cron `03029456` cancelled, enabled=false). main @ `d9dbf62`. **SPONSOR SOAKED #40 (combined, 31ce95c) 2026-06-14 → APPROVE-IN-PART + fixes.** WINS (locked): sea color LIKED (polish later), axe-head color LOVED ("genuinely looks like an axe"), held-axe HEIGHT / no-axe / craft OK. **FIXES IN FLIGHT:** **Devon #39 (agent `a778ad8edb4fe248b`):** (1) STUMP-AXE bite cleaner into the block; (2) HELD-AXE was FLOATING beside the hand with a gap → pose the handle IN the hand (close gap, track Idle/Walk); (3) HAIR orange FRONT-TUFT (4th attempt — a small ginger cowlick still pokes above the now-flat crown at the FRONT, slips the guard); (4) CLOTHES weathering too SUBTLE ("seems unchanged") → make it READ; **(5) NUDGE TOOL — Sponsor CHOSE an in-game build-gated debug tool** to dial held+stump axe placement himself (select prop / nudge pos+rot / read values on HUD → bake), so the axes get a sane DEFAULT + the tool (don't over-iterate exact transforms). **Drew #38 DONE → `f455853`:** the GRAY SLAB = the flat-Y0 `TestGround` slab (moss-grey `(0.42,0.46,0.40)`, the NavMesh/click-move COLLISION PROXY from `MovementCameraScene.BuildFlatGround`) poking ABOVE the dipping Zone-D sand on the seaward foreshore (inland the sand rises above Y0 + hid it → why it only showed on the beach). FIX: kept the collider (NavMesh/click-move BIT-IDENTICAL, zero regression) + **disabled its MeshRenderer** (renderer kept-but-disabled so `.bounds` still resolves for the occlusion test) → only the sand draws. Clean sand→teal sea, sea+debris intact (grey pixel match 0.01%=AA), -verifyMove NavMesh OK; 123/123 + 35/35, CI run 27491059444 green; new guard `TestGround_IsCollisionProxyOnly_RendererDisabled_NoGreySlab`. **PARKED for the combined re-integration QA** (with Devon #39). DOC-CANDIDATE: TestGround = collision/NavMesh proxy ONLY → renderer must stay DISABLED (it pokes above the dipping beach terrain as a grey slab); fold into the real terrain when U5 replaces the env surface. **#40 HOLDS (NOT approved) — being REGENERATED.** Devon #39 **soakfix5 DONE** (`2ca21a2`, CI green run 27491455655, 123/123+38/38, all 5 items): **HAIR — the "orange tuft" was NOT a crown spike** (crown plateau is genuinely flat, 32 verts on one ceiling) — it was the **FRONT FRINGE jutting FORWARD** (frontmost z=−1.216, ~0.22u past radius) projecting above the brow + catching the key light from the over-shoulder cam; fixed (front fringe no outward-jut/forward-wobble + height-graded pull-down → 0.07u below crown), NEW guard `MessyHairCap_FrontFringe_SitsBelowCrown_NoForwardTuft` (the class the plateau guard was blind to); + bold clothes weathering (reads at distance); + held-axe gap closed + stump-axe re-centered (SANE DEFAULTS — Sponsor finalizes via tool); + build-gated **AxeNudgeTool** (F9, inert in normal play). Drew #38 gray-slab DONE (`f455853`, Tess code-review LOGIC SOUND). **→ DREW RE-INTEGRATING** (agent `af07cbfd6914ecf0c`): integ off `2ca21a2` + merge `f455853` → regenerate Boot.unity (Drew pre-flighted via merge-tree: `MovementCameraScene.cs` AUTO-MERGES CLEAN, only Boot.unity+BuildStamp regenerate). **Then Tess QA the combined** (acceptance plan READY `team/tess-qa/combined-resoak-acceptance.md`; **checkout the integ SHA DETACHED, not the branch — drew-wt holds it (worktree branch-lock)**) → re-soak (Sponsor dials axes via the nudge tool → reports values → bake → final). **WHIP results:** Priya DECISIONS catch-up → **#41 Drew-APPROVE** (7 entries, MERGEABLE, Sponsor-gated merge); Uma pre-soak audit → **#42** (scene substantially ON-BOARD, no manufactured issues; polish — clouds/far-horizon-vista/sky-tint — **DEFERRED to a follow-up pass per Sponsor "soak the fixes clean first" 2026-06-14**; the axe-restyle flag is MOOT — Sponsor loves the rustic slate axe). **DOC-CANDIDATES (maintain-docs):** (a) a FLAT crown can still project a tuft via a forward-jutting FRONT FRINGE — world-Y top-vert guards are blind to it (Devon hair); (b) can't `git checkout <branch>` in worktree A if worktree B holds it → checkout the SHA DETACHED for cross-persona PR review (Tess #38-review incident). **DECISIONS (Sponsor 2026-06-14):** axe-tweak mechanism = NUDGE TOOL; sea-color ACCEPTED (polish deferred); axe-head ACCEPTED (slate steel — barn-red idea dropped); auto-status OFF (cron 03029456 cancelled). [PRIOR INTEGRATION STATE:] PR **#40** (`integ/castaway-axe-ocean-beach` @ `31ce95c`) = the combined Boot.unity re-bake, **Tess APPROVE**, CI green (27480153919), both feature-sets' scene-presence tests together (127/127 + 38/38). exe `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe`, stamp `31ce95c`. **ON SPONSOR RETURN:** (1) SERVE the combined soak — path + stamp + checklist (no-axe / stump-axe-in-block / held-axe-at-side / weathered clothes / hair-NO-spike-from-over-shoulder / NOW-VISIBLE teal sea / beach debris); (2) he judges + the soak-NITs (sea SATURATION sky-cyan→deepen? / clothes-tatter degree / axe-head slate→barn-red?); (3) on his APPROVE → **merge #40** (`gh pr merge 40 -R TSandvaer/Far-Horizon --admin --squash --delete-branch` — Sponsor-gated/classifier-blocked, needs explicit go) → close #38/#39 superseded → flip 86ca8ce6y/86ca8ca1m/86ca8fet0/86ca8feuf → complete → fold uncommitted orch-docs into a Sponsor-gated orch-docs PR. **No autonomous team action remains until the Sponsor decides.** —— HISTORY TRAIL BELOW —— **MERGE BATCH COMPLETE — 0 open PRs:** #36 (soak-fix integration) MERGED ✅; #34/#35 CLOSED superseded ✅; #37 (verify-capture hardening, Tess APPROVE — determinism byte-identical, false-green-class fix verified) MERGED → `24cb040` ✅; #33 (DECISIONS, Drew APPROVE) MERGED → `d9dbf62` ✅. **FIX WAVE DISPATCHED 2026-06-13 (2 agents in flight, branched off main `d9dbf62`, background):**
- **Devon DONE → PR #39** (`devon/castaway-axe-soakfix2`, HEAD `adc9b0f`, CI green run 27472412781; agent ae4fb008d4d6827a3). **NO-AXE FIXED** — root cause (trace, not either ticket hypothesis): the held axe was a **0.43u sliver blade-down at the hip (~3.7% of the 14u/55° orbit frame = invisible)**; `-verifyAxe` close-up is zoom-to-fit → FALSE-GREEN (passes at any prop size). Fix: pose in WORLD space after parenting, scale ×267→~1.0u seated at chest, blade flat → ~8.6% frame; NEW `StumpAxe` (inverse-HasAxe gate) plants the hatchet upright in the block VISIBLE FROM SPAWN, hides on craft as held axe appears. + olive-khaki shirt (separates from skin), `MessyHairCap` (no spike). 118/118 EditMode + 37/37 PlayMode, NEW `CraftToVisibleAxePlayModeTests` gameplay-path craft→visible-axe guard. Gameplay-view evidence: craft_after/axe_closeup/craft_before PNGs. **TESS QA #39 = REQUEST_CHANGES** (a8deff74bab0c7a44): **NO-AXE FIX VERIFIED IN GAMEPLAY ✅** (axe in-hand believable size via -verifyCraft on shipped exe real OrbitCam; stump-axe-from-spawn + craft-swap; only-one-axe invariant; shirt olive-khaki; 118/118+37/37; CI green) — BUT **ONE BLOCKER: hair brown spike STILL present** (top vert 0.138u / relGap 0.121 above the top-15 crown ring; Sponsor's exact tilt-to-horizon symptom; no test asserted the silhouette → went green). Non-blocking NIT (soak-gated): axe head reads slate/steel not barn-red (sourced-hatchet look, Sponsor judges). **→ DEVON HAIR-FIX DONE → #39 @ `5f4d6cc`** (a8ea37d162273c264): spike was a DOME-WIDE CONE (not a single apex pole — trace overturned the hypothesis: spread 0.1057u but apexGap only 0.0089u) → fix = CROWN-PLATEAU soft-clamp (every vert above a world-y ceiling pulled toward it, 0.30 residual) → spread 0.0299u (< Tess's 0.05u bar), apexGap 0.0020u. Shipped `-verifyHair` tilt-to-horizon (pitch20/dist14u, FIXED-ORBIT) stamp 5f4d6cc==HEAD: rounded dome, no spike. NEW guards `MessyHairCap_CrownIsFlat_NoProudApexSpike` + `BootScene_CarriesHairVerifyCapture_Serialized`. 120/120+37/37, CI run 27475936503 GREEN. Devon's `gh pr comment` was classifier-denied → **ORCH POSTED the hair-repass Self-Test Report** to #39 (issuecomment-4699496281, content = Devon's verified evidence; the saved file pr39-selftest-soakfix3.md was STALE @ adc9b0f, not reused verbatim). **→ TESS RE-VERIFY = APPROVE** (ab234421de614c1b2): hair spike CLEARED — geometry probe spread 0.0299u (<0.05u) / apexGap 0.0020u (<0.02u), ~64× reduction vs the 0.138u blocker; shipped `-verifyHair` tilt-to-horizon (pitch20/dist14u) rounded dome no spike; both new guards green; CI 27475936503 green. **PR #39 FULLY GATE-CLEARED (Tess APPROVE on the complete castaway+axe+hair fix).** Non-blocking soak-gated NIT carried: axe head slate/steel vs barn-red (sourced-hatchet look). **#39 PARKED — does NOT merge standalone:** it bakes Boot.unity (conflicts with Drew's ocean PR) AND the Sponsor judges the COMBINED fix in the FRESH SOAK before any merge. tickets 86ca8ce6y+86ca8ca1m stay in progress until the post-soak merge. **SEA = DEEPER BUG (Drew's comparison aceade72d2b29e81a OVERTURNED the framing premise):** ran magenta-diff on 6 builds (baseline + both framing variants + reveal probes) → **water pixels = 0 in EVERY build.** The "sea" everyone saw (incl. Tess's #36 "teal sea" sample + the Sponsor's grey-pond/sky-cyan) is FOG/SKY + shadowed ground — the water mesh (Y−0.20, entirely Z≤−10.5) sits BELOW an opaque horizon (spawn beach + flat TestGround slab) → depth-occluded across the whole seaward frame; dead below-sea sand band Z−1…−10.5 with no water over it. Moving spawn / lowering beach / trimming TestGround / re-pitching cam ALL still gave 0 water px. **Framing A/B was MOOT (neither showed water).** → **DREW SEA-RENDER FIX DONE → #38 @ `24bb62e`** (a42e863f4ae82dd9f, CI run 27477452386 GREEN): **ROOT CAUSE WAS NOT OCCLUSION — IT WAS BACKFACE CULLING.** The water grid lays near→far in DECREASING world Z (opposite the terrain); reusing the terrain's triangle index order wound the water faces DOWN → −Y normals → default `Cull Back` culled the sea from the above-looking gameplay cam. Fix = REVERSE the water triangle winding (`LowPolyZoneGen.BuildWaterEdge`) — the geometry chases (slope/deepen/overlap) were REVERTED as wrong-cause. `-seaWaterOnly` probe (hide both grounds → 0 sea px) disproved the occlusion premise. Magenta-diff **0 → 55,103 px (5.98% frame, clear teal band), N=8 deterministic**. Guard `WaterFacesUpTests` (asserts every Water_Play normal +Y); `count_sea_pixels.py` promoted as the magenta-diff tool. EditMode 122/122 + PlayMode 35/35. Evidence: `ci-out/final/sea/sea_seaward.png`. NON-BLOCKING follow-up: now the sea renders, the teal reads bright/sky-cyan → Sponsor-soak SATURATION call (the Sponsor's original "too sky-cyan" tweak is now a REAL water-color call, not fog; Drew didn't re-tune to avoid a color-chase). DOC CANDIDATE: −Z procedural grid reusing +Z terrain winding → inverted normals → backface-culled/invisible; detect via magenta-diff + `-seaWaterOnly` probe; guard = assert +Y normals. → **TESS QA #38 = APPROVE** (a45e3a2a55bc69e23): independent magenta-diff on a clean 24bb62e build = **55,110 water px / 5.98%** (coherent full-width band, confirmed WATER MESH not fog); `WaterFacesUpTests` green; debris no-collider/non-blocking guarded; 122/122+35/35; CI 27477452386 green. Non-blocking sky-cyan saturation = Sponsor-soak call. **#38 FULLY GATE-CLEARED.** **BOTH #39 + #38 GATE-CLEARED + Sponsor-gated. #38 CLEAN/MERGEABLE.** NEXT: on Sponsor #39 soak APPROVE → integrate #39 + #38 (regen Boot.unity ONCE, #32/#36 pattern) → 2nd soak (all fixes + visible teal sea + debris; Sponsor judges sea SATURATION) → Sponsor-gated merge. **INTEGRATION PLAYBOOK READY** → `team/orchestrator/integration-39-38-playbook.md` (validated by workflow run `wf_d63e952a-804`; verdict **clean-integration-expected**). Key: base on Devon #39, merge Drew #38; ONLY 3 files overlap — `MovementCameraScene.cs` AUTO-MERGES CLEAN (0 markers, verified via merge-tree; Devon's craft/axe/hair + Drew's BuildBeachDebris are >160 lines apart), `Boot.unity` + `BuildStamp.txt` REGENERATE (never text-merge). ALL scene-gen methods on both branches confirmed WIRED into BootstrapProject.Run → nothing silently drops. **MANDATORY: after `--theirs` on the binaries, RE-RUN serve_soak (BootstrapProject.Run) ONCE to re-bake Boot.unity with BOTH features; gate with both features' scene-presence EditMode tests green TOGETHER (half-baked-scene gate).** Also CORRECTED a stale doc: unity-conventions.md "WorldBootstrap NOT in shipped scene" is wrong — Run calls WorldBootstrap.BuildEnvironment() (BootstrapProject.cs:62) + re-saves Boot.unity (the ocean ships via this path).
**SPONSOR CHOSE "SOAK THE VERIFIED FIXES NOW" (2026-06-13)** → soak #39 (castaway/axe/hair, Tess APPROVE) ALONE; debris (#38) + the fixed sea ride the SECOND soak. **#39 SOAK SERVED TO SPONSOR (2026-06-13)** — exe `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe`, HUD stamp VERIFIED `zoned | 2026-06-13T19:31:28Z | 5f4d6cc` (Devon a645ef1262993ab88 built+stamp-verified from the baked resources.assets string, 6/6 frames). Handed with explicit checklist (no-axe/stump-axe/shirt/hair; sea + debris EXCLUDED this build). **SPONSOR #39 SOAK VERDICT = REJECT-WITH-FIXES (2026-06-13, stamp 5f4d6cc confirmed via screenshots):** **NO-AXE GENUINELY FIXED ✅** (kid HOLDS the axe + "axe 1" HUD + stump-axe present from spawn — the 3× complaint is SOLVED). 4 FIXES → Devon: (1) STUMP-AXE sunk into the block (only the handle pokes out, head buried) → raise so the head reads (axe-stuck-in-chopping-block); (2) HELD AXE sits too HIGH — handle sticks out by the EAR (chibi huge-head + short-arm + chest-seat) → lower/reposition to the hand at his SIDE, handle clear of the head (maybe shrink/re-angle); (3) SHIRT + PANTS too clean → make them a bit TORN/tattered/weathered (castaway fiction; "a bit", tasteful); (4) HAIR STILL HAS A SPIKE on the crown-top — VISIBLE from the DEFAULT over-shoulder/top-down gameplay cam (Sponsor screenshot 1), which the front-tilt -verifyHair (pitch 20) MISSED → **false-green-capture AGAIN: the capture angle ≠ the player's actual view; the spike must be judged from the DEFAULT GAMEPLAY CAMERA ANGLE (over-shoulder/top-down) + multi-angle, not front-tilt.** **#39 REJECT → DEVON SOAKFIX4 DONE → #39 @ `20b7f0c`** (af67324e1f81d302c; CI run 27479406873 GREEN, 121 EditMode + 37 PlayMode, Self-Test posted): (1) stump-axe head now bites the block-top 0.700 (was buried at −0.266); (2) held-axe dropped to top 0.727 — clears the 0.775 head-bone (off the ear), blade-down-at-side, goldilocks guard [0.45,0.78]; (3) clothes weathered, **PANTS KEPT BLUE** (cell on Object_44 — a 6-SMR trace correction after a single-SMR pass wrongly weathered skin cells); (4) HAIR hard-clamped crown (zeroed top-crown jitter, apexGap<0.01u + ≥4 plateau verts) + `-verifyHair` re-aimed to over-shoulder/top-down/behind/front (hair_overshoulder/hair_topdown shipped show a clean dome). New guards: crown-flat-plateau, clothes-tatter, held-axe-goldilocks. **→ TESS RE-QA #39 SOAKFIX4 = APPROVE** (a9c0158b710f39354): all 4 verified on her own multi-angle shipped captures — **HAIR SPIKE GONE** from the over-shoulder 55° + top-down 70° gameplay angles (the false-green angle-trap closed; she re-ran -verifyHair at the real OrbitCam pitch); stump-axe stuck-in-block; held-axe at the side clear of the ear; pants BLUE + weathered, skin/feet/hair untouched. Guards green (crown-flat-plateau / clothes-tatter / held-axe-goldilocks), 121/121 + 37/37, CI 27479406873 green. Non-blocking soak-NITs: tatter subtle at close-up; axe-head slate-not-barn-red. **#39 GATE-CLEARED.** **→ INTEGRATION DONE → PR #40** (`integ/castaway-axe-ocean-beach` @ `31ce95c`, agent a58767c1ee3a19c90): merged #39+#38, regenerated Boot.unity ONCE; **BOTH feature-sets' scene-presence tests passed TOGETHER** on the one re-baked scene (WaterFacesUp + HeroAxe held/stump + crown-plateau + BeachDebris; EditMode 127/127, PlayMode 38/38 — half-baked-scene gate satisfied). Conflicts exactly as the playbook predicted (only Boot.unity + BuildStamp binary → regenerated; MovementCameraScene.cs auto-merged clean, no orphaned MakeAxeMat). **CI run 27480153919 GREEN** on 31ce95c (attempt 1 = environmental EPERM, cleared by re-run). **COMBINED SOAK STAGED (queued, not served):** `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe`, HUD stamp `zoned | 2026-06-13T21:49:20Z | 31ce95c` (baked-stamp verified == integ HEAD). **→ TESS QA #40 = APPROVE** (a745f5c7955845f33): combined gameplay-orbit view (`sea_seaward.png`, stamp 31ce95c) shows **character + teal ocean + beach debris ALL TOGETHER in one frame** — castaway right (no crown spike, olive shirt/dark pants), ocean teal (winding fix survived the re-bake), debris at the waterline; `chop_after.png` confirms the full loop end-to-end (HUD "axe 1 wood 1", -verifyChop exit 0, NavMesh/click-move intact, pathed past debris, craft→pickup swap fired). **Nothing dropped in the re-bake.** Scene-presence tests green TOGETHER (127/127 + 38/38), CI 27480153919 green. Non-blocking soak-NITs unchanged (sea sky-cyan / tatter subtle / axe-head slate). **#40 FULLY GATE-CLEARED (Tess APPROVE). TEAM IDLE-PENDING-SPONSOR — everything staged + queued; main merge of #40 stays Sponsor-gated.**
> **⏳ QUEUED FOR SPONSOR ON RETURN (do NOT auto-decide):** (a) **COMBINED RE-SOAK — READY NOW:** exe `c:/Trunk/PRIVATE/Far-Horizon-devon-wt/Build/Windows/FarHorizon.exe`, stamp `31ce95c` (PR #40); hand with the checklist (no-axe/stump-axe/held-axe-at-side/clothes/hair-no-spike + the NOW-VISIBLE teal sea + beach debris). **[Tess APPROVED #40 — this exe IS the soak artifact, QA-confirmed.]** On Sponsor APPROVE → merge #40 (`gh pr merge 40 -R TSandvaer/Far-Horizon --admin --squash --delete-branch` — Sponsor-gated/classifier-blocked) → close #38/#39 superseded → flip 86ca8ce6y/86ca8ca1m/86ca8fet0/86ca8feuf → complete → fold uncommitted orch-docs into a Sponsor-gated orch-docs PR. (b) **SEA SATURATION call** — the sea now RENDERS but reads bright/sky-cyan; the Sponsor judges whether to deepen it (Drew's open tweak, not re-tuned to avoid a color-chase). (c) **MAIN MERGE** of the integration PR (classifier-gated — needs the Sponsor's explicit go). (d) Soak-NITs to optionally fold: clothes-tatter degree, axe-head barn-red. **DOC-CANDIDATE (maintain-docs):** the chibi is SIX skinned objects sharing one atlas + degenerate skin data (all verts→Head_05, collapsed EditMode BakeMesh) → atlas-cell→body-region mapping must enumerate ALL SMRs + read per-object UVs, NOT a single FindAnyObjectByType SMR + Y-band/bone-weight split (Devon's SOAKFIX4 blue-pants miss). **On Tess APPROVE → #39 gate-cleared → EXECUTE the integration playbook** (team/orchestrator/integration-39-38-playbook.md) → combined build → QUEUE the combined re-soak for the Sponsor. INTEGRATION ON HOLD until #39 clears. **Drew sea-render DONE → #38** (Tess APPROVE, parked for integration). (GOTCHA logged to unity-conventions: the top-level FarHorizon.exe STUB mtime stays stale (Jun-12); only `FarHorizon_Data/*` rebuilds — judge freshness by the baked stamp, not the exe-stub mtime.) **GOTCHA found:** the `Build/Windows/FarHorizon.exe` in BOTH devon-wt + tess-wt is STALE (stamped adc9b0f / Jun-12) — the 5f4d6cc `-verify*` builds wrote captures to `ci-out/`, NOT a soak-ready exe; a FRESH serve_soak build is required + stamp-verified before hand-off. On Devon's build done → verify stamp==5f4d6cc → hand Sponsor exe path + stamp + explicit checklist (no-axe/stump-axe/shirt/hair; NOTE sea-not-visible + no-debris this build) → **SendMessage Drew "resume runner".** THEN: Drew sea-render fix → Tess QA #38(debris+sea) → integrate #39 + #38 (regen Boot.unity once, #32/#36) → SECOND soak → Sponsor-gated merge. **DECISION DRAFT (Devon):** "is X visible in gameplay" gates need a FIXED-ORBIT-framing capture, not a subject-fit/zoom-to-fit close-up (passes at any size) — sibling of false-green-capture + Drew's magenta-trace.
- **Drew DONE → PR #38** (`drew/ocean-beach-soakfix2`, HEAD `ed8da36`, CI run 27472393537 in progress; agent ab3fe43354c9630a2). **AC2 BEACH DEBRIS (86ca8feuf) DELIVERED** — washed-ashore planks/crate/rope, visible in shipped exe, 120/120 EditMode + 35/35 PlayMode, new BeachDebrisSceneTests/PlayModeTests (no-collider/non-blocking/warm-brown/seaward-of-spawn). **AC1 SEA NEAR-BAND (86ca8fet0) REFUTED BY TRACE** — Drew forced the water material to MAGENTA, rebuilt, cross-diffed magenta-vs-teal builds → only 248/921600 px differ (0.027%), pale "sea" strip byte-identical between builds → **the pale band the Sponsor soaked is SKY/FOG HAZE, not the water mesh; the water is OCCLUDED by raised foreground terrain (~Y+2 vs water Y-0.20) at the gameplay seaward pitch (~0 visible water pixels).** So "too sky-cyan" is NOT a color bug — the water is INVISIBLE. Same root cause as soak-1's "grey pond" + masks the #36 "teal sea" sample (that was fog). Water albedo deepened anyway (ready for when it's visible). **REAL FIX = the 86ca8fet0 framing/occlusion decision (pull-shore+de-occlude vs move-spawn-seaward).** **SPONSOR CHOSE "SHOW ME BOTH FIRST" (2026-06-13)** → **Drew RENDERING 2 framing comparisons** (agent `aceade72d2b29e81a`, off #38 branch, scratch — PROTOTYPE captures, #38 untouched): Variant A lower-beach+pull-shore (spawn locked) vs Variant B move-spawn-seaward. On Drew's 2 PNGs back → show Sponsor → he picks → Drew implements the chosen framing → integrate with Devon #39 → Tess QA integration → Sponsor merge → fresh soak. Reviewer Tess. DOC CANDIDATE: magenta-cross-build pixel-diff trace to prove whether a mesh actually contributes visible pixels (vs fog/sky masquerading) — the "is what I'm seeing actually the asset?" test, sibling of false-green-capture.

**BOTH bake Boot.unity → ORCH INTEGRATES + regenerates once (#32/#36 pattern) when both PRs are back+green.** Flow on completion: integrate both → CI green → Tess QA the integration → **Sponsor-gated merge** → **FRESH combined soak** off the fix-wave main (path+stamp+explicit checklist per `memory/soak-handoff-path-and-explicit-test-checklist.md`). 86ca8fevz → COMPLETE (#37 merged, Tess APPROVE). Sponsor re-soak verdict on `806592d` = **APPROVE-WITH-TWEAKS** (the 5 soak-fixes all landed). Note: 86ca8fet0 in-progress = the near-band tweak; its ORIGINAL shore-framing a/b decision stays DEFERRED (Sponsor did NOT flag shore-framing — camera-pitch fix satisfied "tilt to the sea"). [Liveness: report ONLY from a fresh SendMessage-by-agentId probe + git log on the worktree + gh pr view — never assumption.] **SPONSOR RE-SOAK VERDICT on `806592d` = APPROVE-WITH-TWEAKS (2026-06-13 via popup)** — all 5 soak-fixes landed; QUEUED TWEAKS to iterate AFTER the batch+fresh-soak: (1) SEA near-band too sky-cyan → Drew deepen/saturate the near-band teal anchor; (2) SHIRT too pale-cream → Devon deepen shirt atlas (ticket 86ca8fetr); (3) HAIR has a weird BROWN SPIKE on top — invisible top-down, VISIBLE as you tilt the cam down (pitch-8 horizon view); hair should ALSO be MESSIER/more natural → Devon (NEW finding 2026-06-13, file ticket on dispatch); (4) ADD WRECKAGE/DEBRIS on the beach to narrate the arrival (ticket 86ca8feuf, decide narrate-vs-implied). (5) **NO AXE (Sponsor flagged 3×: soak1 #3, this session "i didnt see any axe", then "stump is there but no axe")** — he REACHED the stump and saw NO axe. Held axe is parented to RightHand_010, HIDDEN until HasAxe (HeldAxe.cs:49-55); reaching the stump (≤2u) should flip it. Likely the SAME FALSE-GREEN CLASS as the castaway: `-verifyCraft`/`-verifyAxe` captures pass in isolation while the gameplay path fails — either (a) craft doesn't trigger in real gameplay (CraftSpot.player wrong transform), or (b) it triggers but the axe is invisibly mis-scaled (the 267× hand-bone lossy-scale trap that already shipped a GIANT axe once) / clipped in body. **Devon FIX: reproduce in a REAL gameplay build (walk kid onto stump @ (8,6), NOT the capture) → root-cause + fix the craft→VISIBLE-held-axe path; THEN add the diegetic win the Sponsor wants — axe visibly resting ON/IN the chopping-block stump from spawn (so an axe is ALWAYS on screen + guides the player), transferring to hand on craft.** Sponsor did NOT flag shore-framing (86ca8fet0) → treat "water at the beach" framing as SATISFIED by the camera fix. **DISPATCH ORDER: merge #37 + #33 FIRST (both move main; #37 touches CastawayCharacter.cs), THEN branch Devon's castaway-fix + Drew's ocean-fix off the settled main to avoid conflicts.** Uncommitted orch-docs on main tree (unity-conventions.md + STATE.md + decisions-while-away.md) → fold into Sponsor-gated orch-docs PR. [PRE-SOAK HISTORY FOLLOWS] — M-U1 + M-U2 CLOSED; **M-U2 loop SOAK VERDICT = FUN** (Sponsor 2026-06-13 via /sponsor-questions-walkthrough — the milestone feel-gate PASSED). **CASTAWAY:** the chunky-cartoon chibi base ("Mini Chibi Kid", Sketchfab/joaobaltieri, CC-BY, ships Idle/Walk/Run anims) is now the player character — **PR #26 MERGED → `9dd317f`** (Tess APPROVE, shipped-exe Idle/Walk verified). DECISIONS.md catch-up **PR #27 MERGED → `97d8283`** (Drew APPROVE, 8 cited entries). PR #25 (Quaternius bone-scale) CLOSED superseded. (Merges done with EXPLICIT Sponsor approval — `main` merges are Sponsor-gated; the auto-mode classifier denies orchestrator auto-merge.) **AXE RE-DO DONE → PR #29** (Devon, HEAD `a968786`, CI green run 27461297552): chibi HOLDS the sourced rustic hatchet in the shipped exe, attached to `RightHand_010`, appears on craft, chop fires; procedural HeroAxeMesh retired; CC-BY attribution added. Devon caught a **267× hand-bone lossy-scale trap** (child prop on the height-normalized chibi FBX inherits a runaway bone scale → first build shipped a GIANT axe; fixed with hand-local scale ~0.0015). 86ca8ce6y → ready-for-qa-test. **AXE PR #29: Tess APPROVE_WITH_NITS** (review …7990557) — independent shipped-build reproduction confirmed the chibi holds a correctly-scaled hatchet (held bounds 0.16×0.26×0.43; scale trap fixed), equip gate proven, procedural axe retired, CI green; **gate-cleared, Sponsor-gated merge.** NIT (non-blocking): the -verifyAxe capture clips the axe at the frame edge → readability falls to the Sponsor soak; re-frame `AxeVerifyCapture` as a future capture-tooling follow-up. **IN FLIGHT (2 agents): (1) Devon — CASTAWAY RECOLOR** (agentId `a5246d0e2832720d9`, branch `devon/castaway-recolor`, ticket 86ca8ca1m): recolor the chibi atlas to our identity (sandy/ginger hair, warm khaki shirt, maybe barefoot) per `castaway-style-v2.md`; reviewer Tess. **BEACH-WATER DONE → PR #30** (Drew, HEAD `8847a9a`, CI green run 27461779718): re-tuned teal vertex-gradient sea (smooth-shaded, in-shader swell, foam shore), VISIBLE behind the wooded shore in the exe (110/110 EditMode, 30/30 PlayMode); Drew also fixed a flat `TestGround` placeholder that was occluding the water plane. **PR #30: Tess APPROVE** (review …183839) — independent shipped-exe reproduction confirmed the ocean visible behind the shore (foam shoreline + islet + fog edge; inland-vs-seaward contrast proves the orbit reveals the sea), 110/110+30/30, CI green, occlusion-fix regression guard green; gate-cleared, Sponsor-gated merge. **THREE gate-cleared PRs awaiting Sponsor merge: #28 (water direction), #29 (axe, APPROVE_WITH_NITS), #30 (water impl, APPROVE).** Drew + Tess FREE. Devon RECOLOR DONE → **PR #31** (HEAD `78a2a3a`, CI green run 27462123137): sandy-ginger hair, warm khaki shirt (0.72,0.60,0.42), bare feet, smooth young face, white-sclera/dark-iris eyes kept — young/hopeful, no grizzled drift; identity guard `Atlas_RecoloredToYoungHopefulIdentity` added (restores shirt-luma>0.6); 105/105+30/30; recolor = UV-cell atlas repaint (luma-preserving), not material tint. **PR #31: Tess APPROVE_WITH_NITS** (review …048466) — shipped-exe identity confirmed young/hopeful (sandy-ginger hair, khaki shirt luma 0.612, white-sclera eyes, bare feet, no grizzled drift), identity guard green, CI green. **ALL FOUR PRs NOW GATE-CLEARED = the full Sponsor merge batch: #28 (water direction, docs) + #29 (axe, APPROVE_WITH_NITS) + #30 (water impl, APPROVE) + #31 (recolor, APPROVE_WITH_NITS).** **MERGE ROUND (Sponsor approved "merge all 4" via walkthrough): #28 + #29 MERGED → main @ `3f3a3b6`** (water-direction doc + axe held in chibi's hand). **#30 (ocean) + #31 (recolor) both CONFLICT on Boot.unity** vs #29's axe-baked scene. **SCENE INTEGRATION MERGED → PR #32 → main @ `46f2a9d`** (Tess APPROVE_WITH_NITS, review …286845; all four elements confirmed TOGETHER in the shipped exe; 107/107 + 32/32; CI green run 27463435576). **#30 + #31 CLOSED superseded-by-#32.** The full castaway/scene wave is ON MAIN: recolored young/hopeful chibi HOLDING the rustic axe on a BEACH with the OCEAN. **COMBINED SOAK SERVING** (background bash `b23f0di6g`, tess-wt off `46f2a9d`) → exe `c:/Trunk/PRIVATE/Far-Horizon-tess-wt/Build/Windows/FarHorizon.exe`, HUD stamp `46f2a9d`. **NO AGENTS IN FLIGHT.** **SPONSOR SOAKED THE COMBINED BUILD (46f2a9d) 2026-06-13 → REJECTED on the visual gate** — his eyes caught what Tess's green guards + the isolated `-verifyCastaway` capture MISSED (the isolated capture was FALSE-GREEN; the GAMEPLAY orbit-cam lighting washes the castaway to a yellow blob). His 5 points: (1) castaway reads ALL-YELLOW washed-out, cap still a HAT not hair; (2) shirt not readable; (3) "no axe" — it's CRAFT-GATED (appears on HasAxe; he likely didn't craft — Devon confirms the flow holds); (4) ocean reads as a small GREY POND not a teal sea; (5) orbit camera PITCH clamped — can't tilt down to the horizon. **SOAK-FIX ROUND: (1) Devon DONE → PR #34** (`devon/castaway-soakfix`, 109/109+32/32 local, unity CI GREEN (run 27465366937); 86ca8ca1m ready-for-qa): ROOT CAUSE confirmed — (a) `-verifyCastaway` cam had NO post-processing → skipped the Zone-D post that washes the gameplay view = FALSE-GREEN (now post-enabled); (b) the cap is TWO meshes (dome `Object_41` + brim `Object_42`) → v1 recolor kept both. FIXED: hid both cap meshes + procedural sandy-ginger hair on `Head_05`; recolor now REPRODUCIBLE-FROM-CODE (editor Recolor method at bootstrap, not a hand-run PIL edit) + idempotent, shirt deepened to mid-khaki separating from skin; luma guard re-tuned to a band `[0.42..0.66]` + cap-hidden/hair-present guards; craft→axe still attaches. **(2) Drew DONE → PR #35** (`drew/ocean-camera-fix`, 86ca8fet0; EditMode 108/108 + PlayMode 33/33; unity CI PENDING — pushed ~11:30 UTC): root cause was NOT the water material (shipped correct) — it was the CAMERA (minPitch 35 never reached the sea) + a beach DUNE crest occluding the water. Fixes: pitch floor 35→8 (minPitch 8 / maxPitch 70 / **defaultPitch 55 unchanged, Sponsor-locked**), beach sloped monotonically DOWN into the sea (no occluding crest), teal brightened to top of the sub-1.0 band (survives the warm fog/grade). Gameplay-view capture verified at `3a548a7`. **INTEGRATION DONE → PR #36** (Devon, HEAD `806592d`, CI green run 27466086196): all FOUR verified in the SHIPPED GAMEPLAY view (real OrbitCam, not isolated rigs) — sandy-ginger-HAIR castaway (no yellow cap/blob), mid-khaki shirt, white-sclera eyes; crafted axe held (gameplay loop: craft→chop confirmed); teal SEA (pixel-sampled B≥G>R), beach sloping in; camera tilts to horizon (pitch floor 8; default 55/max 70). 110/110+33/33, all 6 discriminating guards green together; recolor reproducible-from-code; verify cam post-enabled (no false-green). Supersedes #34/#35. **PR #36: Tess APPROVE_WITH_NITS** (review …412278) — all FOUR ACs verified in the GAMEPLAY view (pixel-sampled: warm-ginger head NOT yellow-blob, axe held on craft, teal sea B≥G>R, pitch-8 frames horizon); CI green, 110/110+33/33, false-green root cause fixed (verify cam post-enabled). NITs (non-blocking → capture-tooling ticket 86ca8fevz): the `-verifyCastaway` close-up is ITSELF unreliable (R-channel-clipped/wrong-facing — don't trust as identity evidence; ACs pass via the GAMEPLAY view); + CS0618 cosmetic warnings. **86ca8fevz DONE → PR #37** (Devon, `devon/verifycapture-hardening` @ `4c54123`): both verify-capture NITs FIXED + DETERMINISTIC (each mode run 2× byte-identical), 116/116 EditMode + 34/34 PlayMode local; new pure unit-tested `VerifyCaptureFraming.cs` (BakeMesh-settled bounds, no Mathf.Max floor, pinned facing, neutral-slate clear) + regression guards (the framing math the prior code left untested = why the bug slipped 3 PRs). **STACKED ON #36** (depends on #36's URP asmdef + regenerated Boot.unity) → **MERGE ORDER #36 → #37**; #37's PR-diff shows #36's commits until #36 merges. #37 CI is RED = PackageCache EPERM (environmental — fails at Bootstrap before project code, rotating package; commit is 8 .cs/.meta only, no package files) → it gets a clean CI run + Tess QA AFTER #36 merges and #37 rebases (clean diff, runner exclusive). NO AGENTS IN FLIGHT. DOC CANDIDATE (maintain-docs): verify-capture evidence must ride the gameplay render path (post + skybox) w/ a NEUTRAL clear, deterministic BakeMesh bounds (no Mathf.Max floor), a PINNED facing, + UNIT-TESTED framing math — the false-green-class fix. **RE-SOAK SERVING off 806592d** (tess-wt, background) → hand the Sponsor the exe + stamp `806592d` + the EXPLICIT test-&-confirm checklist (per `memory/soak-handoff-path-and-explicit-test-checklist.md`); this is the FIX to his soak rejection. **On Sponsor RE-SOAK APPROVAL → merge #36 + close #34/#35 superseded + flip 86ca8ca1m + 86ca8ce6y + 86ca8fet0 → complete.** Sea near-band sky-cyan = soak-tunable (Drew's anchor), not a regression. Soak-tunable carry (not a regression): sea near-band desaturated sky-cyan at distance (Drew's anchor). DOC CANDIDATE (maintain-docs): EPERM — let local Unity/exe processes fully EXIT before pushing; a resident PackageCache lock can trigger the collision even when `gh run list` shows no in-flight CI (Devon PR #36). Both bake Boot.unity → orchestrator INTEGRATES (regenerate-on-rebase, proven via #32) → re-serve a FRESH soak in the NEW format (path + stamp + explicit test-&-confirm checklist per `memory/soak-handoff-path-and-explicit-test-checklist.md`). **LESSON: isolated verify-captures (flattering camera/lighting) can be FALSE-GREEN vs gameplay — visual QA must judge the GAMEPLAY orbit-cam view.** 86ca8ce6y (axe) + 86ca8ca1m both stay pre-complete pending the re-soak. PRIYA DONE → **DECISIONS.md PR #33** (6 entries for the 06-13 wave, commit `ceb6364`) — **Drew APPROVE** (review …257496; 6-not-7 count confirmed, all cited/accurate, append-only); gate-cleared, Sponsor-gated merge. NIT (cosmetic): PR #33 body says "7" entries, diff is 6. + **M-U3-SCENE board FILED** on ClickUp (list 901523878268): `86ca8feqv` M-U3-SCENE-1 beach-ocean [DONE, anchors #28/#32], `86ca8fet0` M-U3-SCENE-2 shore-framing [PENDING-SPONSOR], `86ca8fetr` M-U3-SCENE-3 shirt-tone-deepen [PENDING-SPONSOR], `86ca8feuf` M-U3-SCENE-4 shipwreck-debris [PENDING-SPONSOR], `86ca8fevz` capture-tooling-hardening NIT (Devon/Tess/S). **THREE Sponsor scene-decisions now tracked as tickets (judge from the combined soak): shore-framing (86ca8fet0), shirt-tone (86ca8fetr), wreckage narrate-vs-implied (86ca8feuf).** (Cosmetic: 86ca8fetr's body uses the label "M-U3-SCENE-CAP" for the capture ticket — real ID is 86ca8fevz; a ClickUp cross-ref edit was denied 2× so Priya left the descriptive label, harmless.) **NITs to track (follow-up): capture-tooling — AxeVerifyCapture clips the axe (#29) + CastawayVerifyCapture non-deterministic framing (#31, SMR-bounds floor + +Z-front assumption); same class → one capture-tooling-hardening ticket.** Soak calls for Sponsor: shirt renders pale-cream (#31 deepen option) + shore-framing band (spawn ~16-18u inland). Soak note: shirt renders pale-cream under the warm key — Devon flagged a one-line atlas-deepen option if the Sponsor wants stronger shirt/skin separation. DOC CANDIDATE (maintain-docs): gradient-palette-atlas identity recolors should repaint UV cells (luma-preserving hue remap), never per-material tint (flattens the toon gradient) — unity-conventions §Asset creation / §FBX. **SPONSOR FOLLOW-UP (framing — queued): the sea reads as a BAND behind a grass foreground** because spawn sits ~16-18u inland (Sponsor-locked) — options: pull the shore closer to spawn, OR move spawn seaward (Uma §5 q2). The "water at the beach" directive isn't fully satisfied until this framing is decided. **MERGE-ROUND HAZARD: PR #29 (axe) + #30 (water) + the recolor PR all bake Boot.unity** → first merges clean; each subsequent merger REGENERATES Boot.unity (regenerate-on-rebase per unity-conventions §binary-scene). **PRIYA TODO: formalize the M-U3-scene board** (beach-water DONE + spawn-framing + wreckage/debris [Uma §5 q1, Sponsor TBD]) — the water work landed ticket-less. Recolor = texture-atlas edit of `Assets/Art/Character/MiniChibiKid/mini_material_baseColor.png` to our identity palette (sandy/ginger hair, warm khaki shirt ≈0.72,0.60,0.42, maybe barefoot) per `team/uma-ux/castaway-style-v2.md`; no new Uma direction needed. **SPONSOR-GATED MERGES PILING: PR #28 (water brief), PR #29 (axe)** — gate-clearing; + the water-impl PR + recolor PR to come → batch for the next Sponsor merge round. DOC CANDIDATE (maintain-docs): the 267× hand-bone lossy-scale trap (child prop on a height-normalized FBX) → unity-conventions §FBX. **AI-GEN (Sponsor 2026-06-13): HOLD Hyper3D in reserve — do NOT use speculatively.** Default = Sketchfab sourcing (free, CC-BY, zero Sponsor effort — it covered chibi + axe). Manual Hyper3D web-gen (Creator plan, no $96 Business upgrade) is reserved for when the orch searches Sketchfab and comes up EMPTY for a BESPOKE STATIC prop (Hyper3D output is un-rigged → not for characters). Trigger: orch flags the sourcing gap → Sponsor generates+exports the FBX → orch integrates (expect a mesh-cleanup pass). **NO survival-mechanic M-U3** until scene + castaway polish land (Sponsor priority = finish the world first). UNCOMMITTED on main tree: `.claude/docs/unity-conventions.md` (this session's maintain-docs captures) + `.claude/decisions-while-away.md` — fold into an orch-docs PR (Sponsor-gated merge). RUNNER: serialize CI/Unity, incl. a reviewer's build vs an active CI run (EPERM). Worktrees: ALL detached/clean except devon-wt (axe) + uma-wt (water) in flight. Godot archive: c:/Trunk/PRIVATE/RandomGame/team/STATE.md.

---

## Role sections

Each role updates its own section as it works M-U1 / M-U2 tickets. The orchestrator reads the whole file at every tick to spot stalls. Don't edit another role's section. Last-updated uses ISO date `YYYY-MM-DD`.

### Priya (Project Leader)
(run: 86ca9a340 — authored the Diagnose-Before-Fix pre-fix PR convention: full standalone section in `team/TESTING_BAR.md` + paste-able block in `team/orchestrator/dispatch-template.md`, with the walk-float `-footTrace` worked example + the stack-trace-only OOS boundary; Erik research §A Rank 1; reviewer Devon. Next planning surface is the M-U2 thin-survival-loop backlog, which files when M-U1 closes)

### Uma (Game UX Designer)
(fresh — first M-U2 surface is the thin-loop UX once M-U1 closes)

### Devon (Game Developer #1, lead — engine/runtime/build/CI)
(fresh — ported U3 input + drove U4 CI; next is M-U2 survival-loop systems)

### Drew (Game Developer #2 — content systems/tools)
(fresh — populated as M-U2 content work dispatches)

### Tess (Tester)
(fresh — owns U7 testing-bar translation in QA; build-capture gate per `team/TESTING_BAR.md`)
