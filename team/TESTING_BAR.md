# Testing Bar — Far Horizon (Unity translation)

**Sponsor directive (2026-05-02, carried from RandomGame):** "I want you to use a lot of time testing, I don't want to debug and return findings all the time."

Translation: by the time anything reaches the Sponsor for sign-off, it must already have been hammered thoroughly. Sponsor's role is **acceptance**, not bug-finding. This document is binding on every role; Tess enforces it. Engine surfaces translated to Unity 2026-06-12.

A ticket is "complete" only when ALL of:

1. **Paired tests.** Every behavior change ships EditMode and/or PlayMode tests in the same PR (`FarHorizon.EditTests` / `FarHorizon.PlayTests`). Bug fixes pin the regression first. Edge probes (negative-path + boundary) are part of this, not optional extras.
2. **Green checks.** Full EditMode + PlayMode suites green — verified from the `-testResults` XML's `<test-run result="Passed">` line, not exit codes. CI (from U4 onward) chains BootstrapProject.Run → tests → FarHorizonBuilder.BuildWindows.
3. **Shipped-build verification** (successor to RandomGame's HTML5 gate). Editor evidence is never sufficient — the editor-vs-runtime divergence class (Awake-no-serialize, shader stripping, NavMesh-not-shipped; see `.claude/docs/unity-conventions.md`) is proven by spike incidents. Anything UX/visually-visible needs evidence captured from the BUILT exe (windowed launch, in-game capture, HUD build-stamp visible) attached to the PR/ticket.
4. **Self-Test Report.** UX-visible PRs carry an author-posted Self-Test Report comment (what was run, on which build stamp, what was observed — concrete values only, never invented) before Tess reviews.
5. **Tess sign-off.** QA review verdict (APPROVE / APPROVE_WITH_NITS / REQUEST_CHANGES) as a PR comment. Tess-authored PRs get a Drew/Devon peer reviewer instead (Tess can't self-QA).
6. **Sponsor soak** only where the gate is subjective feel or first-of-class visuals — right-size the ask; always include the exe path + the expected HUD build stamp. **State the bar (bounded silence).** The soak ask must NAME the quality bar this iteration tested + the surfaces in scope, and list the bars NOT tested — "this looks done" is not a convergence claim; "tested bar B on surfaces S; NOT tested: …" is. See § "Predict-Before-Soak + bounded silence" below.
7. **Demonstrated RED — when the PR introduces or amends a GUARD** (a CI gate script, a wiring assertion, a regression pin, a staleness grep, a capture-wrapper needle set, or a quality bar's own check). A gate is not a gate until it has been demonstrated red: the PR must quote **two reds — Red A (discrimination) and Red B (wiring)** — plus a **green**, all from the same tree with the **mutation named**. A PR that ships only Red A must say so explicitly and state that the wiring half is unproven. A green alone is what a gate wired into nothing also produces. **Full requirement + what does and does not qualify: § "A gate is not a gate until its RED has been demonstrated" below.**

---

## Accuracy + performance gates (Erik research, 2026-06-15)

Folded from Erik's developer-accuracy / performance note (`team/erik-consult/developer-accuracy-performance-research.md`, not auto-read). These three gates close recurring failure classes the float saga + world-look churn exposed; they bind alongside the 6-point "complete" rubric above. Full adoption is ticketed (`86ca9a340` / `86ca9a36g` / `86ca9a3b3`) — these lines make the GATE mandatory now, even before the harness/tooling lands.

1. **Diagnose-Before-Fix.** A `fix(...)` PR MUST state the DIAGNOSED root cause + ONE cited isolation result in the PR body BEFORE the fix — not "tried X, seems better." This formalizes the isolation-probe method; it exists because guess-fixes cost 2–4 soak-overturns per defect (the float saga overturned its own root-cause framing ≥3×; the world-look fix-shape was wrong twice — only trace caught the real cause each time). A fix PR whose body asserts a fix without naming the diagnosed cause + evidence is bounced. **Full requirement + worked example: § "Diagnose-Before-Fix — the pre-fix PR convention" below.**
2. **PlayMode locomotion-sampling tests.** Any feature whose correctness is PER-FRAME during motion (grounding, held-prop envelope, finger-curl, camera follow) ships a `[UnityTest]` that `yield return null`-samples the assertion EVERY frame across a real WALK — not just a standing/spawn snapshot. The "tests green but Sponsor sees during-walk elevation" gap is exactly the standing-only assertion missing the motion sample (the smoothing-lag float AND the sole-vs-root float both passed at-rest tests). Sample per-frame through real `Update` + a real `Time.time` window (never the headless `Time.deltaTime~=0` trap — see "Multi-step-loop coverage" below).
3. **SRP-batcher Frame-Debugger audit before any new visual pass.** Before shipping a new shader / material / scatter surface, audit the Frame Debugger that the SRP batcher is actually batching (no per-instance break) — `CBUFFER_START(UnityPerMaterial)` completeness + no live `MaterialPropertyBlock` breaking the batch. Catches the silent perf regression where a new visual pass quietly drops the frame rate before it reaches the Sponsor.
4. **Predict-Before-Soak (the forward sibling of Diagnose-Before-Fix).** Any soak-gated / feel / first-of-class-visual PR MUST carry a falsifiable PRE-soak prediction in its Self-Test Report — "I expect the soak to show Y; I expect Z to NOT appear" — written BEFORE the build is served, then graded against the soak outcome. Diagnose-Before-Fix names the CAUSE before a fix (backward-looking); this names the expected RESULT before serving (forward-looking). It exists because the most expensive recurring loop on this project is asserting "fixed/removed" with no pre-registered prediction a soak then refutes (`claim-removed-but-soak-shows-present`; the 8-soak jump-pullback saga; `soak-fail-test-pass`). Tess bounces a soak-gated PR whose Self-Test Report has no graded prediction + bounded convergence claim. **Full requirement + worked shape: § "Predict-Before-Soak + bounded silence" below.**

---

## Diagnose-Before-Fix — the pre-fix PR convention (ticket 86ca9a340; Erik research §A + Rank 1)

**Source:** Erik R&D note `team/erik-consult/developer-accuracy-performance-research.md` §A + Rank 1 — Erik's #1-impact, zero-tooling recommendation. This section is the binding spec for the one-line gate stated in "Accuracy + performance gates" item 1 above.

**The gate.** Any `fix(...)` PR whose subject is a diagnosed defect MUST carry, in the PR body, BEFORE the fix description, both of:

- **(a) the diagnosed ROOT CAUSE in one sentence** — what the actual broken thing is, named concretely (the system + the mechanism), not "it looked wrong so I changed X."
- **(b) ONE cited isolation result that confirms (a)** — a single piece of evidence that the named cause is real and that other hypotheses were ruled out. Acceptable forms:
  - a **probe flag + its output** (`-seaWaterOnly` → `0 water px`; `-footTrace` → `foot-float -0.003u, shadow-to-feet 0.087u`),
  - a **magenta-diff pixel count** (sentinel-color rebuild + pixel-diff: `248/921600 changed = 0.027%` ⇒ mesh contributes no visible pixels),
  - a **trace dump line** (`[FloatTrace]` / `[TINTDIAG]` / `ClipBaselineDiagnose: Idle −0.003, Walk +0.63`),
  - a **guard that reds→greens** (a failing assertion + its values that the fix flips green).

One isolation result is the floor, not a transcript of every probe — cite the ONE that nailed the cause.

**Enforcement.** **Tess bounces** any `fix(...)` PR whose body asserts a change without naming the diagnosed cause + a cited isolation result (REQUEST_CHANGES, one-line note: "Diagnose-Before-Fix: PR states a fix but no diagnosed root cause + cited isolation result"). A guess-fix that only describes the change does not pass the bar.

**Why it pays.** Every walk-float / world-look convergence in `.claude/docs/unity-conventions.md` happened ONLY after an isolation probe forced the real system to surface; the reverse pattern (guess a fix → rebuild → soak → get contradicted) cost 2–4 build-and-soak rounds per defect. Stating the diagnosis up-front collapses those rounds into one and lets the reviewer check the *cause*, not just the diff.

**Worked example (what "good" looks like — from the walk-float saga; cited to `.claude/docs/unity-conventions.md` §FBX/rigs, walk-float saga + ground-snap entries, do not re-derive here):**

> **Diagnosed root cause:** The avatar reads as "still floating" not because the body is ungrounded but because the **blob shadow is stranded ~9 cm above the snapped feet** — the body IS grounded; the shadow rides a fixed Y while the feet snap to the visible terrain, so the *shadow-to-feet gap* is what reads as float.
> **Cited isolation result:** `-footTrace` overturned the body-snap hypothesis — measured **foot-float −0.003u** (feet ARE planted) while **shadow-to-feet was 0.087u**, dropping to **0.023u** after making the shadow track the snapped feet.
> **Fix:** make the blob shadow track the snapped feet rather than a fixed Y.

The diagnosis sentence names the real system (shadow-vs-feet, not root grounding); the single `-footTrace` line both confirms it AND rules out the obvious-but-wrong hypothesis (body-snap). That is the shape every `fix(...)` PR must hit. (The broader saga overturned its root-cause framing ~6× — NavMesh-slab → renderer-enabled-hit → blob-shadow → `SkinnedMeshRenderer.bounds` false-green → `BakeMesh` actual-lowest-vertex — each overturn forced by a probe; see the saga entry for the full chain.)

**Out of scope — when the gate does NOT apply.** Plain logic bugs that just need a stack trace are NOT in scope (Erik §A: "Plain logic bugs ... are NOT in scope"). A `fix(...)` whose root cause is obvious from a thrown exception, a failing unit test's assertion message, or a null-ref stack trace does not need a manufactured isolation probe — the stack trace IS the diagnosis. The gate targets the **diagnose-via-trace / asset-is-fine-the-view-is-the-problem class**: defects where the visible symptom names the WRONG system (a colour tweak that can't fix a not-rendering mesh; a water-Y change for a composition problem) and an isolation probe is the only thing that surfaces the real cause. When in doubt, the one-sentence diagnosis is cheap — write it.

---

## Predict-Before-Soak + bounded silence (the forward half of Diagnose-Before-Fix; ticket-free, applies now)

**Why this exists.** Diagnose-Before-Fix forces you to name the *cause* before a fix (backward-looking). It does nothing about the project's single most expensive loop: serving a build with a "fixed/removed/done" claim that the Sponsor's soak then refutes — `claim-removed-but-soak-shows-present`, the 8-soak jump-pullback saga, `soak-fail-test-pass`. The cure is a falsifiable prediction written BEFORE the build is served and GRADED against the soak, plus a convergence claim bounded to a NAMED bar.

**The gate (soak-gated / feel / first-of-class-visual PRs).** The Self-Test Report MUST carry, before the build reaches the Sponsor:

- **(a) Prediction (pre-soak, falsifiable):** one line in the shape *"I expect the soak to show **Y**. I expect **Z** to NOT appear."* — concrete, observable, specific enough to be wrong. Not "it should look better."
- **(b) Convergence claim (bounded silence):** *"Tested bar **B** on surfaces **S**; bars NOT tested: **…**."* A "done"/"removed"/"fixed" claim is only honest against a NAMED bar; an unbounded "looks done" is the form most often overturned by the next soak testing a *different* bar. (You already run three independent evaluator families — peer APPROVE + Tess PASS + Sponsor soak; this is the missing named-bar discipline that turns "looks done" into an honest convergence claim.)
- **(c) After the soak — Outcome vs prediction:** was the prediction borne out? A refuted prediction is a *finding*, not a failure — it means the claim's foundation was wrong. Per `claim-removed-but-soak-shows-present`: STOP and deep-investigate WHY the foundation was wrong before re-fixing; never re-assert "removed" until a capture of the actual symptom region proves it.

**Enforcement.** Tess bounces (REQUEST_CHANGES, one-line note: "Predict-Before-Soak: soak-gated PR with no falsifiable pre-soak prediction / unbounded convergence claim") any soak-gated PR whose Self-Test Report lacks (a)+(b). Pairs with — does not replace — Diagnose-Before-Fix on `fix(...)` PRs.

**Out of scope.** Mechanical `chore`/`docs`/`test` PRs, and non-soak-gated changes whose correctness is fully covered by a green paired test — there is nothing for a soak to refute.

**Upstream pre-empt.** The *cheapest* place to win this is BEFORE the dispatch — name the Sponsor's real bar up front via the `/name-the-bar` skill (`team/quality-bars.md`), so the prediction in (a) is written against a *confirmed* bar instead of a guessed one.

**Worked shape:**
> **Prediction (pre-soak):** I expect the pond to read as an organic, irregular outline at gameplay-cam height; I expect NO perfect-circle silhouette and NO "pond-in-a-mound" raised rim.
> **Convergence claim (bounded):** Tested the *organic-outline* bar on the gameplay-cam side profile; NOT tested: foam pulse, reflection, night lighting.
> **Outcome vs prediction (post-soak):** …

---

## test-evidence convention — what the bar expects on a PR

So every PR carries the same shape of proof (and reviewers/CI know exactly what to look for), the mechanical gates are:

**Mechanical gates (CI, `.github/workflows/`):** these run automatically; a PR is not green until they pass.

| Gate | Script (under `.github/workflows/scripts/`) | What it proves | Fails on |
|------|--------|----------------|----------|
| Structure | `structure_check.sh` | repo hygiene, asmdefs, entry-point methods present | committed artifacts, missing `.meta`, renamed entry point |
| Docs tool-call markup | `check_docs_markup.sh` | no committed `*.md` carries an authoring agent's leaked tool-call tags | a tool-call tag alone on a line at column 0 in any tracked `*.md` (`86cayxtw8`). **Wired TWICE on purpose:** in the hosted `structure` job AND in the `*.md`-path-triggered `docs-markup` workflow — `ci.yml`'s `paths-ignore` lists `'**/*.md'` + `'team/**'`, which skips the WHOLE run, so a docs-only PR gets no `structure` check at all (ground-truthed on PRs #360/#358, both filed BEFORE the `docs-markup` workflow existed: `auto-merge` was their only check). All 5 files the guard was written for arrived on docs-only PRs, so a `structure`-only wiring would have caught 0 of 5. ⚠ **Do not read that #360/#358 measurement forward** — since `docs-markup.yml` shipped, a docs-only PR DOES get one real check. See § *What CI actually covers, by PR lane* below. |
| Console-error | `check_unity_log.sh` | no compile/fatal errors in any Unity log | `error CS####` / `Compilation failed` / `Fatal error` / `Unhandled exception` (URP first-import + recovered-NavMesh-race lines allowlisted by **shape**, never subtracted from the error scan) |
| Test-result | `parse_test_results.py` | EditMode + PlayMode genuinely green | `result != Passed`, any failure, or `total == 0` (an empty run is a failure) |
| Build-result | `ci.yml` build-gate | the Windows exe actually built | no `[FarHorizonBuilder] result=Succeeded` line |
| **Shipped-build capture** | `capture_gate.sh` + `frame_check.py` | the BUILT exe renders REAL frames (editor-vs-runtime backstop) | black / empty / uniform / all-magenta (shader-strip) frames, or **zero** frames captured |

### What CI actually covers, by PR lane

**This is the canonical statement. Cite it; do not re-derive it from `paths-ignore` alone** — reasoning from that filter in isolation is what produced the "docs-only PRs get ZERO checks" claim that propagated into five tickets and a PR body before Drew measured it on PR #389 (2026-07-31).

Two workflows decide a PR's coverage, and they trigger **independently**:

- **`ci.yml`** — jobs `structure` (line 133), `build` (223), `capture` (501), `playmode` (1247). Carries a **workflow-level** `paths-ignore` — `'**/*.md'`, `'team/**'`, `'inspiration/**'`, `'.claude/**'`, `'docs/**'` (`:96-101` push / `:104-109` pull_request). When EVERY changed path matches, the run does not start at all, so **none** of those four jobs report. (The old single `unity` job was split into `build` + `capture` + `playmode` by `86cafz9tg` — a ticket or comment still saying "the `unity` job fires" is using a retired name.)
- **`docs-markup.yml`** — a SEPARATE workflow, GitHub-hosted, no Unity, no licence, ~10s. Triggers on `paths: ['**/*.md', <the workflow>, <its script>]` for both `push:main` and `pull_request:main`. `ci.yml`'s `paths-ignore` has **no effect on it**.

| PR lane (what the diff touches) | `structure` / `build` / `capture` / `playmode` | `docs-markup` |
|---|---|---|
| Any `Assets/**`, `.github/**`, or other non-ignored path | run — full merge gate | runs too, if the diff also contains any `*.md` |
| `*.md` only (incl. `team/**/*.md`, `.claude/docs/*.md`) | **do not run — no result at all** | **runs — one real pass/fail** |
| Only non-`.md` ignored paths (e.g. `inspiration/*.png`, a `.claude/*.json`) | do not run | **does not run either** — its `paths:` filter is `**/*.md`. This lane genuinely has zero checks. |

⚠ **`auto-merge` is not a gate.** It shows up under `gh pr checks` on any label-merged PR (measured on #381 / #383 / #384, 2026-07-31) and it is the merge Action reporting itself, not a quality check. Never count it when saying what a PR was verified by.

**Three rules that follow, and the third is the one people get wrong:**

1. **An absent check is never a pass.** `paths-ignore` makes the run not trigger; it does not report a skipped required check as success (`ci.yml:89-95` says so in its own comment).
2. **Never claim "CI green" on a docs-only PR.** Name the check you actually got: *"`docs-markup` pass; `ci.yml` did not run — no build or test result on this PR."*
3. **But "docs-only ⇒ zero CI checks" is FALSE, and writing it is its own over-claim.** A docs-only PR gets one real, hosted, licence-free gate. Measured on PR #389 (`86caz42fr`, Drew, 2026-07-31): `gh pr checks 389` → `docs-markup (hosted, no license)   pass   11s`, run `30649924290`. Independently confirmed `SUCCESS` on PRs #381, #383, #384, #385, #386. What the green does NOT cover is the build and the tests — that is the real caution, and it is not the same sentence as "no checks ran".

**Author evidence on the PR (UX-visible PRs):**

1. **Paired tests** in the same PR — EditMode and/or PlayMode, with edge probes; bug fixes pin the regression first. Script-level gate logic gets bash/python unit-style checks (`tests/scripts/`).
2. **Self-Test Report comment** — what was run, **on which build stamp** (`BUILD <tag> | <UTC> | <sha>` from the HUD), what was observed. Concrete values only, never invented.
3. **Shipped-build capture** — run `.github/workflows/scripts/capture_gate.sh Build/Windows/FarHorizon.exe` against your own build and attach/quote the `frame_check.py` PASS line + the build stamp. Editor evidence is necessary, never sufficient (unity-conventions.md §editor-vs-runtime).
4. **Frame-Debugger / SRP-Batcher audit (any new shader or renderer).** Before the Self-Test Report is posted — i.e. BEFORE merge, NOT after the Sponsor's soak — any PR that adds a new shader, material, scatter surface, or visual renderer must verify in the **Frame Debugger** (or the Rendering Debugger's SRP Batcher stats panel) that the new renderer falls INSIDE an SRP batch. Confirm two things and quote the result in the Self-Test Report: (a) **no `MaterialPropertyBlock`-induced break** — the renderer carries no live `MaterialPropertyBlock`/`SetPropertyBlock` (an MPB on a MeshRenderer disables SRP batching for that renderer AND is a GPU Resident Drawer disqualifier — unity6-mastery §2; it is also mutually exclusive with GPU Instancing — Erik §D Evidence D1); and (b) **all shader properties live inside `CBUFFER_START(UnityPerMaterial)` … `CBUFFER_END`** (a property declared outside the cbuffer silently drops the shader out of the SRP-batchable set). This catches the FPS-regression class where a new visual pass quietly breaks batching — a regression that would otherwise only surface in a Sponsor soak, never at PR time. Colour scatter renderers via distinct inline `sharedMaterial` instances (cheap — SRP batches by shader VARIANT, not material count: unity6-mastery §2), never via per-instance MPB. (This operationalizes the "SRP-batcher Frame-Debugger audit" gate declared in §Accuracy + performance gates item 3.)

**The standard capture component:** new verification captures use the reusable `CaptureGate` MonoBehaviour (launched with `-captureGate`, serialized into the Boot scene), not a new one-off hook — the gate scripts inspect its `capture_NN.png` output. One-off probes (`-verifyMove`, feature-specific tours) remain fine for proving a SPECIFIC behavior, but the black/empty-frame backstop standardizes on `CaptureGate`.

---

## Multi-step-loop coverage — the full-cycle convention (U2-7, ticket 86ca8bdhy)

When a feature is a CHAIN of beats that hand state to each other (the M-U2 survival loop: decay → craft → chop → place → restore), per-beat suites tested in isolation are necessary but NOT sufficient — they each spin up their OWN throwaway rig, so a regression in the HAND-OFF between beats (the chopped wood reaching the placement gate; the lit fire restoring the SAME need instance the decay drained) can pass every isolated suite and still ship a broken loop. The convention is a two-tier loop gate:

1. **One in-process end-to-end PlayMode test on ONE shared rig** drives the whole chain in a single sequence, asserting at each beat on the SAME state the previous beat mutated (`SurvivalLoopPlayModeTests.FullCycle_EndToEnd_ClosesTheLoop` is the template). This catches hand-off regressions headless/fast; it runs through real `Update` + a real `Time.time` window (never per-frame deltas — the headless `Time.deltaTime~=0` trap, unity-conventions.md §headless time).
2. **The shipped-build `-verifyLoop` capture** (`CampfireVerifyCapture`, logs `LOOP CLOSED=`) drives the SAME chain through the real NavMesh + click-move in the exe and quits non-zero if the loop does not close — the editor-vs-runtime backstop the in-process test cannot provide.

**Lifecycle trap when constructing the rig:** a `WarmthNeed` (or any `MonoBehaviour` with `Start`-seeded state) added via `AddComponent` in `[SetUp]` runs `Start()` BEFORE the test body executes — set `startFull = true` in SetUp so `Start()` deterministically seeds the value, then re-seed per-test AFTER the first `yield return null` via the public hooks (`SatisfyFull`/`AddWarmth`); never rely on a SetUp-set inspector flag still being read at `Start` time.

**Success-test discipline (the loop-break catch):** a multi-step-loop PR must DEMONSTRATE the end-to-end test catches a deliberate break of the closing seam (PR body documents: break locally → red, with the failing assertion + values; restore → green). For U2-7 the break was no-op'ing `Campfire.AddWarmth` — warmth then kept decaying at the fire (`0.88 < 0.99`), turning the Beat-4 `Assert.Greater` red exactly as designed.

---

## A gate is not a gate until its RED has been demonstrated (ticket `86caz5nr2`; raised by Tess in her PR #380 delta-confirm, comment 5137747272)

**The rule.** A check introduced or amended in order to CATCH something is **not a gate until it has been demonstrated RED**. A gate shown only passing proves nothing about whether it *can* fail — it may be wired into nothing, guarding a path that is skipped, or asserting a tautology. Its evidence is therefore **two-sided by construction**: a **green on the good case** AND a **red on a deliberately broken one**. The red is the load-bearing half. **The green is what a gate wired into nothing also produces.**

**Scope — which checks this binds.** Any PR that adds or amends a *guard*: a CI gate script, a `gate-wiring`-class registration assertion, a regression pin, a doc-staleness grep, a verify-capture wrapper's needle set, a bar's own check (`team/quality-bars.md` § How to use carries the bar-authoring form of this: *"When WRITING or AMENDING a bar — state what its check returns on an instance that should FAIL it"*). **Not** in scope: ordinary feature tests asserting that new behaviour works — those are trivially reddable by deleting the feature. The target is checks whose whole purpose is to catch a FUTURE degradation, because those are the ones nobody ever watches fail — and therefore the ones whose greens get trusted for months.

⚠ **The regression-pin boundary, stated precisely** (this clause previously read *"rubric item 1's 'bug fixes pin the regression first' already covers the regression case"*, which listed regression pins as in-scope and waved them out in the same sentence — the ambiguity was raised on `86cazmcg1` and is resolved here, not softened). A pin written **pin-first for a bug that was OBSERVED red on the unfixed tree**, guarding only that bug's return, is discharged by rubric item 1: its red is the fix's own history and is already quoted in the PR, so BAR 1 asks for nothing further. A check added in the same PR that guards a **DIFFERENT future change** — a rebind, a rename, a convention, a registration — is a guard like any other and BAR 1 binds it, wherever it lives. **The boundary is a PROVENANCE property, not a path property** (was the red observed, and against what?), which is exactly why the globs below cannot express it and why the residual under them cannot be closed by adding a glob.

**Worked example — the two live in one PR.** #366 (`f44ffdc`) ships both halves: the cadence pins for the bug it fixes (discharged by rubric item 1), *and* `EveryAttackState_BindsAClipTheRuntimeCadenceLookupCanResolve`, which its own body calls **"the red-on-broken guard"** and which exists to catch a FUTURE clip rebind — a different change from the one being fixed. That second check is in scope, and it sits in `Assets/Tests/**` where no glob reaches. So `f44ffdc` is **not** a clean negative control for the glob table: it is an instance of the residual, which is the more useful thing to know about it.

**Greppable trigger — the path globs.** The classes above are prose; run your own diff against this list instead of interpreting them. `git diff --name-only origin/main...HEAD` touching ANY of these means BAR 1 applies (globs verified present at `0f14b4f`):

| Glob | Why it triggers |
|---|---|
| `.github/workflows/scripts/**` | the gate scripts themselves + verify-capture wrapper needle sets (`verify_*_gate.sh`, `capture_gate.sh`, `frame_check.py`, `check_*.sh`, `parse_test_results.py`) |
| `tests/scripts/**` | the gate harness, the `gate-wiring` registration arrays (`HEADLESS_GATES=(` / `WINDOWED_GATES=(` in `test_gate_scripts.sh` — `:912` / `:918` at `2d0658b`; **re-find by name**, a range not containing those two declarations is stale, not empty), and the `check_docs_markup` doc-guard harness |
| `.github/workflows/*.yml` | a gate's **invocation** site — registration is two-part, and `ci.yml` is the half the arrays above do not cover |
| `.claude/hooks/**` | hooks are guards (`orchestrator-anti-idle-stop.sh`, `block-destructive-bash.sh`, both pinned from `tests/scripts/`) |
| `team/quality-bars.md`, `team/TESTING_BAR.md` | a bar's own check — the bar-authoring form quoted above |

⚠ **The globs are a LOWER BOUND on the trigger, not a complete one** — saying otherwise would be this section's own failure mode one level up.

**Measured at `2d0658b`, replacing an earlier asserted count.** The claim used to read *"you have ruled out five of the six classes mechanically"*; nobody had checked it. Enumerated: **four** of the six classes above have every instance under one of the five globs — CI gate scripts (**24** gate/check scripts under `.github/workflows/scripts/`, plus the harness in `tests/scripts/`), the `gate-wiring` registration assertions (`test_gate_scripts.sh` + `.github/workflows/*.yml`), the verify-capture wrappers' needle sets (**15** `verify_*_gate.sh`), and a bar's own check (the two named files). **Two do not:**

1. **A regression pin added inside** `Assets/Tests/**` (**232** `.cs` files there). That tree is overwhelmingly ordinary feature tests, so globbing it would over-trigger; for that path the prose classes still govern — and per § Scope's ⚠ the in/out boundary there is a provenance property no glob can express.
2. **A retired-phrase doc-staleness grep over a doc outside the two named `team/` files.** `git grep -E "grep -v .*(CORRECTED|DECISIONS)"` at `2d0658b` returns exactly **2** sites: this file's own template in § "Doc-staleness greps" below (globbed, and cited by § rather than by line because the edit you are reading moved it) and the live instance in `team/uma-ux/ui-toolkit-panels-ux-spec.md` — its **Staleness-scan note (`86cayw6ve`)**, `:236` at `2d0658b` — which is **not** globbed by anything here. `tests/scripts/**` carries the `check_docs_markup` harness, which is a *markup* guard proved by this section's method; it is **not** the retired-phrase form, so that row does not cover this class the way it used to claim.

**And a guard surface that is in none of the six classes at all:** the self-asserting capture COMPONENT. § "The wrapper-layer sibling" below treats it as a guard in its own right — it *"defaults its criteria to pass"* — yet there are **37** `*VerifyCapture.cs` files at `2d0658b`, all under `Assets/Scripts/Runtime/`, matching no glob and named in no class. Relaxing one's criteria, or changing the token it emits, weakens the gate without touching a globbed path.

So: a diff that misses every glob rules out **four** of the six classes mechanically — not five, and not six — and says nothing about the component surface. **Bounded:** this measures where each class's instances LIVE at `2d0658b`, not that the class list is complete; the component surface is proof that it is not.

### Provenance — three independent convergences in one session (2026-07-30/31), by three different people

Three instances is this project's own threshold for promoting a discipline from habit to written rule. Each was **re-verified for this section** rather than copied from the ticket's summary; two corrections came out of that and are marked ⚠.

1. **PR #363 (MERGED `c8ce9483`, branch `devon/86caynve7-twosided-launch-assert`) — the two-sided assert, then an independent reproduction of it.** `assert_launch_windowed` tested only for the *presence* of the windowed flag, so a gate carrying **both** `-batchmode` **and** the window flags stayed green while capturing black frames. The pair was run on a **byte-identical** mutated tree — boar tree md5 `474387b3b10ac305a31601bee178b023` in *both* runs, the harness differing by exactly one line (1 insertion / 1 deletion, the predicate): post-fix needle → **exit 1, `168 passed, 1 failed`**, boar the sole `[FAIL]`; **pre-fix needle → exit 0, boar `[ OK ]`, zero `[FAIL]`** — the false-PASS reproduced on demand. Unmutated tree + post-fix needle → `169 passed, 0 failed`. The mutation was then applied to a **second** gate independently (`weaponset`), each run producing exactly one `[FAIL]` naming its own gate, which is what rules out "the fix is boar-shaped". Tess then **re-ran the pair herself** in `Far-Horizon-tess-wt` on the same sha — her APPROVE opens *"I did not take the PR body's evidence on trust."*
   ⚠ **Attribution corrected.** `86caz5nr2`'s body credits the two-sided assert to Tess. The branch prefix and the Self-Test Report (*"in worktree `Far-Horizon-devon-wt`"*) make it **Devon's** PR with **Tess's independent reproduction**. That is the *stronger* provenance, not the weaker one: the contrastive pair was run twice, by two people, in two worktrees, and agreed.

2. **PR #370 (OPEN at time of writing, branch `tess/86cayp0p9-prefab-drift-guard`) — the guard had to red on drift as well as green on match, and the peer reproduced both halves.** `check_committed_lineup.py 3992e96` (= `origin/main` at the time) → **exit 1**, `[lineup-drift] FAILED -- ... is MISSING 5 generator node(s)`; `check_committed_lineup.py 917d2c2` (the re-bake commit) → **exit 0**, *"carries all 15 generator node(s)"*. Both re-run by the reviewer in his own worktree, not read off the body. **And her fixture fix was re-verified by deliberately breaking it:** the `LU_NOROW` `sed` was re-broken to the no-op shape (`AddRow( root.transform`, the whitespace mismatch that had silently no-op'd during authoring) in a scratch copy → **`180 passed, 2 failed`**, the pin failing **by name**. Bounded in the same breath: in *that* mutation the negative case also failed, so the hole was not silent there, and the ORIGINAL silent shape was never committed and could not be reproduced — so no claim was made about it.
   **What this instance contributes to the rule: cite the red at an ABSOLUTE ref.** #370's body quoted its red as `HEAD~1`. At the branch's final head `f058ac7`, `HEAD~1` had become `917d2c2` — the **green** commit — so the body's own red command re-runs *green*, and a future reader re-running it verbatim concludes the red evaporated (reviewer nit N2 on that PR).

3. **PR #369 (OPEN, branch `devon/86caynve9-ci-wire-verifyswings`) — mutation-testing the WIRING, in both directions, instead of trusting a clean merge.** After a zero-conflict `git merge origin/main`: dropping `verify_swings_gate.sh` from `WINDOWED_GATES` → **`185 passed, 1 failed`**, `[FAIL] gate-wiring: verify_swings_gate.sh is not in HEADLESS_GATES or WINDOWED_GATES`; renaming both `scripts/verify_swings_gate.sh` occurrences in `ci.yml` → **`186 passed, 1 failed`**, `[FAIL] gate-wiring: verify_swings_gate.sh exists but ci.yml NEVER invokes it`. Unmutated baseline on the merged tree: **`188 passed, 0 failed`**; both mutations restored and the tree verified clean before commit. A third mutation covered the gate's own *content*: commenting out its four in-block needles made the gate exit **`rc=0` on a run that measured nothing** — that `rc=0` *is* the false-green — and the presence-assert reds → `185 passed, 3 failed`.
   **The causal link — the strongest part of this provenance.** That was done BECAUSE **#351 had just shown a clean merge leaving a gate registered nowhere**, and the claim was re-verified from git rather than from the PR text: on `drew/86cah7y5b-find-in-world`, `.github/workflows/scripts/verify_weaponfind_gate.sh` **exists** at `43ddeee` and at `259d890`, while `tests/scripts/test_gate_scripts.sh` at those **same two commits** contains **zero** occurrences of `weaponfind` — the gate was in neither launch-mode list — and the wiring assertion that would have said so was itself absent from the branch at both (`grep -c 'is not in HEADLESS_GATES or WINDOWED_GATES'` → **0**). That assertion reached `main` with PR #355 (`a5fee62`) and reached the branch on merge commit `61ff213`, where `weaponfind` appears **5×**. So the gate script sat in the branch attached to nothing across both commits — and git merged the file cleanly the whole time, because **registration is invisible to a merge**.
   ⚠ **"shipped, green … for two commits" was an over-claim and is withdrawn — re-measured 2026-07-31.** Two separate errors, both of the class this very section prohibits. **(a) The green covers ONE commit, not two.** `259d890` genuinely has it: `gh api repos/TSandvaer/Far-Horizon/commits/259d890/check-runs` → 4 runs, all `conclusion: success` (`build`, `capture`, `structure`, `playmode`). `43ddeee` has `{"total_count":0}` — **no check run ever existed for it**, so calling it green reads a pass off an *absence*, which rule 1 of § What CI actually covers ("an absent check is never a pass") forbids by name. **(b) Nothing "shipped":** PR #351 is still **OPEN** (`mergedAt: null`), so the gate never reached `main` at all — the two commits are branch-only. The surviving claim is the one that carries the lesson and needs neither: at **both** commits the gate file existed while `weaponfind` appeared **0×** in `test_gate_scripts.sh` and the wiring assertion was absent (`grep -c` → 0 at each) — registration, not greenness, is what the merge could not see.
   ⚠ **One figure from `86caz5nr2`'s body is deliberately NOT repeated here:** the "caught at `170p/1f`" count. It appears in neither #351's body, reviews nor comments, nor in #369, `team/STATE.md` or `team/DECISIONS.md`. The git measurement above replaces it. Cite that, not the unverifiable count.

### What SATISFIES the rule in practice — six items, all required

*"We tested both cases"* does **not** pass. Neither does *"verified it reds on drift"*, *"the negative case passes"*, or a table of pre-registered expectations with ✅ in the observed column. Those assert the conclusion and withhold the artifact. What passes:

1. **The mutation, NAMED and QUOTED as an edit** — the exact change that should make the check fail, shown as the line/diff you applied, not described. *"Broke the `sed`"* is not a mutation; *"`LU_NOROW`'s `sed` re-broken to `AddRow( root.transform`, three spaces where the fixture has one, so it no-ops"* is. If the mutation is a deletion, quote what was deleted and from where (`dropped verify_swings_gate.sh from WINDOWED_GATES`).
2. **The RED, quoted VERBATIM as the tool emitted it** — including the check's own identifying token and the counts: `180 passed, 2 failed` plus `[FAIL] gate-wiring: verify_swings_gate.sh is not in HEADLESS_GATES or WINDOWED_GATES`. **A bare exit code is not a red** — exit codes lie in both directions in this repo (rubric item 2's `-testResults` rule; the `grep -v` trailing-exit-1 trap in § Doc-staleness greps; the `rc=0`-measuring-nothing case in #369).
3. **The GREEN, quoted verbatim, from the SAME tree** — the unmutated run (`186 passed, 0 failed`). *Same tree* means the mutation is the only difference, and you must **prove** it: md5 of the mutated file on both sides, or a `git diff` showing the single insertion/deletion (#363 did both). Then **prove the restore**: md5s back to baseline and `git diff HEAD` empty. If the two halves came from different environments (one local, one CI), say so and state what that leaves unproven. **If the mutation changes the total case count, say why in the same breath** — #369's baseline was `188 passed, 0 failed` while its two mutation runs read `185 passed, 1 failed` and `186 passed, 1 failed`, because de-registering a gate removes the cases the wiring loop would have generated for it. Unexplained, three different totals read as unrelated drift and the reviewer cannot tell the red from noise.
4. **Absolute refs, never relative.** Quote `3992e96` / `917d2c2`, never `HEAD~1` / `HEAD^` / `@{u}`. A relative ref re-points as the branch grows and the evidence silently inverts — measured on #370 above.
5. **The red must fail BY NAME, and be the ONLY thing that fails.** The failing check has to identify itself, and no unrelated check may go red in the same run. *"Something went red"* proves the tree is broken, not that this gate discriminates. Where the guard covers a family (N gates, N docs, N assets), mutate at least **two** members independently and show each produces exactly one `[FAIL]` naming its own member — that is what rules out a fix shaped to one instance (#363's boar + weaponset).
6. **TWO reds, not one — DISCRIMINATION and WIRING.** These fail independently, so one red does not imply the other. **Red A (discrimination):** feed the check a bad input; the check fails. **Red B (wiring):** remove the gate's registration or its invocation; something fails. A gate can discriminate perfectly and be attached to nothing — that is precisely #351. #369 shipped both reds; a PR that ships only Red A must say so and state that the wiring half is unproven.

### The gate whose red is expensive — and the one that cannot be made red at all (the exception ladder)

**No exception is granted for the red.** What is negotiable is the **LAYER** at which it is produced. Take the cheapest rung that still produces a real failure, **name the rung you used in the PR body, and state what that rung does not cover.**

- **Rung 1 — mutate the real subject.** `cp -p` byte-copy backup → mutate → run → restore → prove the restore (md5s back to baseline, `git diff HEAD` empty, `git status --porcelain` showing only pre-existing untracked files). The default. Never mutate the tracked tree without a verified restore, and never as a commit.
- **Rung 2 — mutate a synthetic fixture at the seam the gate consumes.** Use when rung 1 needs a scarce resource: the single Unity build slot, a licence, the self-hosted runner, or a destructive/irreversible action. This is already the house idiom, not an invention — `tests/scripts/test_gate_scripts.sh` carries `make_min_repo()` (:274) and `make_fake_exe()` (:464) and drives `fake_pass` / `fake_nochange` / `fake_silent` / `fake_identical` / `fake_devzero` / `fake_norows` / `fake_crush` fixtures through the real gate scripts; `frame_check.py` and `parse_test_results.py` both take their input paths from `sys.argv`, so a hand-made black PNG or a fixture results XML reds them without a build. **Mandatory disclosure:** a rung-2 red proves **discrimination only**. It says nothing about whether the gate runs on the real subject — so **Red B (wiring) is still required**, and Red B is cheap even when the subject is not (it is a one-line edit to a list or to `ci.yml`).
- **Rung 3 — the red cannot be constructed at ANY layer.** Then the thing is **not a gate**, and the rule does not exempt it — it withdraws the *word*. Keep the check if it is useful, but: **(a)** label it in-code / in-table as `UNPROVEN — no red demonstrated; do not cite this green as gate evidence`; **(b)** no PR may cite its green as gate evidence, and no ticket may close on it; **(c)** file a follow-up ticket naming the **specific mechanism** that makes the red unconstructible. An un-reddable check is a smoke test, not a gate; calling it a gate is the false-confidence generator (`LeftHaftPassSW` printed `PASS ✓` for three rounds on a defect it could not see).

**"The red is expensive" is never a rung-3 reason.** Cost moves you DOWN the ladder, not off it. Rung 3 requires the red to be *unconstructible*, and you must say why in one sentence a reviewer can check.

**Measured claim, re-measured at `0f14b4f` (2026-07-31): rung 3 is empty for FIVE of the six rows in § test-evidence convention's mechanical-gate table — the sixth is unmeasured, not exempt.** Those five carry a rung-1-or-2 harness in `tests/scripts/test_gate_scripts.sh` — `capture_gate` 14 references, `frame_check` 6, `structure_check` 4, `parse_test_results` 2, `check_unity_log` 2, `check_docs_markup` 2 (all six counts re-reproduced at `0f14b4f`; `capture_gate` + `frame_check` are the two scripts of the single Shipped-build-capture row).
⚠ **The sixth row — Build-result — was wrongly counted and is withdrawn.** Its cited "needle 1" is `tests/scripts/test_gate_scripts.sh:134`, and that line is `'[FarHorizonBuilder] result=Succeeded size=54000000' \` — one filler line inside the `printf` that builds `corrupt_clean.log`, the **green-side must-not-false-positive fixture for a DIFFERENT gate** (`check_corrupt_build.sh`, `:103`; the assert at `:136` demands `rc 0` + `CORRUPT-BUILD GATE PASSED`). It exercises the build-result gate not at all, and `test_gate_scripts.sh` carries no other `FarHorizonBuilder`-result harness at `0f14b4f` (`grep -n FarHorizonBuilder` → `:134` plus `:315`/`:316`, the latter two a `make_min_repo` stub file). A presence-grep was standing in for discrimination — **the exact substitution this section exists to prohibit, committed inside the section that prohibits it.** The row's correct status is **unmeasured**: nobody has attempted its red, which is not the same as its red being unconstructible.
**Bounded honestly:** the five counts measure that *a fixture harness exists per row*, not that each row's red has been demonstrated *and quoted on a PR* — the latter is unknown and is not claimed here. If you believe your gate is the first rung-3 case, that is a strong claim: state the mechanism and expect it to be challenged.

### The wrapper-layer sibling (already captured in `unity-conventions.md`; same principle one layer out)

`.claude/docs/unity-conventions.md` § "CI architecture — merge-gate vs advisory PlayMode" carries this rule applied to the **wrapper** layer: *"A CI wrapper around a SELF-ASSERTING capture component must gate on LOG-EVIDENCE TOKENS, not the exit code — the component may deliberately treat a missing precondition as a WARNING, so a green exit proves nothing about whether the assertion ever ran"* (`SwingVerifyCapture`, PR #369), whose stated rule is: *"for any verify component that treats a missing precondition permissively, the CI wire must positively confirm the PASS tokens fired AND that no skip/silent-guard path was taken."* Same failure, one layer out: there the green is produced by a component that *defaults its criteria to pass*; here the green is produced by a gate that *is not wired to anything*. Both are greens that report on nothing, and the cure in both cases is to make the silent path produce a red. Cite that entry by **§ anchor, never by line number** — it is currently the file's last line and every insertion above it moves it.

**Enforcement.** Tess bounces (REQUEST_CHANGES, one-line note: *"Gate-RED: guard added/amended with no quoted red + green from the same tree and a named mutation"*) any PR that introduces or amends a guard without the six items. A reviewer who cannot re-run the quoted red from the PR body — because the mutation is described rather than quoted, or the ref is relative — treats that as the same bounce. **Composes with, does not replace:** § Gate-trust contrastive-pair test (that is the *reactive* half, below), § Doc-staleness greps (the docs-lane specialization), and § Multi-step-loop coverage's "Success-test discipline" (the loop-seam specialization).

---

## Gate-trust contrastive-pair test (when a CI gate smells false-green)

> **Relation to the standing rule above.** This section is the **reactive** half — the re-verification procedure for an EXISTING gate that has fallen under suspicion. § "A gate is not a gate until its RED has been demonstrated" is the **proactive** half, and is mandatory at authoring time for every new or amended guard. The "not as a standing per-PR gate" line at the end of this section scopes *this* suspicion-triggered procedure; it does **not** relax the authoring-time requirement.

A green gate is only worth trusting if it can go RED. When a verify-gate is SUSPECTED of false-green — it passes but the symptom it is supposed to catch is present in the soak (`unity-verify-gate-false-green` — a stale pre-gate verify-log poisoned the evidence; the false-green hid the #130 pond foam for 3 rounds via a warm `ci-out/` + `clean:false`) — do NOT just re-read the gate. PROVE it discriminates with a pre-registered pair:

1. **Pre-register (in writing, BEFORE running):** state what the gate SHOULD do on a case it must PASS vs a case it must FAIL — e.g. "clean build WITH the fix → PASS; clean build with the fix REVERTED → FAIL." If you cannot state how a passing vs a failing run look different, the gate is unfalsifiable and its green means nothing.
2. **Run BOTH** — the should-pass case and the should-fail case — from the SAME clean state (cold `ci-out/`, `clean:true`; a warm cache is exactly how the false-green slips in — see `unity-procedural-committed-assets-go-stale`).
3. **Verdict:** the gate is trustworthy ONLY if the should-fail case actually goes RED. If both go green, the gate is not discriminating — fix the gate before trusting ANY of its greens. Trust the POST-gate artifact log + a UTF-16LE DLL string search over a managed assembly (ASCII grep/`strings` lies on wide literals), never a pre-gate `ci-out/` upload.

Borrowed from the reference "earned-autonomy" suite's `probe` skill (contrastive-pair + pre-registration), applied to **gate-trust** rather than agent-reasoning. Use it the next time a soak contradicts a green gate, not as a standing per-PR gate.

---

## Doc-staleness greps — negate the marker context, and DEMONSTRATE the red (ticket `86cayw6ve`)

The sibling of the section above, for the **docs** lane. A docs ticket's success test is usually a grep — *"`grep '<retired phrase>' <doc>` → zero hits."* That form is **unpassable by construction** the moment the doc does the right thing, because the two legitimate shapes both CONTAIN the phrase:

- a **`⚠ CORRECTED` retirement marker**, which quotes the wording it retires *in order to* retire it (the house pattern this doc family merged — `team/uma-ux/item-icon-bake-recipe.md` §2.1's markers quote the retired axe descriptor verbatim); and
- a **DECISIONS citation**, which is accurate AS HISTORY and is protected precisely because rewriting it falsifies the record.

**Why an unpassable test is worse than no test.** A future reader does one of two things with a guard that can never go green: deletes it (the guard is lost), or "fixes" the doc to satisfy it — striking the correction marker or the DECISIONS citation. **The second outcome is the damaging one:** it destroys the record of what was wrong, which is the whole reason the marker exists. Observed on `86caynyq7` / PR #358: `grep -n 'slate/steel' team/uma-ux/ui-toolkit-panels-ux-spec.md` → zero hits reported **2 hits before AND after** the fix, and satisfying it literally would have required breaking two of the ticket's own constraints to satisfy a third.

**The keepable form — negate the CONTEXT MARKER, never the line number:**

    grep -n -i '<retired phrase>' <doc> | grep -v 'CORRECTED\|DECISIONS'   # → expect ZERO LINES

- **Judge the OUTPUT, not the exit code** — a clean run's trailing `grep -v` exits **1**, so an exit-code check reads a passing guard as a failure (same family as "grep the `-testResults` XML's `result=` line, exit codes lie" in item 2 of the rubric).
- **Match the phrase case-INSENSITIVELY (`-i`); keep the EXCLUSION case-SENSITIVE.** A retired phrase's natural shape at a sentence start is *capitalized*, so a case-sensitive match walks straight past a live claim that opens with it. Measured on this doc family: `Slate/steel is the current axe-icon palette. Bake it.` → `lines=0` without `-i`, `lines=1` with it. **The reason the asymmetry runs this way is MONOTONE, not conventional — which is why it generalises to ANY two-stage grep guard rather than only to this doc family.** Case-folding can only ever GROW the set selected by the grep it is applied to, and the two greps' sets point opposite ways: `-i` on the MATCH grep grows the MATCH set, so its worst case is a false POSITIVE — the guard goes **RED: loud, on the PR, and a human looks at it**; `-vi` on the EXCLUSION grep grows the EXCLUDED set, so its worst case is a false NEGATIVE — the guard goes **GREEN: silent, and nobody ever looks again**. A loud wrong answer costs one glance to dismiss; a silent wrong answer costs the entire guard, permanently and undetectably. So when a widening decision is available, take the direction whose failure mode is loud — every time, in any guard of this shape. The concrete instance that makes that abstract reason legible *here* is that the marker words are an ALL-CAPS convention (`CORRECTED` / `DECISIONS`), so folding case into the *second* grep silently EXCLUDES any live-claim line that merely contains lowercase prose "decisions"/"corrected": `Bake decisions aside, the axe icon is Slate/steel today.` is CAUGHT by `grep -v` (`lines=1`) and MISSED by `grep -vi` (`lines=0`). And the hazard is not hypothetical at this repo's scale — re-measured at `b9abf7b` (`git grep -l -E '(^|[^A-Z])decisions([^A-Z]|$)' -- 'team/**.md'`): **16 files**, i.e. **14** once the doc under policy and this rulebook itself are set aside, carry a lowercase prose `decisions`, so a `grep -vi` exclusion would silently swallow a live claim in any of them. **Widen the match; never widen the exclusion.**
- **Anchor on the marker word** (`CORRECTED` / `DECISIONS` / `RETIRED`), **never on a line number.** Line numbers move; `86caynyq7` deliberately refused to quote its own pre-merge anchors as current for exactly this reason.
  - ⚠ **Scope, because this bullet was read wider than it governs** (raised on `86cazmcg1` against § BAR 1's glob table, which cites `test_gate_scripts.sh:912` / `:918` inside this same file). **This bullet governs what the GUARD greps — its exclusion anchor — not how prose cites a source location.** An exclusion pinned to a line number silently stops excluding the moment the file grows, and the guard goes quietly green; that is the failure being prohibited, and it is a property of the *grep*, not of citation style.
  - **The form this file uses for prose citations of code and config, so the two are not confused:** the symbol beside the number, sha-pinned, with the name load-bearing. That is not minted here — it is already the merged house form for these same two arrays, at `.claude/docs/unity-conventions.md` § "Headless / CLI rituals" (`:13` at `2d0658b`): *"re-find by NAME if the numbers have drifted again — … a range that no longer contains `HEADLESS_GATES=(` / `WINDOWED_GATES=(` is stale, not empty."* A number carried this way degrades to a hint; a bare number degrades to a wrong answer with no signal that it is wrong.
  - ⛔ **This does NOT settle a project-wide citation convention.** That is `86cazhtfy` — the Sponsor's, and being re-scoped rather than adopted as written: PR #396 (`86caz4yhj`, measured at `8d355d0`) found that of **43** drifted anchors on `main`, a bare §-anchor rule would have made exactly **1** immune, because the drift is overwhelmingly into code and CI config rather than prose. Cite that measurement as the input to the decision; do not read the bullet above as the decision.
- **Count LINES, not occurrences.** The guard is line-scoped, so a line count is the only thing its output evidences. A doc can legitimately gain an occurrence — a marker or citation quoting the phrase once more — with the line count unchanged, so reporting "2 → 2" without saying *lines* overstates what was verified. When the two numbers diverge, state both and label which is which.
- **State the real bar in words too:** "no line *asserts* the retired phrase as CURRENT" — that is the criterion; the grep is only its mechanical proxy.

**The gate — a staleness grep is not keepable until its RED has been DEMONSTRATED.** This is § "A gate is not a gate until its RED has been demonstrated" applied to the docs lane — that section's six items bind here too (quote the injected claim as the named mutation, quote the emitted line as the red, quote the zero-line run as the green, cite an absolute ref). Same discipline as the contrastive-pair section above, at docs cost: inject a live claim carrying the phrase into the file, run the guard, confirm it emits the line, revert. Then **state in the PR body which half you actually proved** (green-on-current-tree, red-on-injected-claim, or both). A guard that cannot fail is a false-confidence generator — the `LeftHaftPassSW` cap printed `PASS ✓` for three rounds on a defect it could not see.

**Disclose the residual hole.** The `grep -v` form is line-scoped, so a live claim reintroduced ON a line that also contains `CORRECTED` or `DECISIONS` still slips through. That narrows the hole; it does not close it (closing it needs sentence-level parsing — deliberately not built). Say so when citing this pattern, the same way the source-scan-guard entry in `.claude/docs/unity-conventions.md` states that per-file counting narrows but does not close its hole.

**And disclose what `-i` does NOT buy you.** The case-fold closes the CASE variant of the hole and nothing else — measured with `-i` in place: a live claim sharing a line with `DECISIONS` (`Per DECISIONS, the axe icon is Slate/steel today — bake it that way.`) is still missed (`lines=0`), and so is the `CORRECTED` arm (`lines=0`); a re-spaced variant (`slate / steel`) is still missed too, because a literal grep matches bytes, not sentences. Adding `-i` therefore does not let you drop the residual-hole disclosure — it makes the disclosure *shorter by exactly one case*.

**⚠ And one about this rulebook itself.** The illustrations above quote the retired phrase literally (they are injections, not claims). ⚠ **CORRECTED (`86cazmcg1`, measured at `2d0658b`, i.e. BEFORE the edits in this PR — the miscount is pre-existing, not introduced here).** This paragraph used to say *"the guard emits exactly one line, the case-insensitivity bullet above"*, and therefore that *"this section does not pass the guard it documents."* Both are wrong. Run against this file the guard emits **ZERO** lines: the phrase appears on **3** lines, and every one of the three also carries an ALL-CAPS `CORRECTED` or `DECISIONS` — the case-insensitivity bullet included, because that bullet names both marker words in caps while illustrating them — so the exclusion removes all three. **What that actually demonstrates is the residual hole disclosed two paragraphs above**, seen from the inside: a line that carries a marker word is invisible to this guard whatever else is on it. It is not a licence to aim the guard here — it is still scoped to the doc under policy (`<doc>`), never to this file. Don't aim it here, and don't "fix" the illustrations to make it quiet.
