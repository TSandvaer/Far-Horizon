Far Horizon castaway — asset attribution & provenance
=====================================================

STATUS 2026-07-30. Four castaway versions are committed in this folder. The AUTHORITY for which one
is LIVE is Assets/Scripts/Editor/CharacterAssetGen.cs — never this file. Re-check the three toggle
constants quoted below whenever any of them changes.

WHAT SHIPS TODAY
  LIVE hero mesh    v4/castaway_v4_rigged.fbx    (+ v4/castaway_v4_palette.png)
  ROLLBACK target   v3/castaway_v3_rigged.fbx    (+ v3/texture_diffuse_posterized.png)
  Next fallbacks    v2/castaway_rigged_tpose.fbx, then Idle.fbx (v1) — both still committed
  Animation         the Mixamo clip FBX in this folder — shared by EVERY hero version

  CharacterAssetGen.FbxPath (:228-231) resolves HIGHEST-VERSION-FIRST off three constants:
    UseCastawayV4Default = true  (:217)  -> v4 selected                       = LIVE
    UseCastawayV3Default = true  (:164)  -> selected if v4 is flipped false   = ROLLBACK TARGET
    UseCastawayV2Default = true  (:130)  -> selected if v4 AND v3 are false
    (no toggle)                          -> Idle.fbx (v1) if all three are false
  v4 became the default 2026-07-19 (activation ticket 86catvb6u). v1/v2/v3 are deliberately NOT
  deleted — a rollback is a one-line flip of the corresponding const.

ATTRIBUTION — WHAT THIS BUILD OWES CREDIT FOR

  Retain this attribution in any distribution of the game (an in-game / about-screen credits
  entry covers it).

  Third-party, in use regardless of which hero version is live:
    - Mixamo animation clips (Adobe, free account) — every clip FBX in this folder, played on
      the selected hero mesh by CastawayAnimator.controller.
    - The Mixamo auto-rigger skeleton + skin weights (Adobe, free account) — EVERY hero
      version, v4 included, is rigged by Mixamo (mixamorig:* Standard skeleton).
  Third-party, committed here — credit retained regardless of which version is selected:
    - v1 / v2 / v3 hero meshes + their diffuse/normal maps — GENERATED 3D content
      (Hyper3D Rodin, Creator-tier web export), from openai-image A-pose concept references.
      None of these is the live hero mesh today, but all three stay in the repo as the rollback
      chain, so the credit stays. Whether an UNSELECTED FBX is included in a given built player is
      a Unity build-inclusion question this file deliberately does not assert either way.
  IN-HOUSE, no third-party credit owed:
    - the v4 hero MESH (hand-modeled in Blender in this project, driven through the Blender MCP
      tooling) and its palette texture — ticket 86catpwc4, look-dev approved 2026-07-18. Neither
      was generated by a third-party service nor sourced from an asset library.

  NOT a Creative Commons obligation. No castaway asset in this folder is CC-BY or otherwise
  CC-licensed, and no *_License_CC-Attribution.txt ships for the castaway — this file is the
  only attribution/licence file tracked anywhere under Assets/ (checked 2026-07-30). Do NOT add
  or "restore" a CC claim here. The project's two retired CC-BY assets were the Sketchfab axe
  (Viktor.G) and the "Mini Chibi Kid" base; both were replaced in-house and are gone from the
  tree (ticket 86cabh907 removed the axe + its licence file).
  OPEN QUESTION (unverified — do not assert either way): the exact licence / terms-of-use text
  behind the retain instruction above is not recorded anywhere in this repo. Treat the retain
  instruction as the operative rule, and re-read the Hyper3D Rodin (Creator tier) and Adobe
  Mixamo terms before any public distribution.

RIG = GENERIC, NOT Humanoid (ticket 86ca8rdkp). The Mixamo Humanoid muscle-space retarget
EXPLODED the skinned mesh into a cone at runtime under the scaled scene hierarchy; GENERIC binds
each clip by TRANSFORM PATH onto the FBX's own mixamorig skeleton (no muscle retarget) and renders
clean. The with-skin mesh FBX imports CreateFromThisModel (its own avatar); the without-skin clip
FBX each CreateFromThisModel too and bind by transform path onto the mesh — NO CopyFromOther.

Castaway v1 (original Rodin base) — files in this folder:
  STATUS 2026-07-30 — SUPERSEDED, still committed. Selected only if ALL THREE version toggles are
  flipped false (CharacterAssetGen.FbxPath, :228-231). Its own Idle take is unused even when v1 IS
  selected — the Idle STATE plays the Breathing Idle clip (CharacterAssetGen.cs:239). The file
  inventory below stays as the rollback record.
  Route: Hyper3D Rodin (Image-to-3D, Gen-2.5 Quad ~8000) -> Mixamo auto-rig (Standard skeleton) ->
  Unity GENERIC rig, from an openai-image A-pose reference. Full recipe:
  .claude/docs/character-pipeline.md.
  - Idle.fbx              — WITH skin: the rigged mesh (mixamorig Standard skeleton, rootBone
                            mixamorig:Hips) + an Idle take. The Idle STATE now plays the Breathing
                            Idle clip; Idle.fbx supplies the MESH + rig + avatar.
  - Walking.fbx / Running.fbx / Jump_*.fbx / Melee_Attack.fbx / crouch + hit-react FBX
                          — WITHOUT skin (Melee/Idle carry skin): clips only, bound by transform
                            path onto Idle's mesh (Generic — no retarget).
  - texture_diffuse.png   — de-lit toon color albedo (the _BaseMap). The v1 IDENTITY RECOLOR (warm
                            the mustard-yellow shirt toward tan) is a luma-preserving HSV remap baked
                            into this PNG by CharacterAssetGen.RecolorShirtToTan (v1-only).
  - texture_normal.png    — normal map (bound at low strength; the albedo carries shading).

Castaway v2 (Rodin base — ticket 86cajwp23, Sponsor-approved 2026-07-05) — files in v2/:
  STATUS 2026-07-30 — SUPERSEDED, still committed. UseCastawayV2Default is true, but v4 and v3 both
  win ahead of it in FbxPath, so v2 is selected only if BOTH of those consts are flipped false.
  History: v2 was promoted from staged-toggle to LIVE default 2026-07-05 (ticket 86cajx050), then
  superseded by v3 2026-07-06 and by v4 2026-07-19.
  A NEW hero base (bearded rugged adult survivor), same Hyper3D Rodin -> Mixamo -> Unity GENERIC
  route, generated from a fresh A-pose concept. Source-of-truth + provenance:
  art-src/castaway-rodin-export/README.md.
  - v2/castaway_rigged_tpose.fbx — WITH skin: the Rodin mesh + Mixamo Standard skeleton (41 bones,
                            no middle/ring fingers — a subset of the v1 clip skeleton, so the 18
                            existing clips bind by transform path with NO retarget). Imported GENERIC
                            + CreateFromThisModel, height-normalized to ~1.0u.
  - v2/texture_diffuse.png — de-lit toon albedo (Rodin De-light pass). NO shirt-recolor (v2 has no
                            shirt) — RecolorShirtToTan is retired for v2.
  - v2/texture_normal.png  — normal map (bound at low strength).

Castaway v3 (Rodin Smart-Low-poly base — ticket 86cak41d4) — files in v3/:
  STATUS 2026-07-30 — ROLLBACK TARGET, still committed. UseCastawayV3Default is true; v3 is what
  FbxPath selects the moment UseCastawayV4Default is flipped false. v3 was the LIVE default from
  2026-07-06 (activation ticket 86cak9kau) until v4 took over 2026-07-19.
  Same Hyper3D Rodin -> Mixamo -> Unity GENERIC route as v1/v2, but taken from Rodin's
  Smart-Low-poly(BETA) Type=Quad Density=Low output (3,605 quads / ~7.4k tri-equiv of REAL facet
  geometry — not a decimated high-poly), from a fresh openai-image A-pose concept. Source-of-truth
  + provenance: art-src/castaway-v3-rodin-export-lowpoly/README.md.
  - v3/castaway_v3_rigged.fbx — WITH skin: the Rodin mesh + Mixamo Standard skeleton (41 bones incl.
                            16 finger bones, root mixamorig:Hips, all verts weighted), so the
                            existing WITHOUT-skin clips bind by transform path with NO retarget.
                            Imported GENERIC + CreateFromThisModel.
  - v3/texture_diffuse_posterized.png — HSV-posterized flat diffuse (URP/Unlit _BaseMap). NO normal /
                            metallic / roughness map (the weapon-family flat-palette material
                            contract, 86cak3r3k). A URP/Lit-no-normal variant is reachable via
                            FARHORIZON_CASTAWAY_V3_LIT=1 (CharacterAssetGen.cs:174-176).

Castaway v4 (chamfered-blocky "wooden toy" hand-model — ticket 86catpwc4) — files in v4/:
  STATUS 2026-07-30 — LIVE (default hero). UseCastawayV4Default = true (CharacterAssetGen.cs:217),
  flipped 2026-07-19 by activation ticket 86catvb6u after the Sponsor soaked v4 in a shipped build.
  PROVENANCE — each claim below is from CharacterAssetGen.cs:178-220, tickets 86catpwc4 / 86catvb6u,
  or .claude/docs/character-pipeline.md. It is NOT the Rodin route v1/v2/v3 used; do not read the
  Rodin lines above as covering v4.
    Mesh      IN-HOUSE. Hand-modeled in Blender in one session, driven through the Blender MCP
              tooling (Sponsor-directed; look-dev approved 2026-07-18 via a blockout-silhouette gate
              then a 6-render judge set). 40 rigid segmented blocks joined into ONE mesh for
              the Mixamo upload; 960 verts / 1,760 tris; ~3.9 heads tall / 1.90 m in Blender;
              mitten+thumb hands, painted face patch + geometry nose. Same castaway IDENTITY as v3,
              restyled geometric. No third-party mesh licence applies.
    Texture   IN-HOUSE. v4/castaway_v4_palette.png — a flat palette authored alongside the mesh in
              the same Blender session, bound as the URP/Lit _BaseMap (CharacterAssetGen.cs:205).
    Rig       THIRD-PARTY — Mixamo (Adobe, free account). A FRESH Mixamo Standard auto-rig run on
              our OWN Blender FBX export (the first character through our exporter; Sponsor manual
              step, 86catpwc4 phase B). Verified: root mixamorig:Hips, 33 bones (v3 = 41; the delta
              is finger geometry — v4 has only the Index chain per hand and NO thumb bones), 0
              unweighted verts, single skinned mesh CastawayV4, material CastawayV4Palette.
    Animation THIRD-PARTY — the same Mixamo clip FBX in this folder, unchanged. Every core mixamorig
              bone those clips drive is present on v4, so they bind by TRANSFORM PATH with NO
              retarget (Generic), exactly as on v2/v3.
    Unity     Imported GENERIC + CreateFromThisModel (the anti-Humanoid recipe, 86ca8rdkp).
  - v4/castaway_v4_rigged.fbx  — WITH skin: the Mixamo-returned mesh + rig (its idle take is unused).
  - v4/castaway_v4_palette.png — the flat palette texture (URP/Lit _BaseMap).
  DO NOT re-export this rigged FBX through Blender. A Blender round-trip re-derives every bone's
  rest orientation and produces a whole-skeleton "helicopter" (ticket 86cau4za2 / PR #330, CONFIRMED;
  guarded by a v7700 FBX-version EditMode canary). Known accepted defect, Sponsor-deferred: the RIGHT
  hand's thumb weights sit on the index chain with no thumb skin cluster (86cau4za2).
  OPEN QUESTION (unverified): v4 has NO committed provenance README, unlike v2 and v3.
  CharacterAssetGen.cs:201 names "art-src/castaway-v4-export/" as the source-of-truth export, and
  character-pipeline.md names art-src/castaway-v4-README.md, art-src/castaway_v4.blend and
  art-src/castaway_v4_palette.png — none of those paths are committed (checked 2026-07-30). v4's
  provenance therefore rests on code comments + the two tickets above, not on a committed source
  artifact. Do not treat the missing README's absence as evidence of a different route.
